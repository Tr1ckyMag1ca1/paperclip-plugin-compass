---
phase: 08
plan: 07
type: auto
wave: 3
depends_on: [08-04, 08-05, 08-06]
files_modified:
  - src/ui/found/VisionPreview.tsx
  - src/ui/found/ApplyProgress.tsx
  - src/ui/found/ApplyErrorDisplay.tsx
  - src/ui/found/ProvisioningSummary.tsx
  - src/ui/found/ConfirmationModal.tsx
autonomous: true
requirements: [UIFM-02, UIFM-03]
user_setup: []

must_haves:
  truths:
    - "VisionPreview uses Card variant=muted + SectionHeader per section (D-14)"
    - "ApplyProgress uses Card variant=default with linear progress bar track (D-15)"
    - "ApplyProgress fill: neutral foreground (in-flight, not emerald); done steps use emerald CheckCircle (D-15)"
    - "ApplyErrorDisplay is inline alert, not modal: bg-red-500/10 border-red-500/30 text-red-600 (D-16)"
    - "ProvisioningSummary uses Card variant=default with SectionHeader (D-14)"
    - "ConfirmationModal uses unified modal chrome (D-09, D-10): same as Plan 4 modals"
    - "All components work correctly in light and dark modes"
  artifacts:
    - path: "src/ui/found/VisionPreview.tsx"
      provides: "Vision preview with Card muted + SectionHeader"
      exports: ["VisionPreview"]
      contains: "Card.*muted|SectionHeader"
    - path: "src/ui/found/ApplyProgress.tsx"
      provides: "Apply progress bar with step indicators"
      exports: ["ApplyProgress"]
      contains: "Card|progress.*foreground|text-emerald-500.*CheckCircle"
    - path: "src/ui/found/ApplyErrorDisplay.tsx"
      provides: "Inline error alert"
      exports: ["ApplyErrorDisplay"]
      contains: "bg-red-500/10.*border-red-500/30"
    - path: "src/ui/found/ProvisioningSummary.tsx"
      provides: "Provisioning summary with Card + SectionHeader"
      exports: ["ProvisioningSummary"]
      contains: "Card.*SectionHeader"
    - path: "src/ui/found/ConfirmationModal.tsx"
      provides: "Confirmation modal with unified chrome"
      exports: ["ConfirmationModal"]
      contains: "bg-background/80.*backdrop-blur-sm.*bg-card"
  key_links:
    - from: "src/ui/found/VisionPreview.tsx"
      to: "src/ui/primitives/Card.tsx|src/ui/primitives/SectionHeader.tsx"
      via: "component composition"
      pattern: "Card|SectionHeader"
    - from: "src/ui/found/ApplyProgress.tsx"
      to: "host CSS variables"
      via: "progress bar styling and step indicator colors"
      pattern: "bg-foreground.*foreground.*text-emerald-500"

---

<objective>
Migrate apply gate components (VisionPreview, ApplyProgress, ApplyErrorDisplay, ProvisioningSummary, ConfirmationModal) to host tokens; lock progress/error display patterns. Heavy Card + SectionHeader reuse (D-14, D-15). ApplyProgress fill uses neutral foreground (in-flight), emerald only for done steps (D-15, semantic lock). ApplyErrorDisplay is inline, not modal (D-16). ConfirmationModal reuses unified modal chrome from Plan 4 (D-09, D-10).

Purpose: Complete Found mode UI migration; establish progress bar and error display patterns; finalize confirmation flow. Prepare for founder visual verification via extended DualRenderProbe.

Output: Five TypeScript React components with host tokens and primitives, ready for integration testing and production verification (Phase 10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-assess-found-panels/08-CONTEXT.md
@.planning/phases/08-assess-found-panels/08-RESEARCH.md
@.planning/phases/08-assess-found-panels/08-UI-SPEC.md
@.planning/phases/07-foundations-shared-primitives/07-01-SUMMARY.md
@./UI_REDO_HANDOFF.md
@./CLAUDE.md
</context>

<interfaces>
Card + SectionHeader primitives (Phase 7, ready for reuse):
```typescript
export function Card({ variant?: "default"|"muted"|"elevated", padding?: "sm"|"md"|"lg" }): React.ReactElement { }
export function SectionHeader({ title: string, subtitle?: string, icon?: React.ComponentType, actions?: React.ReactNode }): React.ReactElement { }
```

Step interface (inferred from Found mode logic):
```typescript
interface Step { id: string; name: string; status: "pending" | "active" | "done"; }
```

Host tokens (verified Phase 7):
```
Surfaces: bg-card, bg-muted
Semantic: text-emerald-500 (done/success), text-red-600 (error)
Backgrounds: bg-red-500/10
Borders: border-border, border-red-500/30
Text: text-foreground, text-muted-foreground
Spacing: gap-2, gap-3, p-3, p-4, mt-2, mt-3
Modal: bg-background/80 backdrop-blur-sm, shadow-lg
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate VisionPreview to use Card muted + SectionHeader (D-14)</name>
  <files>src/ui/found/VisionPreview.tsx</files>
  <read_first>
    - src/ui/found/VisionPreview.tsx (current implementation, identify section structure)
    - src/ui/primitives/Card.tsx and src/ui/primitives/SectionHeader.tsx (Phase 7, available for import)
    - .planning/phases/08-assess-found-panels/08-UI-SPEC.md (VisionPreview section: "Card variant=muted with SectionHeader per vision section")
    - .planning/phases/08-assess-found-panels/08-CONTEXT.md (D-14 decision)
  </read_first>
  <action>
Migrate `src/ui/found/VisionPreview.tsx` to use Card + SectionHeader per D-14:

1. **Import:** Card and SectionHeader from primitives
2. **Structure per D-14:**
   ```tsx
   <Card variant="muted" padding="md">
     <SectionHeader title="Section Name" subtitle="Optional subtitle" />
     <div className="mt-3 text-sm">
       {/* preview content */}
     </div>
   </Card>
   ```
3. **Token migration:**
   - Container: Card handles bg-muted, border-border, rounded-none, p-4
   - Header: SectionHeader handles title/subtitle typography and layout
   - Content: text-sm, spacing (mt-3, gap-2, etc.) using Tailwind native scale
   - No broken tokens

4. **Component structure:** Wrapper for vision sections (likely array of sections); keep data structure, only update rendering.

Reference implementation: See 08-UI-SPEC.md "VisionPreview" and 08-CONTEXT.md D-14.
  </action>
  <verify>
    <automated>
      grep -c "import.*Card\|import.*SectionHeader" src/ui/found/VisionPreview.tsx && \
      grep -c "<Card.*muted\|variant=\"muted\"" src/ui/found/VisionPreview.tsx && \
      grep -c "<SectionHeader" src/ui/found/VisionPreview.tsx && \
      [ $(grep -c "gap-sm\|px-md\|p-md\|rounded-lg\|text-body\|text-heading" src/ui/found/VisionPreview.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Imports Card and SectionHeader from primitives
    - Uses <Card variant="muted" padding="md">
    - Uses <SectionHeader title="..." subtitle="...">
    - Internal spacing uses Tailwind native (mt-3, gap-2, text-sm)
    - No broken tokens (gap-sm, p-md, text-heading, etc.)
  </acceptance_criteria>
  <done>
    VisionPreview.tsx migrated to Card muted + SectionHeader pattern (D-14). Wraps vision sections with Card. Delegates header rendering to SectionHeader. Internal spacing migrated. No broken tokens. Ready for integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ApplyProgress with neutral-foreground fill + emerald done steps (D-15)</name>
  <files>src/ui/found/ApplyProgress.tsx</files>
  <read_first>
    - src/ui/found/ApplyProgress.tsx (current implementation, identify progress bar and step list)
    - .planning/phases/08-assess-found-panels/08-CONTEXT.md (D-15 decision: neutral fill, emerald for done only)
    - .planning/phases/08-assess-found-panels/08-UI-SPEC.md (ApplyProgress section: "Track bg-muted, fill bg-foreground, done steps CheckCircle text-emerald-500")
  </read_first>
  <action>
Migrate `src/ui/found/ApplyProgress.tsx` to host tokens with semantic progress/done color distinction (D-15):

1. **Import Card from primitives**
2. **Structure per D-15:**
   ```tsx
   <Card variant="default" padding="md">
     {/* Linear Progress Bar */}
     <div className="flex flex-col gap-3">
       <div>
         <span className="text-xs font-medium text-foreground">Progress</span>
         <div className="mt-2 h-2 bg-muted rounded-none overflow-hidden">
           <div
             className="h-full bg-foreground transition-all"
             style={{ width: `${percentComplete}%` }}
           />
         </div>
       </div>

       {/* Step List */}
       <div className="space-y-2">
         {steps.map(step => (
           <div key={step.id} className="flex items-center gap-2">
             {step.status === "done" && (
               <CheckCircle className="h-4 w-4 text-emerald-500" />
             )}
             {step.status === "active" && (
               <div className="h-4 w-4 text-foreground font-semibold" />
             )}
             {step.status === "pending" && (
               <div className="h-4 w-4 text-muted-foreground" />
             )}
             <span className={`text-sm ${
               step.status === "active" ? "text-foreground font-semibold" :
               step.status === "done" ? "text-foreground" :
               "text-muted-foreground"
             }`}>
               {step.name}
             </span>
           </div>
         ))}
       </div>
     </div>
   </Card>
   ```

3. **Token migration checklist:**
   - Container: Card variant=default handles bg-card, border-border, p-4
   - Progress bar track: bg-muted rounded-none h-2 (8px)
   - Progress bar fill: bg-foreground (neutral, in-flight, NOT emerald per D-15)
   - Done step icon: CheckCircle h-4 w-4 text-emerald-500 (success signal, per D-15)
   - Active step: text-foreground font-semibold (bold, neutral)
   - Pending step: text-muted-foreground (subtle)
   - Spacing: gap-3, space-y-2, mt-2 (Tailwind native)
   - No broken tokens

4. **Semantic lock (D-15 critical):** Progress fill MUST be neutral foreground, NOT emerald. Emerald reserved for done steps only. This visual distinction is load-bearing for Found mode UX.

Reference implementation: See 08-UI-SPEC.md "ApplyProgress" and 08-CONTEXT.md D-15.
  </action>
  <verify>
    <automated>
      grep -c "import.*Card" src/ui/found/ApplyProgress.tsx && \
      grep -c "<Card" src/ui/found/ApplyProgress.tsx && \
      grep -c "bg-foreground.*foreground\|bg-foreground.*fill" src/ui/found/ApplyProgress.tsx && \
      grep -c "text-emerald-500.*CheckCircle\|CheckCircle.*text-emerald-500" src/ui/found/ApplyProgress.tsx && \
      grep -c "bg-muted.*h-2" src/ui/found/ApplyProgress.tsx && \
      [ $(grep -c "gap-sm\|p-md\|space-y-md\|bg-emerald.*progress\|rounded-lg" src/ui/found/ApplyProgress.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Uses Card variant=default
    - Progress bar track: bg-muted rounded-none h-2 (8px)
    - Progress bar fill: bg-foreground (neutral, NOT emerald)
    - Done step icon: CheckCircle text-emerald-500 (emerald reserved for done only)
    - Active step: text-foreground font-semibold
    - Pending step: text-muted-foreground
    - Spacing: Tailwind native (gap-3, space-y-2, mt-2)
    - No broken tokens
    - Semantic lock enforced (progress=neutral, done=emerald)
  </acceptance_criteria>
  <done>
    ApplyProgress.tsx migrated with neutral-foreground progress fill + emerald done steps (D-15). Card wrapper applied. Step status icons and text colors reflect semantic hierarchy. Emerald reserved for done-state CheckCircle (success signal). Progress fill is neutral (in-flight state). All tokens replaced. Semantic lock enforced. Ready for integration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ApplyErrorDisplay as inline alert (D-16)</name>
  <files>src/ui/found/ApplyErrorDisplay.tsx</files>
  <read_first>
    - .planning/phases/08-assess-found-panels/08-CONTEXT.md (D-16 decision: inline alert, not modal)
    - .planning/phases/08-assess-found-panels/08-UI-SPEC.md (ApplyErrorDisplay section: "bg-red-500/10 border-red-500/30 p-3 text-red-600 text-sm")
  </read_first>
  <action>
Create or migrate `src/ui/found/ApplyErrorDisplay.tsx` as inline alert per D-16:

1. **Structure (NOT a modal, inline only):**
   ```tsx
   export function ApplyErrorDisplay({ message, details }: ApplyErrorDisplayProps): React.ReactElement {
     return (
       <div className="bg-red-500/10 border border-red-500/30 p-3 rounded-none">
         <p className="text-red-600 text-sm font-medium">{message}</p>
         {details && (
           <p className="text-red-600 text-sm mt-2 opacity-80">{details}</p>
         )}
       </div>
     );
   }
   ```

2. **Token specification (D-16 exact):**
   - Container: `bg-red-500/10 border border-red-500/30 p-3 rounded-none`
   - Title: `text-red-600 text-sm font-medium`
   - Details: `text-red-600 text-sm` with opacity or muted tone
   - Spacing: `p-3` (12px), `mt-2` for details if present

3. **Placement:** Inline within ApplyProgress Card, above or below progress bar (caller determines position).

4. **NOT a modal:** D-16 explicitly states "inline alert, distinct from ConfirmationModal" — this is important for Found mode UX flow.

Reference implementation: See 08-CONTEXT.md D-16 and 08-UI-SPEC.md "ApplyErrorDisplay" section.
  </action>
  <verify>
    <automated>
      grep -c "bg-red-500/10\|border-red-500/30" src/ui/found/ApplyErrorDisplay.tsx && \
      grep -c "text-red-600" src/ui/found/ApplyErrorDisplay.tsx && \
      grep -c "p-3\|rounded-none" src/ui/found/ApplyErrorDisplay.tsx && \
      [ $(grep -c "modal\|Modal\|dialog\|Dialog\|gap-sm\|p-md\|rounded-lg" src/ui/found/ApplyErrorDisplay.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - ApplyErrorDisplay is inline alert, NOT modal
    - Container: bg-red-500/10 border-red-500/30 p-3 rounded-none
    - Text: text-red-600 text-sm (message bold, details normal)
    - Placement: inline within ApplyProgress or caller's container
    - No modal structure (no backdrop, no fixed positioning, no z-layer)
    - No broken tokens
  </acceptance_criteria>
  <done>
    ApplyErrorDisplay.tsx created as inline alert (D-16). Container styling: bg-red-500/10, border-red-500/30, p-3, rounded-none. Text color: text-red-600. Message/details typography per spec. Inline placement (not modal). All tokens host-based. Ready for integration with ApplyProgress.
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate ProvisioningSummary with Card + SectionHeader</name>
  <files>src/ui/found/ProvisioningSummary.tsx</files>
  <read_first>
    - src/ui/found/ProvisioningSummary.tsx (current implementation)
    - src/ui/primitives/Card.tsx and src/ui/primitives/SectionHeader.tsx (Phase 7)
    - .planning/phases/08-assess-found-panels/08-UI-SPEC.md (ProvisioningSummary section: "Card variant=default + SectionHeader + list of provisioning steps")
  </read_first>
  <action>
Migrate `src/ui/found/ProvisioningSummary.tsx` to use Card + SectionHeader:

1. **Structure (similar to VisionPreview but variant=default, not muted):**
   ```tsx
   <Card variant="default" padding="md">
     <SectionHeader title="Provisioning Summary" />
     <div className="mt-4 space-y-2">
       {/* provisioning steps with status badges */}
     </div>
   </Card>
   ```

2. **Token migration:** Card variant=default, SectionHeader handles title, spacing Tailwind native.

3. **Status badges in step list:** Reuse StatusBadge or semantic color classes per step status.

4. **No broken tokens.**

Component structure (step iteration, status rendering) unchanged; only wrapper and container styling updated.
  </action>
  <verify>
    <automated>
      grep -c "import.*Card\|import.*SectionHeader" src/ui/found/ProvisioningSummary.tsx && \
      grep -c "<Card.*default\|variant=\"default\"" src/ui/found/ProvisioningSummary.tsx && \
      grep -c "<SectionHeader" src/ui/found/ProvisioningSummary.tsx && \
      [ $(grep -c "gap-sm\|p-md\|rounded-lg" src/ui/found/ProvisioningSummary.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Imports Card and SectionHeader
    - Uses <Card variant="default" padding="md">
    - Uses <SectionHeader title="...">
    - Status badges render with semantic colors
    - Spacing: Tailwind native (mt-4, space-y-2, etc.)
    - No broken tokens
  </acceptance_criteria>
  <done>
    ProvisioningSummary.tsx migrated to Card default + SectionHeader pattern. Wraps provisioning steps. Delegates header rendering. Status badges integrated. Spacing migrated. No broken tokens. Ready for integration.
  </done>
</task>

<task type="auto">
  <name>Task 5: Migrate ConfirmationModal to unified chrome pattern (D-09, D-10)</name>
  <files>src/ui/found/ConfirmationModal.tsx</files>
  <read_first>
    - src/ui/found/ConfirmationModal.tsx (current implementation)
    - src/ui/assess/ApprovalRoutingModal.tsx (Plan 4, just migrated, reuse same modal chrome pattern)
    - .planning/phases/08-assess-found-panels/08-CONTEXT.md (D-10 decision: same pattern as ApprovalRoutingModal)
  </read_first>
  <action>
Migrate `src/ui/found/ConfirmationModal.tsx` to unified modal chrome pattern per D-09, D-10:

1. **Reuse ApprovalRoutingModal pattern:**
   - Backdrop: `bg-background/80 backdrop-blur-sm`
   - Panel: `bg-card border border-border rounded-none shadow-lg`
   - Z-layers: backdrop z-40, panel z-50
   - Positioning: fixed inset-0 flex items-center justify-center

2. **Text and spacing (D-11):**
   - Title: text-base font-semibold
   - Body: text-sm
   - Spacing: gap-3, p-4, space-y-3
   - Buttons: confirm emerald, cancel neutral

3. **No new pattern needed** — Same as ApprovalRoutingModal. Copy modal chrome structure.

Component logic (confirmation prompt, action handlers) unchanged; only styling updated.
  </action>
  <verify>
    <automated>
      grep -c "bg-background/80.*backdrop-blur-sm" src/ui/found/ConfirmationModal.tsx && \
      grep -c "bg-card.*border.*border-border.*rounded-none" src/ui/found/ConfirmationModal.tsx && \
      grep -c "shadow-lg" src/ui/found/ConfirmationModal.tsx && \
      [ $(grep -c "bg-black/50\|rounded-lg\|gap-md\|p-md\|text-heading" src/ui/found/ConfirmationModal.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Backdrop: bg-background/80 backdrop-blur-sm (NOT bg-black/50)
    - Panel: bg-card border-border rounded-none shadow-lg
    - Text tokens: per D-11 map (text-heading→text-base font-semibold, etc.)
    - Spacing: per UI_REDO_HANDOFF.md (gap-md→gap-3, p-md→p-4, etc.)
    - Z-layers: backdrop z-40, panel z-50
    - Confirm button: emerald, cancel: neutral
    - No broken tokens or rounded corners
  </acceptance_criteria>
  <done>
    ConfirmationModal.tsx migrated to unified modal chrome (D-09, D-10). Same pattern as ApprovalRoutingModal. Backdrop/panel/z-layers correct. Text/spacing tokens replaced per D-11. No light-only utilities. Ready for verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Step status → visual indication | Step status enum (pending/active/done) determines icon/text color |
| Error message → display text | Error messages from component props; React escapes by default |
| Modal confirmation → callback trigger | Modal open/close state and callbacks from parent component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Information Disclosure (error details) | ApplyErrorDisplay rendering | accept | Error messages are intended for display. No sensitive data in error text (validation, logs from prior systems OK). |
| T-08-12 | Denial of Service (backdrop click dismiss) | ConfirmationModal backdrop | accept | Matches Paperclip host modal pattern. User can dismiss; behavior is intentional. |

**Phase 8 Plan 07 Security Posture:** No new attack surface. UI components are presentational. Inherits Plugin SDK + host security model.

</threat_model>

<verification>
## Phase 8 Plan 07 Verification Checklist

**Automated checks:**
- [ ] All five components use host tokens (no light-only utilities, no custom spacing tokens)
- [ ] Card primitive and SectionHeader imported where used
- [ ] ApplyProgress fill: bg-foreground (NOT emerald)
- [ ] ApplyProgress done: text-emerald-500 CheckCircle (emerald for done only)
- [ ] ApplyErrorDisplay: bg-red-500/10, border-red-500/30, text-red-600
- [ ] ConfirmationModal: bg-background/80 backdrop-blur-sm, bg-card border-border
- [ ] Broken tokens absent: `[ $(grep -c "gap-sm\|p-md\|rounded-lg\|text-heading\|bg-black/50" src/ui/found/VisionPreview.tsx src/ui/found/ApplyProgress.tsx src/ui/found/ApplyErrorDisplay.tsx src/ui/found/ProvisioningSummary.tsx src/ui/found/ConfirmationModal.tsx) -eq 0 ]`

**Manual checks:**
- [ ] All five components render with correct appearance
- [ ] ApplyProgress bar fill is neutral (not green)
- [ ] ApplyProgress done steps show green CheckCircle
- [ ] ApplyErrorDisplay is inline alert (no modal backdrop)
- [ ] ConfirmationModal backdrop dims and blurs correctly
- [ ] All components in light and dark modes without regressions
- [ ] No console errors

**Traceability:**
- [ ] UIFM-02 (VisionPreview, ApplyProgress, ApplyErrorDisplay, ProvisioningSummary) addressed by Task 1, 2, 3, 4
- [ ] UIFM-03 (ConfirmationModal, interview visual consistency) addressed by Task 5

</verification>

<success_criteria>
1. VisionPreview.tsx uses Card muted + SectionHeader per section (D-14).
2. ApplyProgress.tsx uses Card default with linear progress bar.
3. ApplyProgress fill: bg-foreground (neutral, in-flight, NOT emerald per D-15).
4. ApplyProgress done steps: CheckCircle text-emerald-500 (emerald reserved for completion per D-15).
5. ApplyErrorDisplay.tsx is inline alert: bg-red-500/10, border-red-500/30, text-red-600, NOT modal.
6. ProvisioningSummary.tsx uses Card default + SectionHeader (D-14).
7. ConfirmationModal.tsx uses unified modal chrome (D-09, D-10): same as Plan 4 modals.
8. All spacing tokens migrated per UI_REDO_HANDOFF.md (gap-sm→gap-2, p-md→p-4, etc.).
9. All text tokens migrated per Phase 7 D-05 map (text-heading→text-base font-semibold, etc.).
10. No light-only utilities (bg-black/50) or rounded corners (rounded-lg).
11. All five components work in light and dark modes without color regressions.
12. Semantic locks enforced (emerald for completion/success only, neutral for in-flight states, red for errors).
13. Ready for founder visual verification via extended DualRenderProbe and Phase 10 production verification gates.
</success_criteria>

<output>
After completion, create `.planning/phases/08-assess-found-panels/08-07-SUMMARY.md` documenting:
- Task completion timestamps
- Card + SectionHeader primitive reuse pattern confirmed (D-14)
- ApplyProgress progress/done semantic distinction locked (neutral foreground for in-flight, emerald CheckCircle for done per D-15)
- ApplyErrorDisplay inline-alert pattern confirmed (NOT modal per D-16)
- ConfirmationModal unified chrome complete (same as Plan 4 modals per D-09, D-10)
- Test pass rates (if applicable)
- No broken token violations found
- All ~19 Phase 8 components migrated (9 Assess + 10 Found)
- Ready for Phase 8 verification checkpoint and Phase 10 production gates
</output>
