---
phase: 08
plan: 06
type: auto
wave: 3
depends_on: [08-05]
files_modified:
  - src/ui/found/PresetSelector.tsx
  - src/ui/found/QuestionRenderer.tsx
  - src/ui/found/InterviewSection.tsx
autonomous: true
requirements: [UIFM-02]
user_setup: []

must_haves:
  truths:
    - "PresetSelector uses Card primitive (variant=default, padding=md)"
    - "QuestionRenderer uses Card primitive (variant=default, padding=md)"
    - "InterviewSection uses Card primitive or bg-card styling"
    - "All components use host tokens (bg-card, text-foreground, border-border)"
    - "Heavy Card + SectionHeader reuse per D-14 reference"
    - "All spacing tokens migrated per UI_REDO_HANDOFF.md"
    - "Components work correctly in light and dark modes"
  artifacts:
    - path: "src/ui/found/PresetSelector.tsx"
      provides: "Preset selector with Card primitive"
      exports: ["PresetSelector"]
      contains: "Card|bg-card"
    - path: "src/ui/found/QuestionRenderer.tsx"
      provides: "Question display with Card primitive"
      exports: ["QuestionRenderer"]
      contains: "Card|bg-card"
    - path: "src/ui/found/InterviewSection.tsx"
      provides: "Interview section container with host tokens"
      exports: ["InterviewSection"]
      contains: "Card|bg-card|border-border"
  key_links:
    - from: "src/ui/found/PresetSelector.tsx"
      to: "src/ui/primitives/Card.tsx"
      via: "component composition"
      pattern: "Card"
    - from: "src/ui/found/QuestionRenderer.tsx"
      to: "src/ui/primitives/Card.tsx"
      via: "component composition"
      pattern: "Card"

---

<objective>
Migrate interview body components (PresetSelector, QuestionRenderer, InterviewSection) to host tokens, reusing Phase 7 Card primitive heavily (D-14). Pure token swap with zero structural changes. Establish interview visual baseline for found-mode interview flow.

Purpose: Migrate remaining found-mode UI components; prepare for apply gate (Plan 7) and founder visual review via DualRenderProbe.

Output: Three TypeScript React components using Card primitive and host tokens, ready for integration with Plan 7 apply gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-assess-found-panels/08-CONTEXT.md
@.planning/phases/08-assess-found-panels/08-RESEARCH.md
@.planning/phases/08-assess-found-panels/08-UI-SPEC.md
@.planning/phases/07-foundations-shared-primitives/07-01-SUMMARY.md
@./UI_REDO_HANDOFF.md
@./CLAUDE.md
</context>

<interfaces>
Card primitive (Phase 7, ready for reuse):
```typescript
export function Card({
  variant?: "default" | "muted" | "elevated",
  padding?: "sm" | "md" | "lg",
  children: React.ReactNode,
}): React.ReactElement { }
```

Host tokens (verified Phase 7):
```
Surfaces: bg-card, bg-muted
Borders: border-border
Text: text-foreground, text-muted-foreground
Spacing: gap-1, gap-2, px-2, px-3, py-2, py-3
Corners: rounded-none
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate PresetSelector to use Card primitive</name>
  <files>src/ui/found/PresetSelector.tsx</files>
  <read_first>
    - src/ui/found/PresetSelector.tsx (current implementation, identify layout and styling)
    - src/ui/primitives/Card.tsx (Phase 7, available for import)
    - .planning/phases/08-assess-found-panels/08-UI-SPEC.md (PresetSelector section: "Card variant=default, padding=md")
  </read_first>
  <action>
Migrate `src/ui/found/PresetSelector.tsx` to use Card primitive (D-14):

1. **Current structure:** Identify root container div with styling
2. **Target structure:** Wrap root content in `<Card variant="default" padding="md">`
3. **Import Card:** `import { Card } from "../primitives/Card.js"`
4. **Token migration checklist:**
   - Remove standalone `bg-card`, `border-border`, `p-md/lg` from div className
   - Delegate to Card primitive (which already provides: bg-card, border-border, rounded-none, padding)
   - Keep internal spacing (gap-sm/md → gap-2/3, px-sm → px-2, etc.)
   - No broken tokens: `gap-xs`, `rounded-lg`

5. **Component structure unchanged** — Only wrapping container, children unmodified.

Reference implementation: See 08-UI-SPEC.md "PresetSelector" and 08-RESEARCH.md Architecture section.
  </action>
  <verify>
    <automated>
      grep -c "import.*Card" src/ui/found/PresetSelector.tsx && \
      grep -c "<Card" src/ui/found/PresetSelector.tsx && \
      [ $(grep -c "gap-xs\|gap-sm\|gap-md\|px-sm\|px-md\|rounded-lg\|bg-card.*border-border" src/ui/found/PresetSelector.tsx | head -1) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Imports Card from primitives
    - Uses <Card variant="default" padding="md">
    - Internal spacing uses Tailwind native scale (gap-2/3, px-2/3, etc.)
    - No standalone bg-card/border-border/p-md in div className
    - No broken tokens
  </acceptance_criteria>
  <done>
    PresetSelector.tsx wrapped with Card primitive. bg-card, border-border, rounded-none, padding delegated to Card. Internal spacing migrated. No broken tokens. Ready for integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate QuestionRenderer to use Card primitive</name>
  <files>src/ui/found/QuestionRenderer.tsx</files>
  <read_first>
    - src/ui/found/QuestionRenderer.tsx (current implementation)
    - src/ui/primitives/Card.tsx (Phase 7, available for import)
    - .planning/phases/08-assess-found-panels/08-UI-SPEC.md (QuestionRenderer section: "Card variant=default, padding=md")
  </read_first>
  <action>
Migrate `src/ui/found/QuestionRenderer.tsx` to use Card primitive (D-14):

Same pattern as Task 1:
1. Import Card from primitives
2. Wrap content in <Card variant="default" padding="md">
3. Remove standalone card styling from div className
4. Migrate internal spacing (gap-sm→gap-2, px-sm→px-2, etc.)
5. No broken tokens

Component structure and question-rendering logic unchanged.
  </action>
  <verify>
    <automated>
      grep -c "import.*Card" src/ui/found/QuestionRenderer.tsx && \
      grep -c "<Card" src/ui/found/QuestionRenderer.tsx && \
      [ $(grep -c "gap-xs\|gap-sm\|px-md\|rounded-lg" src/ui/found/QuestionRenderer.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Imports Card from primitives
    - Uses <Card variant="default" padding="md">
    - Internal spacing migrated to Tailwind native
    - No broken tokens
  </acceptance_criteria>
  <done>
    QuestionRenderer.tsx wrapped with Card primitive. Same pattern as PresetSelector. Ready for integration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate InterviewSection container to host tokens</name>
  <files>src/ui/found/InterviewSection.tsx</files>
  <read_first>
    - src/ui/found/InterviewSection.tsx (current implementation, identify root container)
    - .planning/phases/08-assess-found-panels/08-UI-SPEC.md (InterviewSection section: "Card variant=default, or bg-card border-border styling")
  </read_first>
  <action>
Migrate `src/ui/found/InterviewSection.tsx` container to host tokens:

1. **Option A (preferred if interior supports Card):** Wrap in <Card variant="default" padding="md">
2. **Option B (if interior has distinct layout):** Use Card component or direct host token styling: `className="bg-card border border-border rounded-none p-4"`

3. **Token migration checklist:**
   - Container: `bg-card border border-border rounded-none` (Card styling)
   - Padding: `p-4` (16px, default from Tailwind scale)
   - Internal: gap-sm→gap-2, px-sm→px-2, text-body→text-sm, etc.
   - No broken tokens

4. **Component structure unchanged** — Only container styling.

Reference implementation: See 08-UI-SPEC.md "InterviewSection container" section.
  </action>
  <verify>
    <automated>
      grep -c "Card\|bg-card.*border.*border-border" src/ui/found/InterviewSection.tsx && \
      grep -c "rounded-none" src/ui/found/InterviewSection.tsx && \
      [ $(grep -c "gap-xs\|gap-sm\|px-md\|rounded-lg" src/ui/found/InterviewSection.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Uses Card primitive OR direct host token styling (bg-card border-border rounded-none p-4)
    - Internal spacing migrated to Tailwind native
    - No broken tokens
  </acceptance_criteria>
  <done>
    InterviewSection.tsx container migrated to host tokens. Card primitive or direct styling applied. Internal spacing migrated. No broken tokens. Ready for Plan 7 apply gate integration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Card primitive API → component composition | Card props (variant, padding) determine styling |
| Interview section content → rendering | Content from component props; React escapes text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Information Disclosure (interview content display) | Interview components rendering | accept | Interview content is intended for visual review by founder. No sensitive data exposure. |

**Phase 8 Plan 06 Security Posture:** No new attack surface. UI components are presentational. Inherits Plugin SDK + host security model.

</threat_model>

<verification>
## Phase 8 Plan 06 Verification Checklist

**Automated checks:**
- [ ] All three components import Card or use host tokens
- [ ] All three use bg-card, border-border, rounded-none
- [ ] Internal spacing uses Tailwind native (gap-2/3, px-2/3, etc.)
- [ ] Broken tokens absent: `[ $(grep -c "gap-xs\|gap-sm\|px-md\|rounded-lg" src/ui/found/PresetSelector.tsx src/ui/found/QuestionRenderer.tsx src/ui/found/InterviewSection.tsx) -eq 0 ]`

**Manual checks:**
- [ ] All three components render with Card-like appearance (white card with border in light mode)
- [ ] Spacing looks consistent
- [ ] Both light and dark modes render correctly
- [ ] No console errors

**Traceability:**
- [ ] UIFM-02 (Interview subcomponents) addressed by Task 1, Task 2, Task 3

</verification>

<success_criteria>
1. PresetSelector.tsx uses Card primitive or host tokens (bg-card, border-border, rounded-none).
2. QuestionRenderer.tsx uses Card primitive or host tokens (same as PresetSelector).
3. InterviewSection.tsx uses Card primitive or host tokens (same pattern).
4. All internal spacing migrated to Tailwind native (gap-2/3, px-2/3, py-2/3, etc.).
5. No broken custom tokens or rounded corners.
6. All three components work in light and dark modes without color regressions.
7. Ready for Plan 7 apply gate integration and full interview flow testing.
</success_criteria>

<output>
After completion, create `.planning/phases/08-assess-found-panels/08-06-SUMMARY.md` documenting:
- Task completion timestamps
- Card primitive reuse pattern confirmed (D-14)
- All three interview body components migrated
- Internal spacing token migration complete
- No broken token violations found
- Pattern ready for Plan 7 apply gate (VisionPreview, ApplyProgress, etc.)
</output>
