---
phase: 08
plan: 04
type: auto
wave: 2
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - src/ui/assess/ApprovalRoutingModal.tsx
  - src/ui/assess/CustomOverrideWarning.tsx
  - src/ui/assess/ApprovingWaitingState.tsx
autonomous: true
requirements: [UIA-05]
user_setup: []

must_haves:
  truths:
    - "ApprovalRoutingModal uses unified modal chrome pattern (D-09): bg-card border border-border rounded-none shadow-lg, backdrop=bg-background/80 backdrop-blur-sm"
    - "CustomOverrideWarning uses same modal chrome pattern (D-10)"
    - "ApprovingWaitingState uses same modal chrome pattern (D-10)"
    - "All text tokens migrated per Phase 7 D-05 map (text-heading→text-base font-semibold, etc.)"
    - "No bg-black/50 backdrop (light-only); no rounded-lg corners"
    - "Approval flow UX unchanged, pure token migration"
    - "Components work correctly in light and dark modes"
  artifacts:
    - path: "src/ui/assess/ApprovalRoutingModal.tsx"
      provides: "Approval routing modal with host tokens and unified chrome"
      exports: ["ApprovalRoutingModal"]
      contains: "bg-card.*border-border.*bg-background/80"
    - path: "src/ui/assess/CustomOverrideWarning.tsx"
      provides: "Custom override warning modal with unified chrome"
      exports: ["CustomOverrideWarning"]
      contains: "bg-card.*border-border.*bg-background/80"
    - path: "src/ui/assess/ApprovingWaitingState.tsx"
      provides: "Approving/waiting state modal with unified chrome"
      exports: ["ApprovingWaitingState"]
      contains: "bg-card.*border-border.*bg-background/80"
  key_links:
    - from: "src/ui/assess/ApprovalRoutingModal.tsx"
      to: "host CSS variables"
      via: "Modal chrome classes (bg-card, border-border, backdrop-blur)"
      pattern: "bg-card.*border-border.*bg-background/80"
    - from: "src/ui/assess/CustomOverrideWarning.tsx"
      to: "ApprovalRoutingModal"
      via: "Same modal chrome pattern (D-10)"
      pattern: "bg-card.*border-border"

---

<objective>
Migrate three approval/confirmation modals to unified modal chrome pattern (D-09, D-10). Lock pattern: all modals use host tokens (bg-card, border-border), sharp corners (rounded-none), dim+blur backdrop (bg-background/80 backdrop-blur-sm). Preserve approval flow UX unchanged.

Purpose: Ensure all modals render indistinguishable from Paperclip host; establish consistent modal pattern for remaining phase modals (Plan 7 ConfirmationModal).

Output: Three TypeScript React components with unified chrome, ready for visual verification and integration with Assess flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-assess-found-panels/08-CONTEXT.md
@.planning/phases/08-assess-found-panels/08-RESEARCH.md
@.planning/phases/08-assess-found-panels/08-UI-SPEC.md
@./UI_REDO_HANDOFF.md
@./CLAUDE.md
</context>

<interfaces>
Host token names for modals (verified Phase 7):
```
Surfaces: bg-background (backdrop), bg-card (panel)
Borders: border-border
Semantic: text-emerald-600 (approve CTA), text-red-600 (cancel/warning)
Typography: text-base font-semibold (title), text-sm (body)
Spacing: gap-2, gap-3, p-3, p-4
Effects: shadow-lg, backdrop-blur-sm, rounded-none
```

Modal pattern (from host shadcn Dialog, 08-RESEARCH.md Pattern 3):
```typescript
export function ModalComponent({ isOpen, onClose }: Props): React.ReactElement | null {
  if (!isOpen) return null;
  return (
    <>
      {/* Backdrop */}
      <div className="fixed inset-0 bg-background/80 backdrop-blur-sm z-40" onClick={onClose} />
      {/* Panel */}
      <div className="fixed inset-0 flex items-center justify-center z-50 pointer-events-none">
        <div className="bg-card border border-border rounded-none shadow-lg pointer-events-auto">
          {/* Content */}
        </div>
      </div>
    </>
  );
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ApprovalRoutingModal to unified chrome pattern</name>
  <files>src/ui/assess/ApprovalRoutingModal.tsx</files>
  <read_first>
    - src/ui/assess/ApprovalRoutingModal.tsx (current implementation, identify backdrop and panel styling)
    - .planning/phases/08-assess-found-panels/08-CONTEXT.md (D-09, D-10, D-11 locked decisions)
    - .planning/phases/08-assess-found-panels/08-RESEARCH.md (Pattern 3: Modal Chrome D-09)
    - .planning/phases/08-assess-found-panels/08-UI-SPEC.md (ApprovalRoutingModal section)
  </read_first>
  <action>
Migrate `src/ui/assess/ApprovalRoutingModal.tsx` to unified modal chrome per D-09, D-10, D-11:

1. **Current structure:** Identify backdrop (likely `bg-black/50`) and panel (likely `rounded-lg`)
2. **Target structure (D-09):**
   - Backdrop: `bg-background/80 backdrop-blur-sm` (NOT `bg-black/50`)
   - Panel: `bg-card border border-border rounded-none shadow-lg` (NOT `rounded-lg`)
   - Positioning: `fixed inset-0 flex items-center justify-center z-50`
   - Z-layers: backdrop z-40, panel z-50 pointer-events-none on container, pointer-events-auto on panel

3. **Text token migration (D-11):**
   - `text-heading` → `text-base font-semibold`
   - `text-body` → `text-sm`
   - `text-label` → `text-xs font-medium`
   - `text-accent` → `text-emerald-600` (only for approve CTA or success; otherwise `text-foreground`)

4. **Spacing migration:**
   - `space-y-md/lg` → `space-y-3/4`
   - `gap-md/lg` → `gap-3/4`
   - `p-md/lg` → `p-3/4`

5. **Buttons:**
   - Cancel: `border border-border text-foreground hover:bg-muted`
   - Approve: `bg-emerald-500 text-white hover:bg-emerald-600`

6. **No structural changes** — Pure token swap and backdrop/panel styling.

Reference implementation: See 08-RESEARCH.md "Example 3: Modal Chrome Pattern (D-09)".
  </action>
  <verify>
    <automated>
      grep -c "bg-background/80.*backdrop-blur-sm" src/ui/assess/ApprovalRoutingModal.tsx && \
      grep -c "bg-card.*border.*border-border.*rounded-none" src/ui/assess/ApprovalRoutingModal.tsx && \
      grep -c "shadow-lg" src/ui/assess/ApprovalRoutingModal.tsx && \
      [ $(grep -c "bg-black/50\|rounded-lg\|gap-md\|p-md\|text-heading\|text-body" src/ui/assess/ApprovalRoutingModal.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Backdrop: `bg-background/80 backdrop-blur-sm` (NOT `bg-black/50`)
    - Panel: `bg-card border border-border rounded-none shadow-lg` (NOT `rounded-lg`)
    - Z-layers: backdrop z-40, panel z-50 with pointer-events handling
    - Text tokens: all replaced per D-11 map
    - Spacing: all custom tokens replaced with Tailwind native
    - Buttons: cancel neutral, approve emerald
    - No broken tokens or rounded corners
  </acceptance_criteria>
  <done>
    ApprovalRoutingModal.tsx migrated to unified modal chrome (D-09). Backdrop uses bg-background/80 backdrop-blur-sm. Panel uses bg-card border-border rounded-none shadow-lg. Text and spacing tokens replaced per D-11. Approval flow UX unchanged. Ready for integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate CustomOverrideWarning to unified chrome pattern</name>
  <files>src/ui/assess/CustomOverrideWarning.tsx</files>
  <read_first>
    - src/ui/assess/CustomOverrideWarning.tsx (current implementation)
    - src/ui/assess/ApprovalRoutingModal.tsx (just migrated, reuse same modal chrome pattern)
    - .planning/phases/08-assess-found-panels/08-CONTEXT.md (D-10 decision: migrate in place, same pattern)
  </read_first>
  <action>
Migrate `src/ui/assess/CustomOverrideWarning.tsx` using ApprovalRoutingModal as pattern reference. Apply identical modal chrome (D-09) and text/spacing tokens (D-11).

Token checklist same as Task 1:
- Backdrop: `bg-background/80 backdrop-blur-sm`
- Panel: `bg-card border border-border rounded-none shadow-lg`
- Text: per D-11 map (text-heading→text-base font-semibold, etc.)
- Spacing: per UI_REDO_HANDOFF.md (gap-md→gap-3, p-md→p-4, etc.)
- Buttons: confirm button uses semantic color appropriate to warning (likely red for critical, yellow for caution)

No structural changes — pure chrome pattern reuse and token migration.
  </action>
  <verify>
    <automated>
      grep -c "bg-background/80.*backdrop-blur-sm" src/ui/assess/CustomOverrideWarning.tsx && \
      grep -c "bg-card.*border.*border-border.*rounded-none" src/ui/assess/CustomOverrideWarning.tsx && \
      [ $(grep -c "bg-black/50\|rounded-lg\|gap-md\|p-md\|text-heading" src/ui/assess/CustomOverrideWarning.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Same modal chrome as ApprovalRoutingModal (bg-background/80, bg-card, rounded-none)
    - Text/spacing tokens replaced per D-11 and UI_REDO_HANDOFF.md
    - No broken tokens or rounded corners
  </acceptance_criteria>
  <done>
    CustomOverrideWarning.tsx migrated using unified modal chrome (D-09, D-10). Same pattern as ApprovalRoutingModal. All tokens replaced. Ready for verification.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate ApprovingWaitingState to unified chrome pattern</name>
  <files>src/ui/assess/ApprovingWaitingState.tsx</files>
  <read_first>
    - src/ui/assess/ApprovingWaitingState.tsx (current implementation)
    - src/ui/assess/ApprovalRoutingModal.tsx (just migrated, reuse same modal chrome pattern)
  </read_first>
  <action>
Migrate `src/ui/assess/ApprovingWaitingState.tsx` using ApprovalRoutingModal as pattern reference. Apply identical modal chrome (D-09) and text/spacing tokens (D-11).

Token checklist same as Tasks 1-2:
- Backdrop: `bg-background/80 backdrop-blur-sm`
- Panel: `bg-card border border-border rounded-none shadow-lg`
- Text: per D-11 map
- Spacing: per UI_REDO_HANDOFF.md
- Status indicators (if present): Use semantic palette colors

No structural changes — pure chrome pattern reuse and token migration.
  </action>
  <verify>
    <automated>
      grep -c "bg-background/80.*backdrop-blur-sm" src/ui/assess/ApprovingWaitingState.tsx && \
      grep -c "bg-card.*border.*border-border.*rounded-none" src/ui/assess/ApprovingWaitingState.tsx && \
      [ $(grep -c "bg-black/50\|rounded-lg\|gap-md\|p-md" src/ui/assess/ApprovingWaitingState.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Same modal chrome as ApprovalRoutingModal and CustomOverrideWarning
    - All tokens replaced per D-11 and UI_REDO_HANDOFF.md
    - No broken tokens or rounded corners
  </acceptance_criteria>
  <done>
    ApprovingWaitingState.tsx migrated using unified modal chrome (D-09, D-10). Same pattern as other two modals. All tokens replaced. Approval flow integration complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal visibility prop → rendered modal | isOpen prop controls modal display; trusted caller responsibility |
| Modal click handlers → approval/cancel logic | onApprove/onCancel callbacks; logic is caller's responsibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Denial of Service (backdrop click dismiss) | Modal backdrop clickable to close | accept | Behavior matches Paperclip host modals; expected pattern. User can click backdrop to dismiss non-critical modals. |
| T-08-08 | Tampering (modal content) | Modal panel rendering content prop | accept | Content comes from component props; React escapes text by default. No raw HTML injection. |

**Phase 8 Plan 04 Security Posture:** No new attack surface. Modal chrome is presentational. Inherits Plugin SDK + host security model.

</threat_model>

<verification>
## Phase 8 Plan 04 Verification Checklist

**Automated checks:**
- [ ] All three modals contain `bg-background/80 backdrop-blur-sm`
- [ ] All three modals contain `bg-card border-border rounded-none`
- [ ] Broken tokens absent: `[ $(grep -c "bg-black/50\|rounded-lg\|gap-md\|text-heading" src/ui/assess/ApprovalRoutingModal.tsx src/ui/assess/CustomOverrideWarning.tsx src/ui/assess/ApprovingWaitingState.tsx) -eq 0 ]`

**Manual checks:**
- [ ] All three modals render with same chrome (backdrop + panel styling)
- [ ] Backdrop dims correctly in light and dark modes
- [ ] Backdrop blur effect visible
- [ ] Panel has sharp corners (rounded-none)
- [ ] Modal buttons work (approve/cancel/confirm)
- [ ] Both light and dark modes render without color regressions
- [ ] No console errors

**Traceability:**
- [ ] UIA-05 (ApprovalRoutingModal, CustomOverrideWarning, ApprovingWaitingState) addressed by Task 1, Task 2, Task 3

</verification>

<success_criteria>
1. All three modals use unified chrome pattern (D-09, D-10): backdrop `bg-background/80 backdrop-blur-sm`, panel `bg-card border-border rounded-none shadow-lg`.
2. All text tokens migrated per D-11 (text-heading→text-base font-semibold, text-body→text-sm, text-label→text-xs font-medium).
3. All spacing tokens migrated per UI_REDO_HANDOFF.md (gap-md→gap-3, p-md→p-4, space-y-lg→space-y-4).
4. No light-only utilities (bg-black/50) or rounded corners (rounded-lg).
5. Approval flow UX unchanged (buttons, actions, conditional rendering preserved).
6. All three modals work in light and dark modes without color regressions.
7. Ready for integration with DriftItemCard approval flow and Found mode confirm gate (Plan 7).
</success_criteria>

<output>
After completion, create `.planning/phases/08-assess-found-panels/08-04-SUMMARY.md` documenting:
- Task completion timestamps
- Unified modal chrome pattern locked (all 3 modals identical)
- Text/spacing token migration complete (D-11)
- No broken token violations found
- Approval flow UX verified unchanged
- Pattern ready for reuse in ConfirmationModal (Plan 7)
</output>
