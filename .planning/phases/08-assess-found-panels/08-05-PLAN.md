---
phase: 08
plan: 05
type: auto
wave: 2
depends_on: []
files_modified:
  - src/ui/found/FoundPanel.tsx
  - src/ui/found/SectionNavRail.tsx
autonomous: true
requirements: [UIFM-01, UIFM-03]
user_setup: []

must_haves:
  truths:
    - "FoundPanel root uses bg-background with vertical flex layout"
    - "SectionNavRail uses horizontal flex, overflow-x-auto for narrow sidebar scroll"
    - "SectionNavRail active state: bg-foreground text-background (neutral bold, NOT emerald per D-13)"
    - "SectionNavRail completed state: CheckCircle icon text-emerald-500 (success signal)"
    - "All spacing tokens migrated per UI_REDO_HANDOFF.md (gap-xs→gap-1, gap-sm→gap-2, etc.)"
    - "Components work correctly in light and dark modes"
  artifacts:
    - path: "src/ui/found/FoundPanel.tsx"
      provides: "FoundPanel root container with host tokens"
      exports: ["FoundPanel"]
      contains: "bg-background"
    - path: "src/ui/found/SectionNavRail.tsx"
      provides: "Horizontal tab strip with neutral-bold active state"
      exports: ["SectionNavRail"]
      contains: "bg-foreground.*text-background|text-emerald-500"
  key_links:
    - from: "src/ui/found/FoundPanel.tsx"
      to: "host CSS variables"
      via: "bg-background layout"
      pattern: "bg-background|flex.*flex-col"
    - from: "src/ui/found/SectionNavRail.tsx"
      to: "host CSS variables"
      via: "active-state styling and icon color"
      pattern: "bg-foreground.*text-background|text-emerald-500"

---

<objective>
Migrate FoundPanel root and SectionNavRail to host tokens. Lock active-state pattern (neutral bold, D-13) and preserve horizontal tab-strip layout. Establish Found mode shell with correct semantic coloring (emerald reserved for completed state icon only, not active-tab color).

Purpose: Establish Found mode container and navigation baseline; prevent emerald overuse in non-completion states; prepare for interview body components (Plan 6) and apply gate (Plan 7).

Output: Two TypeScript React components migrated to host tokens, ready for Plans 6-7 integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-assess-found-panels/08-CONTEXT.md
@.planning/phases/08-assess-found-panels/08-RESEARCH.md
@.planning/phases/08-assess-found-panels/08-UI-SPEC.md
@./UI_REDO_HANDOFF.md
@./CLAUDE.md
</context>

<interfaces>
From `src/types/found.ts`:
```typescript
export interface InterviewSection {
  id: string;
  name: string;
  completed: boolean;
}
```

Host tokens (verified Phase 7):
```
Surfaces: bg-background (root), bg-sidebar (if sidebar variant used)
Text: text-foreground, text-sidebar-foreground
Semantic colors: text-emerald-500 (completed state icon only)
Spacing: gap-1, gap-2, px-2, px-3, py-1, py-2
Layout: overflow-x-auto (horizontal scroll), rounded-none
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate FoundPanel root to host tokens</name>
  <files>src/ui/found/FoundPanel.tsx</files>
  <read_first>
    - src/ui/found/FoundPanel.tsx (current implementation, identify root container and layout)
    - .planning/phases/08-assess-found-panels/08-UI-SPEC.md (FoundPanel section: "Root container: bg-background, Layout: Vertical flex")
    - .planning/phases/08-assess-found-panels/08-RESEARCH.md (Architecture section, FoundPanel structure)
  </read_first>
  <action>
Migrate `src/ui/found/FoundPanel.tsx` root container to host tokens per UIFM-01:

1. **Root container baseline:**
   - Current: likely has custom tokens or broken styling
   - Target: `bg-background flex flex-col gap-4 w-full` (vertical layout, full width)

2. **Token migration checklist (UI_REDO_HANDOFF.md):**
   - [ ] Root: `bg-background` (panel background)
   - [ ] Layout: `flex flex-col` (vertical stack)
   - [ ] Gap: `gap-sm` → `gap-2`, `gap-md` → `gap-3`
   - [ ] Padding: `px-md` → `px-3`, `py-sm` → `py-2`
   - [ ] No broken tokens: `gap-xs`, `space-y-lg`, `rounded-lg`

3. **Component structure:**
   - FoundPanel renders SectionNavRail at top
   - Interview body below (PresetSelector, QuestionRenderer, InterviewSection)
   - Children unchanged in this task

4. **No new imports needed** — Pure token swap in className.

Reference implementation: See 08-UI-SPEC.md "FoundPanel (Root Container)" section.
  </action>
  <verify>
    <automated>
      grep -c "bg-background" src/ui/found/FoundPanel.tsx && \
      grep -c "flex flex-col\|flex-col" src/ui/found/FoundPanel.tsx && \
      [ $(grep -c "gap-xs\|gap-sm\|gap-md\|gap-lg\|px-sm\|px-md\|space-y-lg\|rounded-lg" src/ui/found/FoundPanel.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - FoundPanel root div uses bg-background
    - Root container has flex flex-col layout
    - Spacing uses Tailwind native scale (gap-2/3/4, px-2/3/4, py-2/3/4)
    - No broken custom tokens
    - Child components (SectionNavRail, interview body) not modified
  </acceptance_criteria>
  <done>
    FoundPanel.tsx root container migrated to host tokens. bg-background, flex flex-col applied. All spacing tokens replaced. No broken tokens. Ready for Plan 6 (interview body) and Plan 7 (apply gate).
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate SectionNavRail with neutral-bold active state</name>
  <files>src/ui/found/SectionNavRail.tsx</files>
  <read_first>
    - src/ui/found/SectionNavRail.tsx (current implementation, locate active-state styling)
    - .planning/phases/08-assess-found-panels/08-CONTEXT.md (D-12, D-13 locked decisions)
    - .planning/phases/08-assess-found-panels/08-UI-SPEC.md (SectionNavRail section: "Active: bg-foreground text-background (neutral bold), Completed: CheckCircle text-emerald-500")
  </read_first>
  <action>
Migrate `src/ui/found/SectionNavRail.tsx` to host tokens with neutral-bold active state (D-13):

1. **Current structure:** Horizontal tabs, likely has light-only styling or wrong active color (emerald)
2. **Target structure (D-13):**
   - Layout: `flex overflow-x-auto gap-2` (horizontal scroll for narrow sidebar)
   - Inactive tab: `bg-sidebar text-sidebar-foreground` or `bg-muted text-muted-foreground` (fallback if sidebar tokens unavailable)
   - **Active tab (D-13 CRITICAL):** `bg-foreground text-background` (neutral bold, NOT emerald) — foundational for Found mode signal
   - **Completed state (D-13):** Lucide CheckCircle icon `text-emerald-500` (emerald reserved for completed, not active)

3. **Tab styling (per D-12):**
   - Each tab: `px-3 py-2 rounded-none` (12px h, 8px v, sharp corners)
   - Padding: `px-md` → `px-3`, `py-sm` → `py-2`
   - Gaps: `gap-xs` → `gap-1`, `gap-sm` → `gap-2`
   - Remove: `rounded`, `rounded-lg`, `mr-sm` → `mr-2`, `pb-sm` → `pb-2`

4. **CheckCircle icon:**
   - Only rendered for completed sections
   - Color: `text-emerald-500` (success signal, per D-13)
   - Size: `h-4 w-4` (standard icon size)

5. **Implementation note:** Active state is the load-bearing semantic pattern — do NOT use emerald for active tabs. Emerald must be reserved for completed-state CheckCircle icon and Found-mode success signal per Phase 8 D-01, D-04, D-13 alignment.

Reference implementation: See 08-CONTEXT.md D-13 and 08-UI-SPEC.md "SectionNavRail" section.
  </action>
  <verify>
    <automated>
      grep -c "bg-foreground.*text-background\|text-background.*bg-foreground" src/ui/found/SectionNavRail.tsx && \
      grep -c "text-emerald-500" src/ui/found/SectionNavRail.tsx && \
      grep -c "CheckCircle" src/ui/found/SectionNavRail.tsx && \
      grep -c "overflow-x-auto" src/ui/found/SectionNavRail.tsx && \
      [ $(grep -c "bg-accent\|text-accent\|gap-xs\|px-md\|rounded-lg\|bg-green-50" src/ui/found/SectionNavRail.tsx) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Active tab: bg-foreground text-background (neutral bold, NOT emerald)
    - Inactive tab: bg-sidebar or bg-muted
    - Completed state: CheckCircle icon text-emerald-500 (emerald reserved for completion, not active)
    - Layout: flex overflow-x-auto (horizontal scroll preserved)
    - Tab padding: px-3 py-2 (12px h, 8px v)
    - Gaps: gap-2 (8px, from gap-sm migration)
    - All broken tokens removed (gap-xs, px-md, rounded-lg, bg-accent)
    - Active-state pattern locks emerald semantics (completion only, not active)
  </acceptance_criteria>
  <done>
    SectionNavRail.tsx migrated with neutral-bold active state (D-13). Active tabs use bg-foreground text-background (NOT emerald). Completed state uses CheckCircle text-emerald-500. Horizontal scroll preserved. All tokens replaced. Emerald semantics locked (completion only). Ready for interview body integration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Section completion status → CheckCircle visibility | Completed boolean determines icon render; status from backend |
| Section index/active state → tab styling | Active section index determines active-tab styling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering (CSS override of active state) | SectionNavRail active-state styling | accept | Active state is CSS class-based, not user-settable. Styles are host tokens, read-only from component perspective. |

**Phase 8 Plan 05 Security Posture:** No new attack surface. UI components are presentational. Inherits Plugin SDK + host security model.

</threat_model>

<verification>
## Phase 8 Plan 05 Verification Checklist

**Automated checks:**
- [ ] `grep -c "bg-foreground.*text-background" src/ui/found/SectionNavRail.tsx` returns ≥1 (active state)
- [ ] `grep -c "text-emerald-500.*CheckCircle\|CheckCircle.*text-emerald-500" src/ui/found/SectionNavRail.tsx` returns ≥1 (completed)
- [ ] `grep -c "bg-background" src/ui/found/FoundPanel.tsx` returns ≥1
- [ ] Broken tokens absent: `[ $(grep -c "bg-accent\|px-md\|gap-xs\|rounded-lg\|text-accent" src/ui/found/FoundPanel.tsx src/ui/found/SectionNavRail.tsx) -eq 0 ]`

**Manual checks:**
- [ ] FoundPanel background correct (white in light, dark gray in dark)
- [ ] SectionNavRail tabs render horizontally with scroll
- [ ] Active tab is visually bold (dark on light, or light on dark) — definitely NOT green
- [ ] Completed tabs show CheckCircle icon in green (emerald)
- [ ] Both light and dark modes render without color regressions
- [ ] No console errors

**Traceability:**
- [ ] UIFM-01 (FoundPanel) addressed by Task 1
- [ ] UIFM-03 (SectionNavRail active state, D-13) addressed by Task 2

</verification>

<success_criteria>
1. FoundPanel.tsx root uses bg-background with flex flex-col vertical layout.
2. SectionNavRail active state: bg-foreground text-background (neutral bold, D-13 critical).
3. SectionNavRail completed state: CheckCircle icon text-emerald-500 (emerald reserved for completion only).
4. SectionNavRail layout: flex overflow-x-auto (horizontal scroll for narrow sidebar).
5. All spacing tokens migrated per UI_REDO_HANDOFF.md (gap-xs→gap-1, px-md→px-3, py-sm→py-2, etc.).
6. No emerald used for active-tab state (semantic lock for completion only).
7. Both components work in light and dark modes without color regressions.
8. Ready for Plans 6-7 (interview body and apply gate).
</success_criteria>

<output>
After completion, create `.planning/phases/08-assess-found-panels/08-05-SUMMARY.md` documenting:
- Task completion timestamps
- FoundPanel root migration complete
- SectionNavRail active-state neutral-bold pattern locked (critical D-13 semantic enforcement)
- CheckCircle completion icon emerald semantic confirmed
- No broken token violations found
- Pattern ready for Plans 6-7 interview body and apply gate
</output>
