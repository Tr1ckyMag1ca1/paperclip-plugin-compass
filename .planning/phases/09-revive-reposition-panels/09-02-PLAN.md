---
phase: 09
plan: 02
type: auto
wave: 1
depends_on: []
files_modified:
  - src/ui/revive/ActionQueuePanel.tsx
  - src/ui/revive/ActionConfirmationModal.tsx
  - src/ui/revive/SamplePivotModal.tsx
autonomous: true
requirements: [UIR-03, UIR-04]
user_setup: []

must_haves:
  truths:
    - "ActionQueuePanel root uses host tokens (bg-card, border-border, no broken spacing)"
    - "ActionQueuePanel title uses host typography (text-base font-semibold, not text-heading)"
    - "ActionConfirmationModal follows D-09 pattern: bg-card border border-border rounded-none, dim/blur backdrop"
    - "SamplePivotModal follows D-09 pattern: same modal chrome as ActionConfirmationModal"
    - "Modal backdrops use bg-background/80 backdrop-blur-sm (NOT bg-black/30)"
    - "All spacing tokens migrated per UI_REDO_HANDOFF.md (gap-md→gap-3, px-lg→px-4, py-md→py-3)"
    - "All components work correctly in light and dark modes"
  artifacts:
    - path: "src/ui/revive/ActionQueuePanel.tsx"
      provides: "ActionQueuePanel container migrated to host tokens"
      exports: ["ActionQueuePanel"]
      contains: "bg-card|border-border"
    - path: "src/ui/revive/ActionConfirmationModal.tsx"
      provides: "Modal with D-09 chrome: bg-card border border-border rounded-none, dim backdrop"
      exports: ["ActionConfirmationModal"]
      contains: "bg-background/80.*backdrop-blur-sm|bg-card.*border-border.*rounded-none"
    - path: "src/ui/revive/SamplePivotModal.tsx"
      provides: "Modal with unified D-09 chrome"
      exports: ["SamplePivotModal"]
      contains: "bg-background/80.*backdrop-blur-sm|bg-card.*border-border"
  key_links:
    - from: "src/ui/revive/ActionQueuePanel.tsx"
      to: "ActionItemCard"
      via: "component composition and array mapping"
      pattern: "ActionItemCard"
    - from: "src/ui/revive/ActionQueuePanel.tsx"
      to: "ActionConfirmationModal"
      via: "modal trigger and callback wiring"
      pattern: "ActionConfirmationModal|useState"
    - from: "src/ui/revive/ActionConfirmationModal.tsx"
      to: "backdrop element"
      via: "overlay with bg-background/80 backdrop-blur-sm"
      pattern: "bg-background/80.*backdrop-blur-sm"
    - from: "src/ui/revive/SamplePivotModal.tsx"
      to: "backdrop element"
      via: "overlay with same styling as ActionConfirmationModal"
      pattern: "bg-background/80.*backdrop-blur-sm"

---

<objective>
Migrate Revive action queue and confirmation modals to host tokens, applying the D-09 modal chrome pattern (sharp corners, semantic backdrop) established in Phase 8. Ensure ActionQueuePanel displays action queue with working confirmation flow; lock both modals to unified D-09 styling (no inline modal primitives extraction in v1.1 per D-10).

Purpose: Complete Revive mode panel suite with action selection flow and modal confirmations; establish modal chrome consistency across all modes (D-09 pattern carries forward from Phase 8).

Output: Three TypeScript React components (ActionQueuePanel + two modals) with host tokens and D-09 modal pattern, ready for integration with Plan 1 (RevivePanel base).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-revive-reposition-panels/09-CONTEXT.md
@.planning/phases/09-revive-reposition-panels/09-RESEARCH.md
@.planning/phases/09-revive-reposition-panels/09-UI-SPEC.md
@.planning/phases/08-assess-found-panels/08-02-PLAN.md
@.planning/phases/08-assess-found-panels/08-02-SUMMARY.md
@.planning/phases/08-assess-found-panels/08-03-PLAN.md
@.planning/phases/08-assess-found-panels/08-03-SUMMARY.md
@./UI_REDO_HANDOFF.md
@./CLAUDE.md
</context>

<interfaces>
<!-- Key types and contracts from existing codebase. Executor uses these directly. -->

From `src/types/revive.ts`:
```typescript
export type Priority = "low" | "medium" | "high";
export interface ActionItem {
  id: string;
  description: string;
  priority: Priority;
  estimatedHours?: number;
  completionDate?: string;
}
export interface SamplePivot {
  id: string;
  name: string;
  description: string;
}
```

Modal props patterns (from Phase 8 ApprovalRoutingModal, ConfirmationModal patterns):
```typescript
interface ModalProps {
  isOpen: boolean;
  onClose: () => void;
  onConfirm?: (data?: any) => void;
}
```

Host token names for modals (D-09 pattern):
```
Backdrop: bg-background/80 backdrop-blur-sm
Panel: bg-card border border-border rounded-none shadow-lg
Padding (modals): px-4 py-3 (16px horizontal, 12px vertical per Phase 9 UI-SPEC)
Gap: gap-3 for modal content spacing
Text: text-sm (body), text-base font-semibold (heading/title)
Buttons: px-3 py-2 text-xs font-medium, rounded-none
```

React.ReactElement return type and component pattern (consistent):
```typescript
import React from "react";
export function ComponentName(props: Props): React.ReactElement { }
```

No `cn()` helper in codebase; inline template literals for conditional classes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ActionQueuePanel to host tokens</name>
  <files>src/ui/revive/ActionQueuePanel.tsx</files>
  <action>
Migrate `src/ui/revive/ActionQueuePanel.tsx` to host tokens per UIR-03. This panel displays action queue and triggers ActionConfirmationModal:

1. **Root container baseline:**
   - Current: likely uses `bg-white p-lg gap-md space-y-lg` or similar broken tokens
   - Target per Phase 9 UI-SPEC.md: `bg-card border border-border rounded-none p-4` with vertical flex layout

2. **Token migration checklist:**
   - [ ] Container: `bg-card border border-border rounded-none` (Card styling per D-09)
   - [ ] Padding: `p-4` (16px, from Tailwind numeric scale)
   - [ ] Gap/spacing: `gap-lg` → `gap-4`, `space-y-lg` → `space-y-4`, `space-y-md` → `space-y-3`
   - [ ] Title/heading: `text-heading` → `text-base font-semibold` (per Phase 9 UI-SPEC.md Typography table)
   - [ ] Body text: `text-body` → `text-sm`
   - [ ] Remove broken tokens: `gap-sm` → `gap-2`, `px-md` → `px-3`, `py-sm` → `py-2`, `mb-md` → `mb-3`
   - [ ] Corners: `rounded` → `rounded-none`
   - [ ] Colors: Verify no light-only colors present; use semantic palette if needed

3. **Component structure:**
   - Root container (bg-card border border-border rounded-none p-4)
   - Title section (text-base font-semibold)
   - Action list mapping over actionItems array, rendering ActionItemCard for each
   - Confirmation modal trigger (likely a button with onClick handler)

4. **No new imports needed** — ActionQueuePanel should already import ActionItemCard from Plan 1.

Reference: Phase 8 Plan 2 Task 1 (evidence/confidence panel migration) and Phase 9 UI-SPEC.md "ActionQueuePanel: space-y-lg for action queue spacing".
  </action>
  <verify>
    <automated>
      grep -c "bg-card" src/ui/revive/ActionQueuePanel.tsx && \
      grep -c "border-border" src/ui/revive/ActionQueuePanel.tsx && \
      grep -c "rounded-none" src/ui/revive/ActionQueuePanel.tsx && \
      grep -c "p-4" src/ui/revive/ActionQueuePanel.tsx && \
      grep -c "text-base font-semibold" src/ui/revive/ActionQueuePanel.tsx && \
      [ $(grep -c "gap-md\|px-md\|py-sm\|space-y-lg\|space-y-md\|text-heading\|text-body\|rounded-lg\|rounded-xl" src/ui/revive/ActionQueuePanel.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    ActionQueuePanel.tsx migrated to host tokens. bg-card border-border rounded-none p-4 applied. All spacing per Tailwind numeric scale. Typography migrated (text-heading → text-base font-semibold). No broken tokens. Component structure unchanged. Ready for modal integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ActionConfirmationModal to D-09 chrome pattern</name>
  <files>src/ui/revive/ActionConfirmationModal.tsx</files>
  <action>
Migrate `src/ui/revive/ActionConfirmationModal.tsx` to host tokens and D-09 modal chrome pattern per UIR-03:

1. **Current state:**
   - Likely uses `bg-black/30` backdrop (WRONG — breaks light mode)
   - Likely uses `rounded-lg` or `rounded-xl` (WRONG — host uses sharp corners)
   - Likely uses custom spacing: `p-lg mb-md space-y-xs text-body text-label` (WRONG — custom tokens)

2. **Apply D-09 modal chrome pattern** (established Phase 8, locked in CONTEXT.md):
   - **Backdrop:** `fixed inset-0 bg-background/80 backdrop-blur-sm` (NOT `bg-black/30`)
   - **Panel:** `absolute ... bg-card border border-border rounded-none shadow-lg`
   - **Padding:** `px-4 py-3` (16px horizontal, 12px vertical per Phase 9 UI-SPEC.md)
   - **Gap:** `gap-3` for modal content spacing

3. **Structure (typical confirmation modal):**
   - Fixed backdrop overlay (full screen, transparent + blur)
   - Positioned modal panel (center or custom position)
   - Title (text-base font-semibold)
   - Content section (text-sm body text, can include description/confirmation prompt)
   - Button group (flex row, gap-2, right-aligned if dual-button)
   - Cancel button: `px-3 py-2 text-xs font-medium bg-muted text-foreground rounded-none border border-border hover:bg-muted/80`
   - Confirm button: `px-3 py-2 text-xs font-medium bg-foreground text-background rounded-none hover:bg-foreground/90` (per D-13 primary action)

4. **Token migration checklist:**
   - [ ] Backdrop: `fixed inset-0 bg-background/80 backdrop-blur-sm` (NOT `bg-black/30`)
   - [ ] Panel: `bg-card border border-border rounded-none shadow-lg` (NOT `rounded-lg`)
   - [ ] Padding: `px-4 py-3` (modal padding per UI-SPEC)
   - [ ] Gap: `gap-3` for vertical spacing between title/content/buttons
   - [ ] Title: `text-base font-semibold` (NOT `text-display` or `text-heading`)
   - [ ] Body: `text-sm` (NOT `text-body`)
   - [ ] Buttons: `px-3 py-2 text-xs font-medium rounded-none` (NOT `rounded-lg`, no custom button tokens)
   - [ ] Remove: `bg-black/30`, `rounded-lg`, `rounded-xl`, `p-lg`, `mb-md`, `space-y-xs`, `text-display`, `list-*`

5. **Component structure:**
   - Modal receives isOpen boolean, onClose callback, onConfirm callback
   - Renders nothing if !isOpen (or portal/fixed overlay if isOpen)
   - Props interface unchanged

6. **No new imports needed.**

Reference: Phase 8 Plan 3 Task 1 (ApprovalRoutingModal D-09 migration) and 09-CONTEXT.md "D-09 modal sharp corners + dim/blur backdrop" + "D-10 modals migrated in place — no Modal primitive extraction in v1.1".
  </action>
  <verify>
    <automated>
      grep -c "bg-background/80" src/ui/revive/ActionConfirmationModal.tsx && \
      grep -c "backdrop-blur-sm" src/ui/revive/ActionConfirmationModal.tsx && \
      grep -c "bg-card" src/ui/revive/ActionConfirmationModal.tsx && \
      grep -c "border-border" src/ui/revive/ActionConfirmationModal.tsx && \
      grep -c "rounded-none" src/ui/revive/ActionConfirmationModal.tsx && \
      grep -c "shadow-lg" src/ui/revive/ActionConfirmationModal.tsx && \
      [ $(grep -c "bg-black/30\|rounded-lg\|rounded-xl\|p-lg\|mb-md\|space-y-xs\|text-display\|list-" src/ui/revive/ActionConfirmationModal.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    ActionConfirmationModal.tsx migrated to D-09 chrome pattern. Backdrop: bg-background/80 backdrop-blur-sm (not bg-black/30). Panel: bg-card border border-border rounded-none shadow-lg (not rounded-lg). All spacing and typography per Tailwind native scale. No broken tokens. Component structure unchanged. Ready for integration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate SamplePivotModal to D-09 chrome pattern</name>
  <files>src/ui/revive/SamplePivotModal.tsx</files>
  <action>
Migrate `src/ui/revive/SamplePivotModal.tsx` to host tokens and D-09 modal chrome pattern per UIR-04:

1. **Current state:**
   - Likely uses same broken patterns as ActionConfirmationModal: `bg-black/30` backdrop, `rounded-lg` panel, custom spacing tokens
   - Likely uses `p-lg gap-md px-md py-sm` (broken custom tokens)

2. **Apply D-09 modal chrome pattern** (identical to ActionConfirmationModal, unlocked in Phase 8):
   - **Backdrop:** `fixed inset-0 bg-background/80 backdrop-blur-sm`
   - **Panel:** `absolute ... bg-card border border-border rounded-none shadow-lg px-4 py-3`
   - **Gap:** `gap-3` for content spacing

3. **Structure (typical pivot selection modal):**
   - Backdrop overlay (full screen)
   - Modal panel (centered or custom position)
   - Title (text-base font-semibold)
   - Pivot selection content (text-sm body, likely a list or grid of sample pivot options)
   - Action buttons (flex row, gap-2, same button styling as ActionConfirmationModal)

4. **Token migration checklist:**
   - [ ] Backdrop: `fixed inset-0 bg-background/80 backdrop-blur-sm`
   - [ ] Panel: `bg-card border border-border rounded-none shadow-lg px-4 py-3`
   - [ ] Gap: `gap-3`
   - [ ] Title: `text-base font-semibold`
   - [ ] Body: `text-sm`
   - [ ] Remove: `bg-black/30`, `rounded-lg`, `rounded-xl`, `p-lg`, `gap-md`, `px-md`, `py-sm`, `mb-lg`

5. **Component structure:**
   - SamplePivotModal receives isOpen boolean, onClose callback, onSelectPivot callback
   - Modal renders nothing if !isOpen
   - Props interface unchanged

6. **No new imports needed.**

Reference: Phase 8 Plan 3 (modal migration pattern) and ActionConfirmationModal Task 2 above.
  </action>
  <verify>
    <automated>
      grep -c "bg-background/80" src/ui/revive/SamplePivotModal.tsx && \
      grep -c "backdrop-blur-sm" src/ui/revive/SamplePivotModal.tsx && \
      grep -c "bg-card" src/ui/revive/SamplePivotModal.tsx && \
      grep -c "border-border" src/ui/revive/SamplePivotModal.tsx && \
      grep -c "rounded-none" src/ui/revive/SamplePivotModal.tsx && \
      grep -c "shadow-lg" src/ui/revive/SamplePivotModal.tsx && \
      [ $(grep -c "bg-black/30\|rounded-lg\|rounded-xl\|p-lg\|gap-md\|px-md\|py-sm\|mb-lg" src/ui/revive/SamplePivotModal.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    SamplePivotModal.tsx migrated to D-09 chrome pattern. Backdrop: bg-background/80 backdrop-blur-sm. Panel: bg-card border border-border rounded-none shadow-lg. All spacing and typography per Tailwind native scale. No broken tokens. Component structure unchanged. Ready for integration with Revive panels.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal isOpen prop ↔ UI registration | Modal visibility controlled by parent state; modal receives boolean, renders overlay accordingly |
| User action selection ↔ onConfirm callback | User selects action in ActionConfirmationModal; selection passed to parent callback for processing |
| Sample pivot selection ↔ onSelectPivot callback | User selects pivot in SamplePivotModal; pivot data passed to parent callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Spoofing | Modal titles + content | accept | Display-only text; no security-sensitive data in modal body (per Phase 8 modal audit) |
| T-09-05 | Denial of Service | Large action list in ActionQueuePanel | accept | ActionQueuePanel receives finite array; no unbounded rendering |
| T-09-06 | Information Disclosure | Modal backdrop blur | mitigate | Backdrop blur-sm with bg-background/80 prevents content bleed-through; maintains visual separation |

</threat_model>

<verification>
Plan 2 completion verified when:
1. `npm run typecheck` passes (all three files)
2. `npm run test:run` includes modal tests, all passing
3. Grep audit: 0 hits for broken tokens in src/ui/revive/{ActionQueuePanel,ActionConfirmationModal,SamplePivotModal}.tsx
4. Build succeeds: `npm run build` → dist/{worker.js,manifest.js,ui/index.js} compiled
5. All files use host tokens exclusively (bg-background/80, bg-card, border-border, text-*, gap-*, px-*, py-*, rounded-none)
6. Backdropannel D-09 chrome verified: `bg-background/80 backdrop-blur-sm` (not `bg-black/30`)
</verification>

<success_criteria>
- [x] ActionQueuePanel: bg-card border border-border rounded-none p-4, all spacing numeric
- [x] ActionConfirmationModal: D-09 chrome (backdrop: bg-background/80 backdrop-blur-sm; panel: bg-card border border-border rounded-none shadow-lg)
- [x] SamplePivotModal: D-09 chrome (identical to ActionConfirmationModal)
- [x] Zero broken custom tokens in all three files (gap-md, px-lg, py-sm, text-heading, text-body, text-label, rounded-lg, bg-black/30)
- [x] All components pass typecheck and tests
- [x] Build succeeds
- [x] Modals maintain functionality (isOpen, onClose, onConfirm/onSelectPivot callbacks unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/09-revive-reposition-panels/09-02-SUMMARY.md` with:
- 3 components migrated (ActionQueuePanel, two modals)
- D-09 modal chrome pattern applied consistently
- All tokens verified per host baseline
- Ready for Plan 3 (Reposition panels + verification gates)
</output>
