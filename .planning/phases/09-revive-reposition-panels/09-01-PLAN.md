---
phase: 09
plan: 01
type: auto
wave: 1
depends_on: []
files_modified:
  - src/ui/revive/RevivePanel.tsx
  - src/ui/revive/ActionItemCard.tsx
  - src/ui/revive/PriorityBadge.tsx
  - src/ui/revive/StallSummaryBadge.tsx
  - tests/ui/revive/PriorityBadge.test.tsx
autonomous: true
requirements: [UIR-01, UIR-02]
user_setup: []

must_haves:
  truths:
    - "RevivePanel root container uses host tokens (bg-background, no broken spacing tokens)"
    - "ActionItemCard uses host tokens (bg-card, border-border, rounded-none, semantic padding)"
    - "PriorityBadge uses static PRIORITY_CLASSES map with full class strings (low/medium/high)"
    - "StallSummaryBadge uses host tokens (semantic palette, no light-only colors)"
    - "All spacing tokens migrated per UI_REDO_HANDOFF.md (gap-sm→gap-2, px-md→px-3, py-sm→py-2)"
    - "All components work correctly in light and dark modes"
  artifacts:
    - path: "src/ui/revive/RevivePanel.tsx"
      provides: "RevivePanel root container migrated to host tokens"
      exports: ["RevivePanel"]
      contains: "bg-background"
    - path: "src/ui/revive/ActionItemCard.tsx"
      provides: "ActionItemCard card layout migrated to host tokens"
      exports: ["ActionItemCard"]
      contains: "bg-card|border-border"
    - path: "src/ui/revive/PriorityBadge.tsx"
      provides: "PriorityBadge with static PRIORITY_CLASSES map"
      exports: ["PriorityBadge"]
      contains: "PRIORITY_CLASSES"
    - path: "src/ui/revive/StallSummaryBadge.tsx"
      provides: "StallSummaryBadge with semantic palette"
      exports: ["StallSummaryBadge"]
      contains: "text-red-600|text-yellow-600|text-muted-foreground"
    - path: "tests/ui/revive/PriorityBadge.test.tsx"
      provides: "Unit tests for priority map"
      contains: "low.*medium.*high"
  key_links:
    - from: "src/ui/revive/RevivePanel.tsx"
      to: "ActionItemCard"
      via: "component composition and array mapping"
      pattern: "ActionItemCard"
    - from: "src/ui/revive/ActionItemCard.tsx"
      to: "PriorityBadge + StallSummaryBadge"
      via: "child component imports and rendering"
      pattern: "PriorityBadge|StallSummaryBadge"
    - from: "src/ui/revive/PriorityBadge.tsx"
      to: "PRIORITY_CLASSES"
      via: "static map lookup in className"
      pattern: "PRIORITY_CLASSES\\[.*priority.*\\]"
    - from: "tests/ui/revive/PriorityBadge.test.tsx"
      to: "priority map verification"
      via: "assertions on rendered class strings"
      pattern: "expect.*PRIORITY"

---

<objective>
Migrate Revive panel root containers and establish the priority/severity color map pattern for action cards and badges. Lock explicit static Record pattern for dynamic colors (Tailwind JIT-safe per D-02); establish RevivePanel and ActionItemCard root token baseline, including badge components (PriorityBadge, StallSummaryBadge).

Purpose: Pattern-setting for all dynamic color usage in Revive mode; establishes priority color semantics (low/medium/high) and stall severity indicators for immediate reuse in ActionQueuePanel and modals.

Output: Four TypeScript React components with priority/severity map patterns + unit tests, ready for Plan 2 (action queue + modals).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-revive-reposition-panels/09-CONTEXT.md
@.planning/phases/09-revive-reposition-panels/09-RESEARCH.md
@.planning/phases/09-revive-reposition-panels/09-UI-SPEC.md
@.planning/phases/08-assess-found-panels/08-01-PLAN.md
@.planning/phases/08-assess-found-panels/08-01-SUMMARY.md
@./UI_REDO_HANDOFF.md
@./CLAUDE.md
</context>

<interfaces>
<!-- Key types and contracts from existing codebase. Executor uses these directly. -->

From `src/types/revive.ts` (inferred from current codebase):
```typescript
export type Priority = "low" | "medium" | "high";
export type StallSeverity = "low" | "medium" | "high";

export interface ActionItem {
  id: string;
  description: string;
  priority: Priority;
  estimatedHours?: number;
  completionDate?: string;
}

export interface StallSummary {
  severity: StallSeverity;
  duration: string;
  blockingFactor: number;
}
```

React.ReactElement return type and component pattern (consistent across all Compass components):
```typescript
import React from "react";
export function ComponentName(props: Props): React.ReactElement { }
```

Host token names available (verified Phase 7-8):
```
Surfaces: bg-background, bg-card, bg-muted
Text: text-foreground, text-muted-foreground
Borders: border-border
Semantic colors: text-yellow-600, bg-yellow-500/10, text-red-600, bg-red-500/10, emerald tokens for success
Spacing: gap-1, gap-2, gap-3, gap-4, gap-6, px-2, px-3, px-4, py-2, py-3, py-4
Corners: rounded-none (NEVER rounded-lg/rounded-xl)
```

Phase 8 SEVERITY_CLASSES pattern (reference for Phase 9 PRIORITY_CLASSES):
```typescript
const SEVERITY_CLASSES: Record<"low" | "medium" | "high", string> = {
  low: "text-muted-foreground",
  medium: "text-yellow-600 bg-yellow-500/10",
  high: "text-red-600 bg-red-500/10"
};
```

No `cn()` helper in codebase; inline template literals for conditional classes (match existing pattern).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor PriorityBadge with static priority color map</name>
  <files>src/ui/revive/PriorityBadge.tsx</files>
  <action>
Refactor `src/ui/revive/PriorityBadge.tsx` to replace dynamic priority color classes with static semantic palette map per D-02:

1. **Current state (from PriorityBadge.tsx):**
   - Currently uses dynamic if/else (lines 27-39) with class variables (bgClass, textClass)
   - Uses broken tokens: `bg-accent/20`, `text-accent`, `text-destructive`, `bg-destructive/20` (which do not exist in Paperclip host)
   - Uses `px-xs py-xs text-label rounded` (broken custom tokens: px-xs, text-label)

2. **Replace with PRIORITY_CLASSES static map** (exact pattern from Phase 8 D-02, adapted for Priority type):
   ```typescript
   const PRIORITY_CLASSES: Record<"low" | "medium" | "high", string> = {
     low: "px-2 py-1 text-xs font-medium text-muted-foreground bg-muted rounded-none",
     medium: "px-2 py-1 text-xs font-medium text-yellow-600 bg-yellow-500/10 rounded-none",
     high: "px-2 py-1 text-xs font-medium text-red-600 bg-red-500/10 rounded-none"
   };
   ```
   - Place at TOP of file after imports, before component function
   - Full class strings (NOT partials or arrays) — Tailwind JIT requires static string literals
   - Opacity modifiers (/10) are valid with host OKLCH tokens (verified Phase 7-8)
   - Padding: `px-2 py-1` (8px horizontal, 4px vertical) per Phase 9 UI-SPEC.md tight badge sizing
   - Text: `text-xs font-medium` (12px, weight 500, per UI-SPEC Typography table)
   - Corners: `rounded-none` per D-09 (no rounded-lg)

3. **Update component body to consume map:**
   - Delete if/else block (lines 27-39)
   - Replace with single line: `const classes = PRIORITY_CLASSES[priority];`
   - Update span className to: `className={classes}`
   - Keep aria-label logic intact: `aria-label={`Priority: ${label}`}` where label is derived from priority

4. **Token migration checklist:**
   - [ ] `px-xs py-xs text-label rounded` → `px-2 py-1 text-xs font-medium rounded-none`
   - [ ] `bg-accent/20 text-accent` → `bg-yellow-500/10 text-yellow-600` (medium)
   - [ ] `bg-destructive/20 text-destructive` → `bg-red-500/10 text-red-600` (high)
   - [ ] Add low severity class: `bg-muted text-muted-foreground`
   - [ ] NEVER use `rounded`, `rounded-lg`, `rounded-xl` — use `rounded-none` per D-09

5. **Component structure preserved:**
   - Props interface (PriorityBadgeProps with priority: number) unchanged
   - Label text derivation from priority threshold (0.66/0.33) unchanged
   - aria-label attribute unchanged
   - Only className logic refactored to use static map

6. **No new imports needed** — PRIORITY_CLASSES is local const.

Reference: Phase 8 Plan 1 Task 1 (DriftItemCard severity map pattern) and 09-RESEARCH.md "Severity priority badge pattern (new to Phase 9)".
  </action>
  <verify>
    <automated>
      grep -c "const PRIORITY_CLASSES" src/ui/revive/PriorityBadge.tsx && \
      grep -c "low.*medium.*high" src/ui/revive/PriorityBadge.tsx && \
      grep -c "text-yellow-600" src/ui/revive/PriorityBadge.tsx && \
      grep -c "text-red-600" src/ui/revive/PriorityBadge.tsx && \
      grep -c "bg-yellow-500/10" src/ui/revive/PriorityBadge.tsx && \
      grep -c "bg-red-500/10" src/ui/revive/PriorityBadge.tsx && \
      [ $(grep -c "px-xs\|py-xs\|text-label\|bg-accent\|text-accent\|text-destructive\|rounded-lg\|rounded-xl" src/ui/revive/PriorityBadge.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    PriorityBadge.tsx refactored with PRIORITY_CLASSES static map at top of file. All priority display logic uses map lookup. No broken tokens present. Component structure and prop interface unchanged. Ready for testing and use in ActionItemCard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor StallSummaryBadge with semantic palette</name>
  <files>src/ui/revive/StallSummaryBadge.tsx</files>
  <action>
Refactor `src/ui/revive/StallSummaryBadge.tsx` to replace dynamic severity color classes with semantic palette per D-02 (reuse severity map pattern from Phase 8):

1. **Current state:**
   - Uses dynamic severity-to-color mapping (likely with custom tokens: `bg-accent/20`, `text-accent`, `bg-destructive/20`, etc.)
   - Uses broken custom spacing: `my-md px-md py-sm rounded text-label` (custom tokens)

2. **Replace with STALL_SEVERITY_CLASSES static map:**
   ```typescript
   const STALL_SEVERITY_CLASSES: Record<"low" | "medium" | "high", string> = {
     low: "px-3 py-2 text-xs font-medium text-muted-foreground bg-muted rounded-none",
     medium: "px-3 py-2 text-xs font-medium text-yellow-600 bg-yellow-500/10 rounded-none",
     high: "px-3 py-2 text-xs font-medium text-red-600 bg-red-500/10 rounded-none"
   };
   ```
   - Place at TOP of file after imports
   - Padding: `px-3 py-2` (12px horizontal, 8px vertical) per Phase 9 UI-SPEC.md "StallSummaryBadge: px-3 py-2 for slightly larger badge"
   - Text: `text-xs font-medium` (12px, weight 500)
   - Corners: `rounded-none` per D-09

3. **Update component body:**
   - Delete if/else severity color mapping
   - Replace with: `const severityClasses = STALL_SEVERITY_CLASSES[severity];`
   - Update root div className to: `className={severityClasses}`

4. **Token migration checklist:**
   - [ ] `my-md px-md py-sm rounded text-label` → `px-3 py-2 text-xs font-medium rounded-none`
   - [ ] `bg-accent/20 text-accent` → `bg-yellow-500/10 text-yellow-600` (medium)
   - [ ] `bg-destructive/20 text-destructive` → `bg-red-500/10 text-red-600` (high)
   - [ ] Add low severity class: `bg-muted text-muted-foreground`
   - [ ] NEVER use `rounded`, `rounded-lg`, `rounded-xl`

5. **Component structure:**
   - Props: StallSummaryBadgeProps (likely contains severity: "low" | "medium" | "high", duration: string, blockingFactor: number)
   - Children/subcomponents (if any) unchanged
   - Only color/spacing logic refactored

6. **No new imports needed.**

Reference: Phase 8 Plan 1 Task 1 (severity map pattern) and 09-RESEARCH.md.
  </action>
  <verify>
    <automated>
      grep -c "const STALL_SEVERITY_CLASSES" src/ui/revive/StallSummaryBadge.tsx && \
      grep -c "text-yellow-600" src/ui/revive/StallSummaryBadge.tsx && \
      grep -c "text-red-600" src/ui/revive/StallSummaryBadge.tsx && \
      grep -c "bg-yellow-500/10" src/ui/revive/StallSummaryBadge.tsx && \
      grep -c "bg-red-500/10" src/ui/revive/StallSummaryBadge.tsx && \
      grep -c "px-3 py-2" src/ui/revive/StallSummaryBadge.tsx && \
      [ $(grep -c "my-md\|px-md\|py-sm\|text-label\|bg-accent\|text-accent\|text-destructive\|rounded-lg\|rounded-xl" src/ui/revive/StallSummaryBadge.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    StallSummaryBadge.tsx refactored with STALL_SEVERITY_CLASSES static map. All severity display logic uses map lookup. No broken tokens present. Component structure unchanged. Ready for integration with ActionItemCard.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate ActionItemCard to host tokens</name>
  <files>src/ui/revive/ActionItemCard.tsx</files>
  <action>
Migrate `src/ui/revive/ActionItemCard.tsx` to host tokens per UIR-02. This card wraps action item data and renders PriorityBadge + StallSummaryBadge (now refactored in Tasks 1-2):

1. **Root container baseline:**
   - Current: likely uses `bg-white p-md gap-md rounded` or similar broken tokens
   - Target per Phase 9 UI-SPEC.md: `bg-card border border-border rounded-none p-3 gap-3`

2. **Token migration checklist:**
   - [ ] Container: `bg-card border border-border rounded-none` (Card-like styling)
   - [ ] Padding: `p-3` (12px, from Tailwind numeric scale per UI-SPEC "ActionItemCard internal spacing: gap-3 for action content flex layout, p-3 for card padding")
   - [ ] Gap: `gap-3` between flex children (12px)
   - [ ] Text: All text uses `text-sm` (body), `text-xs font-medium` (labels)
   - [ ] Remove broken tokens: `gap-md` → `gap-3`, `px-md` → `px-3`, `py-sm` → `py-2`, `rounded` → `rounded-none`
   - [ ] Remove light-only colors: Replace with semantic palette (none expected in layout, but verify)
   - [ ] Remove custom text tokens: `text-body` → `text-sm`, `text-label` → `text-xs font-medium`

3. **Component structure:**
   - Root div container with flex layout (row or column per design)
   - Renders child content: action description, priority badge (PriorityBadge), stall summary badge (StallSummaryBadge), etc.
   - No changes to child imports — just verify components are wired (already refactored in Tasks 1-2)

4. **No new imports needed.**

Reference: Phase 8 Plan 1 Task 2-3 (AssessPanel + DriftReportPanel migration patterns) and Phase 9 UI-SPEC.md "ActionItemCard internal spacing" section.
  </action>
  <verify>
    <automated>
      grep -c "bg-card" src/ui/revive/ActionItemCard.tsx && \
      grep -c "border-border" src/ui/revive/ActionItemCard.tsx && \
      grep -c "rounded-none" src/ui/revive/ActionItemCard.tsx && \
      grep -c "p-3\|gap-3" src/ui/revive/ActionItemCard.tsx && \
      [ $(grep -c "gap-md\|px-md\|py-sm\|text-body\|text-label\|bg-white\|rounded-lg\|rounded-xl" src/ui/revive/ActionItemCard.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    ActionItemCard.tsx migrated to host tokens. bg-card border-border rounded-none applied. All spacing per Tailwind numeric scale. No broken tokens. Component structure unchanged. Ready for RevivePanel integration.
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate RevivePanel root container to host tokens</name>
  <files>src/ui/revive/RevivePanel.tsx</files>
  <action>
Migrate `src/ui/revive/RevivePanel.tsx` root container and layout to host tokens per UIR-01:

1. **Root container baseline:**
   - Current: likely has custom tokens (gap-md, p-lg, etc.) or plain div
   - Target per Phase 9 UI-SPEC.md: `bg-background` (panel background, not bg-card) with vertical flex layout
   - Apply: `className="bg-background flex flex-col gap-4"` or similar vertical flex

2. **Token migration checklist:**
   - [ ] Root: `bg-background` (panel background, not bg-card)
   - [ ] Layout: `flex flex-col` (vertical stack)
   - [ ] Gap/spacing: `gap-md` → `gap-3`, `gap-lg` → `gap-4`, `space-y-md` → `space-y-3`
   - [ ] Padding (if present): `p-md` → `p-4`, `px-md` → `px-3`, `py-sm` → `py-2`
   - [ ] Remove: `gap-xs`, `gap-sm` → `gap-1`/`gap-2`; `space-y-lg` → `space-y-4`
   - [ ] Borders: `border-gray-200` → `border-border`
   - [ ] Corners: `rounded` → `rounded-none`, no `rounded-lg`/`rounded-xl`
   - [ ] Typography: `text-heading` → `text-base font-semibold`, `text-body` → `text-sm`

3. **Component structure:**
   - Root container wraps all RevivePanel content (likely ActionQueuePanel and other child panels)
   - Props interface (RevivePanelProps) unchanged
   - Children updated in Plan 2

4. **No new imports needed.**

Reference: Phase 8 Plan 1 Task 2 (AssessPanel migration pattern) and Phase 9 UI-SPEC.md "Typography: Heading (16px, 600) for RevivePanel title".
  </action>
  <verify>
    <automated>
      grep -c "bg-background" src/ui/revive/RevivePanel.tsx && \
      grep -c "flex flex-col\|flex-col" src/ui/revive/RevivePanel.tsx && \
      grep -c "gap-[0-4]" src/ui/revive/RevivePanel.tsx | head -1 && \
      [ $(grep -c "gap-xs\|gap-sm\|gap-md\|gap-lg\|px-sm\|px-md\|py-sm\|py-md\|space-y-md\|space-y-lg\|rounded-lg\|rounded-xl\|text-heading\|text-body" src/ui/revive/RevivePanel.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    RevivePanel.tsx root container migrated to host tokens. bg-background applied. All spacing per Tailwind numeric scale. No broken tokens. Component structure unchanged. Ready for Plan 2 (action queue + modals).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin UI ↔ Host React Tree | Plugin components render in host React tree; token classes must match host Tailwind config |
| ActionItemCard props ↔ Rendering | ActionItemCard receives action data (priority, description); must validate priority enum before rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Integrity | PRIORITY_CLASSES map | mitigate | Static const Record guarantees no runtime injection; full class strings prevent Tailwind JIT collision |
| T-09-02 | Spoofing | Priority/Severity labels | accept | Low-value display text; priority score calculated in parent action queue logic, not here |
| T-09-03 | Denial of Service | Large action list rendering | accept | RevivePanel receives finite action array; no unbounded loops in Plan 1 scope |

</threat_model>

<verification>
Plan 1 completion verified when:
1. `npm run typecheck` passes (all four files)
2. `npm run test:run` includes new PriorityBadge.test.tsx tests, all passing
3. Grep audit: 0 hits for broken tokens in src/ui/revive/{RevivePanel,ActionItemCard,PriorityBadge,StallSummaryBadge}.tsx
4. Build succeeds: `npm run build` → dist/{worker.js,manifest.js,ui/index.js} compiled
5. All files use host tokens exclusively (bg-background, bg-card, border-border, text-*, gap-*, px-*, py-*, rounded-none)
</verification>

<success_criteria>
- [x] RevivePanel root: bg-background, flex flex-col, numeric gap/padding only
- [x] ActionItemCard: bg-card border border-border rounded-none, p-3 gap-3
- [x] PriorityBadge: static PRIORITY_CLASSES map with low/medium/high classes
- [x] StallSummaryBadge: static STALL_SEVERITY_CLASSES map with semantic colors
- [x] Zero broken custom tokens (gap-xs, px-md, text-label, rounded-lg, bg-accent, text-accent, text-destructive)
- [x] All components pass typecheck and tests
- [x] Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-revive-reposition-panels/09-01-SUMMARY.md` with:
- 4 components migrated + 1 test file created
- PRIORITY_CLASSES and STALL_SEVERITY_CLASSES patterns established
- All tokens verified per host baseline
- Ready for Plan 2 (action queue + modals)
</output>
