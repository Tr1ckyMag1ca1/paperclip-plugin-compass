---
phase: 09
plan: 03
type: auto
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - src/ui/reposition/RepositionPanel.tsx
  - src/ui/reposition/RepositionInterviewFlow.tsx
  - src/ui/reposition/IntentEntry.tsx
  - src/ui/reposition/ScopeConfirmation.tsx
  - src/ui/reposition/CascadeReviewPanel.tsx
autonomous: false
requirements: [UIRP-01, UIRP-02, UIRP-03]
user_setup: []

must_haves:
  truths:
    - "RepositionPanel root uses host tokens (bg-background, no broken spacing)"
    - "RepositionInterviewFlow wraps Found interview components; wrapper styling uses host tokens"
    - "IntentEntry uses host tokens (bg-card, border-border for textarea, rounded-none)"
    - "ScopeConfirmation modal follows D-09 chrome pattern (bg-background/80 backdrop, bg-card panel)"
    - "CascadeReviewPanel modal follows D-09 chrome pattern; all confirm/apply modals consistent across modes"
    - "All spacing tokens migrated per UI_REDO_HANDOFF.md (gap-md→gap-3, px-lg→px-4, py-md→py-3)"
    - "React shim guard: zero forbidden imports (useId, useReducer, etc.) in src/ui/reposition/"
    - "Build passes: npm run build"
    - "Typecheck passes: npm run typecheck"
    - "Tests pass: npm run test:run"
  artifacts:
    - path: "src/ui/reposition/RepositionPanel.tsx"
      provides: "RepositionPanel root container migrated to host tokens"
      exports: ["RepositionPanel"]
      contains: "bg-background"
    - path: "src/ui/reposition/RepositionInterviewFlow.tsx"
      provides: "Wrapper for Found interview components; reuses Phase 8 migrated interview machinery"
      exports: ["RepositionInterviewFlow"]
      contains: "flex.*flex-col"
    - path: "src/ui/reposition/IntentEntry.tsx"
      provides: "Intent textarea input with host tokens"
      exports: ["IntentEntry"]
      contains: "bg-card|border-border"
    - path: "src/ui/reposition/ScopeConfirmation.tsx"
      provides: "Modal with D-09 chrome (scope confirmation flow)"
      exports: ["ScopeConfirmation"]
      contains: "bg-background/80.*backdrop-blur-sm|bg-card.*border-border"
    - path: "src/ui/reposition/CascadeReviewPanel.tsx"
      provides: "Modal with D-09 chrome (cascade review)"
      exports: ["CascadeReviewPanel"]
      contains: "bg-background/80.*backdrop-blur-sm|bg-card.*border-border"
  key_links:
    - from: "src/ui/reposition/RepositionPanel.tsx"
      to: "RepositionInterviewFlow + IntentEntry"
      via: "component composition and state wiring"
      pattern: "RepositionInterviewFlow|IntentEntry"
    - from: "src/ui/reposition/RepositionInterviewFlow.tsx"
      to: "Phase 8 Found interview components"
      via: "reuses InterviewSection, SectionNavRail, QuestionRenderer from src/ui/found/"
      pattern: "InterviewSection|SectionNavRail|QuestionRenderer"
    - from: "src/ui/reposition/IntentEntry.tsx"
      to: "textarea element"
      via: "form input with host tokens"
      pattern: "textarea.*className.*bg-card"
    - from: "src/ui/reposition/ScopeConfirmation.tsx"
      to: "backdrop element"
      via: "modal overlay with D-09 chrome"
      pattern: "bg-background/80.*backdrop-blur-sm"
    - from: "src/ui/reposition/CascadeReviewPanel.tsx"
      to: "backdrop element"
      via: "modal overlay with D-09 chrome"
      pattern: "bg-background/80.*backdrop-blur-sm"

---

<objective>
Migrate remaining Reposition mode panels (RepositionPanel, RepositionInterviewFlow, IntentEntry, ScopeConfirmation, CascadeReviewPanel) to host tokens. RepositionInterviewFlow reuses Found interview components from Phase 8 (already migrated); focus on wrapper styling. Apply D-09 modal chrome to both Reposition modals. Includes critical verification gates: React shim guard (npm run lint:shim), live mount smoke test (VPS), and plugin SDK payload audit (new requirements from Phase 9 CONTEXT.md).

Purpose: Complete all mode panel migrations (Assess, Found, Revive, Reposition all shipped with host tokens); establish gate procedures for Phase 10 verification (grep + live mount + payload audit).

Output: Five TypeScript React components with host tokens + D-09 modals, plus three verification checkpoints (shim guard, live host mount smoke test, SDK payload audit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-revive-reposition-panels/09-CONTEXT.md
@.planning/phases/09-revive-reposition-panels/09-RESEARCH.md
@.planning/phases/09-revive-reposition-panels/09-UI-SPEC.md
@.planning/phases/08-assess-found-panels/08-05-PLAN.md
@.planning/phases/08-assess-found-panels/08-05-SUMMARY.md
@./UI_REDO_HANDOFF.md
@./CLAUDE.md
@./scripts/check-react-shim.mjs
</context>

<interfaces>
<!-- Key types and contracts from existing codebase. Executor uses these directly. -->

From `src/types/reposition.ts`:
```typescript
export interface RepositionContext {
  currentIntentId?: string;
  currentScope?: string;
  cascadeReview?: CascadeReviewData;
}

export interface CascadeReviewData {
  affectedAreas: string[];
  cascadingDecisions: string[];
}
```

From Phase 8 Found interview components (reused in RepositionInterviewFlow):
```typescript
// src/ui/found/InterviewSection.tsx, SectionNavRail.tsx, QuestionRenderer.tsx
// All already migrated to host tokens in Phase 8; RepositionInterviewFlow wraps these
```

Modal props patterns (from Phase 8 + Plan 2):
```typescript
interface ModalProps {
  isOpen: boolean;
  onClose: () => void;
  onConfirm?: (data?: any) => void;
}
```

Host token names (verified Phase 7-8-9):
```
Surfaces: bg-background, bg-card, bg-muted
Text: text-foreground, text-muted-foreground, text-sm, text-xs font-medium, text-base font-semibold
Borders: border-border
Semantic: text-yellow-600, bg-yellow-500/10, text-red-600, bg-red-500/10
Spacing: gap-1, gap-2, gap-3, gap-4, gap-6, px-2, px-3, px-4, py-2, py-3, py-4
Corners: rounded-none (NEVER rounded-lg/rounded-xl)
Modal backdrop: bg-background/80 backdrop-blur-sm (NOT bg-black/30)
```

React.ReactElement return type and component pattern:
```typescript
import React from "react";
export function ComponentName(props: Props): React.ReactElement { }
```

No `cn()` helper; inline template literals for conditional classes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate RepositionPanel root container to host tokens</name>
  <files>src/ui/reposition/RepositionPanel.tsx</files>
  <action>
Migrate `src/ui/reposition/RepositionPanel.tsx` root container to host tokens per UIRP-01:

1. **Root container baseline:**
   - Current: likely uses custom tokens (gap-lg, p-lg, etc.)
   - Target: `bg-background` (panel background, not bg-card) with vertical flex layout
   - Apply: `className="bg-background flex flex-col gap-4"`

2. **Token migration checklist:**
   - [ ] Root: `bg-background` (panel background)
   - [ ] Layout: `flex flex-col` (vertical stack)
   - [ ] Gap: `gap-lg` → `gap-4`, `space-y-lg` → `space-y-4`
   - [ ] Padding (if present): `p-lg` → `p-6`, `px-lg` → `px-4`, `py-md` → `py-3`
   - [ ] Borders: `border-gray-200` → `border-border`
   - [ ] Corners: `rounded` → `rounded-none`, no `rounded-lg`/`rounded-xl`
   - [ ] Typography: `text-heading` → `text-base font-semibold`

3. **Component structure:**
   - Root wraps RepositionInterviewFlow, IntentEntry, and modals
   - Props interface (RepositionPanelProps) unchanged
   - Children components updated in Tasks 2-5

4. **No new imports needed.**

Reference: Phase 8 Plan 1 Task 2 (AssessPanel pattern) and Phase 9 Plan 1 Task 4 (RevivePanel pattern).
  </action>
  <verify>
    <automated>
      grep -c "bg-background" src/ui/reposition/RepositionPanel.tsx && \
      grep -c "flex flex-col\|flex-col" src/ui/reposition/RepositionPanel.tsx && \
      grep -c "gap-[0-6]" src/ui/reposition/RepositionPanel.tsx | head -1 && \
      [ $(grep -c "gap-lg\|gap-md\|px-lg\|py-md\|text-heading\|rounded-lg\|rounded-xl" src/ui/reposition/RepositionPanel.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    RepositionPanel.tsx root container migrated to host tokens. bg-background applied. All spacing per Tailwind numeric scale. No broken tokens. Component structure unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate RepositionInterviewFlow wrapper to host tokens</name>
  <files>src/ui/reposition/RepositionInterviewFlow.tsx</files>
  <action>
Migrate `src/ui/reposition/RepositionInterviewFlow.tsx` wrapper styling to host tokens per UIRP-01. This component wraps Phase 8 Found interview machinery (InterviewSection, SectionNavRail, QuestionRenderer — all already migrated):

1. **Current state:**
   - Likely uses custom spacing tokens (gap-lg, p-lg, gap-sm, gap-md, px-lg, py-md)
   - Wrapper layout (flex container for interview flow)

2. **Token migration checklist:**
   - [ ] Container: `flex flex-col` (vertical layout for interview flow)
   - [ ] Gap: `gap-lg` → `gap-4`, `gap-md` → `gap-3`, `gap-sm` → `gap-2`
   - [ ] Padding: `p-lg` → `p-6`, `px-lg` → `px-4`, `pt-md` → `pt-3`
   - [ ] Typography (wrapper level): `text-body` → `text-sm`, `text-foreground/70` (if used) → `text-muted-foreground`
   - [ ] Spacing between sections: `space-y-lg` → `space-y-4`
   - [ ] Remove: `gap-xs`, `gap-sm` (if using Tailwind numeric), `rounded` → `rounded-none`

3. **Component structure:**
   - RepositionInterviewFlow wraps InterviewSection, SectionNavRail (both from Phase 8, already host-token-safe)
   - No changes to child imports or props
   - Focus only on wrapper div styling
   - Verify no forbidden React imports (useId, etc. — checked in Task 6)

4. **No new imports needed.**

Reference: Phase 8 Plan 5 Task 1 (FoundPanel wrapper) and Phase 9 UI-SPEC.md "RepositionInterviewFlow: gap-lg → gap-4, space-y-lg → space-y-4".
  </action>
  <verify>
    <automated>
      grep -c "flex flex-col\|flex-col" src/ui/reposition/RepositionInterviewFlow.tsx && \
      grep -c "gap-[0-6]" src/ui/reposition/RepositionInterviewFlow.tsx | head -1 && \
      grep -c "text-sm\|text-muted-foreground" src/ui/reposition/RepositionInterviewFlow.tsx && \
      grep -c "InterviewSection\|SectionNavRail\|QuestionRenderer" src/ui/reposition/RepositionInterviewFlow.tsx && \
      [ $(grep -c "gap-lg\|gap-md\|gap-sm\|px-lg\|py-md\|pt-md\|text-body\|text-foreground/70\|rounded-lg\|rounded-xl" src/ui/reposition/RepositionInterviewFlow.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    RepositionInterviewFlow.tsx wrapper migrated to host tokens. All spacing per Tailwind numeric scale. No broken tokens. Reuses Phase 8 Found interview components (already host-token-safe). Ready for integration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate IntentEntry textarea to host tokens</name>
  <files>src/ui/reposition/IntentEntry.tsx</files>
  <action>
Migrate `src/ui/reposition/IntentEntry.tsx` to host tokens per UIRP-02. This component is a text input for intent entry:

1. **Current state:**
   - Likely uses custom tokens: `space-y-lg p-lg rounded text-body` (broken)
   - Textarea element with custom border/focus styling (likely `border-gray-200 focus:border-accent`)

2. **Token migration checklist:**
   - [ ] Container: `space-y-lg` → `space-y-4` (vertical gap between label/textarea/help text)
   - [ ] Padding (wrapper): `p-lg` → `p-6`
   - [ ] Textarea: `bg-card border border-border rounded-none text-sm` (per Phase 9 UI-SPEC.md "IntentEntry textarea styling")
   - [ ] Focus styling: `focus:border-border focus:ring-1 focus:ring-foreground` (match host focus pattern, NOT `focus:border-accent`)
   - [ ] Placeholder: `placeholder-muted-foreground`
   - [ ] Label: `text-sm` (body) or `text-xs font-medium` (label) depending on UX design
   - [ ] Help text: `text-xs text-muted-foreground`
   - [ ] Remove: `rounded-lg`, `border-gray-200`, `focus:border-accent`, custom padding

3. **Component structure:**
   - Label (text-xs font-medium or text-sm)
   - Textarea (bg-card border border-border rounded-none)
   - Help text (text-xs text-muted-foreground)
   - Props: likely IntentEntryProps with label, placeholder, value, onChange, etc.

4. **No new imports needed.**

Reference: Phase 8 Plan 6 (form input patterns) and Phase 9 UI-SPEC.md "IntentEntry: space-y-lg → space-y-4, textarea styling".
  </action>
  <verify>
    <automated>
      grep -c "bg-card" src/ui/reposition/IntentEntry.tsx && \
      grep -c "border-border" src/ui/reposition/IntentEntry.tsx && \
      grep -c "rounded-none" src/ui/reposition/IntentEntry.tsx && \
      grep -c "text-sm" src/ui/reposition/IntentEntry.tsx && \
      grep -c "placeholder-muted-foreground" src/ui/reposition/IntentEntry.tsx && \
      [ $(grep -c "space-y-lg\|p-lg\|rounded-lg\|border-gray-200\|focus:border-accent\|text-body" src/ui/reposition/IntentEntry.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    IntentEntry.tsx migrated to host tokens. Textarea: bg-card border border-border rounded-none. All spacing and typography per Tailwind numeric scale. No broken tokens. Component structure unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate ScopeConfirmation modal to D-09 chrome</name>
  <files>src/ui/reposition/ScopeConfirmation.tsx</files>
  <action>
Migrate `src/ui/reposition/ScopeConfirmation.tsx` to host tokens and D-09 modal chrome pattern per UIRP-02:

1. **Current state:**
   - Likely uses `bg-black/30` backdrop (WRONG)
   - Likely uses `rounded-lg` panel (WRONG)
   - Likely uses custom spacing: `p-lg gap-md` (WRONG)

2. **Apply D-09 modal chrome pattern** (identical to Plan 2 modals):
   - **Backdrop:** `fixed inset-0 bg-background/80 backdrop-blur-sm`
   - **Panel:** `absolute ... bg-card border border-border rounded-none shadow-lg px-4 py-3`
   - **Gap:** `gap-3` for content spacing

3. **Structure (typical scope confirmation modal):**
   - Backdrop overlay (full screen)
   - Modal panel (centered or custom position)
   - Title (text-base font-semibold)
   - Scope confirmation content (text-sm body text, description of scope change)
   - Button group (flex row, gap-2, right-aligned)
   - Cancel/Adjust button: `px-3 py-2 text-xs font-medium bg-muted text-foreground rounded-none border border-border`
   - Confirm button: `px-3 py-2 text-xs font-medium bg-foreground text-background rounded-none` (D-13 primary)

4. **Token migration checklist:**
   - [ ] Backdrop: `fixed inset-0 bg-background/80 backdrop-blur-sm` (NOT `bg-black/30`)
   - [ ] Panel: `bg-card border border-border rounded-none shadow-lg px-4 py-3` (NOT `rounded-lg`)
   - [ ] Gap: `gap-3`
   - [ ] Title: `text-base font-semibold`
   - [ ] Body: `text-sm`
   - [ ] Buttons: `px-3 py-2 text-xs font-medium rounded-none`
   - [ ] Remove: `bg-black/30`, `rounded-lg`, `rounded-xl`, `p-lg`, `gap-md`

5. **Component structure:**
   - Modal props: isOpen, onClose, onConfirm, scope data
   - Renders nothing if !isOpen
   - Props interface unchanged

6. **No new imports needed.**

Reference: Phase 9 Plan 2 Task 2 (ActionConfirmationModal D-09 migration).
  </action>
  <verify>
    <automated>
      grep -c "bg-background/80" src/ui/reposition/ScopeConfirmation.tsx && \
      grep -c "backdrop-blur-sm" src/ui/reposition/ScopeConfirmation.tsx && \
      grep -c "bg-card" src/ui/reposition/ScopeConfirmation.tsx && \
      grep -c "border-border" src/ui/reposition/ScopeConfirmation.tsx && \
      grep -c "rounded-none" src/ui/reposition/ScopeConfirmation.tsx && \
      grep -c "shadow-lg" src/ui/reposition/ScopeConfirmation.tsx && \
      [ $(grep -c "bg-black/30\|rounded-lg\|rounded-xl\|p-lg\|gap-md" src/ui/reposition/ScopeConfirmation.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    ScopeConfirmation.tsx migrated to D-09 chrome pattern. Backdrop: bg-background/80 backdrop-blur-sm. Panel: bg-card border border-border rounded-none shadow-lg. All spacing and typography per Tailwind native scale. No broken tokens. Component structure unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 5: Migrate CascadeReviewPanel modal to D-09 chrome</name>
  <files>src/ui/reposition/CascadeReviewPanel.tsx</files>
  <action>
Migrate `src/ui/reposition/CascadeReviewPanel.tsx` to host tokens and D-09 modal chrome pattern per UIRP-03. This modal displays cascade impact review:

1. **Current state:**
   - Likely uses same broken patterns as ScopeConfirmation: `bg-black/30` backdrop, `rounded-lg` panel, custom spacing

2. **Apply D-09 modal chrome pattern** (identical to ScopeConfirmation and Plan 2 modals):
   - **Backdrop:** `fixed inset-0 bg-background/80 backdrop-blur-sm`
   - **Panel:** `absolute ... bg-card border border-border rounded-none shadow-lg px-4 py-3`
   - **Gap:** `gap-3` for content spacing

3. **Structure (typical cascade review modal):**
   - Backdrop overlay (full screen)
   - Modal panel (centered or custom position)
   - Title (text-base font-semibold)
   - Review content: list of cascading decisions/affected areas (text-sm body text)
   - Approval buttons (flex row, gap-2, right-aligned)
   - Adjust button: `px-3 py-2 text-xs font-medium bg-muted text-foreground rounded-none border border-border`
   - Approve button: `px-3 py-2 text-xs font-medium bg-foreground text-background rounded-none` (D-13 primary)

4. **Token migration checklist:**
   - [ ] Backdrop: `fixed inset-0 bg-background/80 backdrop-blur-sm`
   - [ ] Panel: `bg-card border border-border rounded-none shadow-lg px-4 py-3`
   - [ ] Gap: `gap-3`
   - [ ] Title: `text-base font-semibold`
   - [ ] Body: `text-sm`
   - [ ] List items (if present): `gap-2` between items
   - [ ] Buttons: `px-3 py-2 text-xs font-medium rounded-none`
   - [ ] Remove: `bg-black/30`, `rounded-lg`, `rounded-xl`, `p-lg`, `gap-md`

5. **Component structure:**
   - Modal props: isOpen, onClose, onApprove, cascadeData
   - Renders nothing if !isOpen
   - Props interface unchanged

6. **No new imports needed.**

Reference: Phase 9 Plan 2 Task 2 (ActionConfirmationModal D-09 migration) and Task 4 above.
  </action>
  <verify>
    <automated>
      grep -c "bg-background/80" src/ui/reposition/CascadeReviewPanel.tsx && \
      grep -c "backdrop-blur-sm" src/ui/reposition/CascadeReviewPanel.tsx && \
      grep -c "bg-card" src/ui/reposition/CascadeReviewPanel.tsx && \
      grep -c "border-border" src/ui/reposition/CascadeReviewPanel.tsx && \
      grep -c "rounded-none" src/ui/reposition/CascadeReviewPanel.tsx && \
      grep -c "shadow-lg" src/ui/reposition/CascadeReviewPanel.tsx && \
      [ $(grep -c "bg-black/30\|rounded-lg\|rounded-xl\|p-lg\|gap-md" src/ui/reposition/CascadeReviewPanel.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    CascadeReviewPanel.tsx migrated to D-09 chrome pattern. Backdrop: bg-background/80 backdrop-blur-sm. Panel: bg-card border border-border rounded-none shadow-lg. All spacing and typography per Tailwind native scale. No broken tokens. Component structure unchanged.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Checkpoint 1: React shim guard verification (NEW — Phase 9 requirement)</name>
  <what-built>
    All five Reposition components migrated to host tokens and integrated into RepositionPanel.
    Automated step: Run React shim guard to verify zero forbidden imports in all Phase 9 components.
  </what-built>
  <how-to-verify>
    Execute the React shim guard check:

    ```bash
    npm run lint:shim
    ```

    Expected output:
    ```
    ✓ src/ui/revive/ — 0 forbidden React imports
    ✓ src/ui/reposition/ — 0 forbidden React imports
    ✓ No useId, useReducer, useLayoutEffect, useImperativeHandle, useDebugValue, useDeferredValue, useTransition, useSyncExternalStore, useInsertionEffect, useActionState, useOptimistic, useFormStatus found in plugin components
    ```

    If any forbidden imports found:
    1. `grep -n "useId\|useReducer\|useLayoutEffect\|useImperativeHandle\|useDebugValue\|useDeferredValue\|useTransition\|useSyncExternalStore\|useInsertionEffect\|useActionState\|useOptimistic\|useFormStatus" src/ui/revive/*.tsx src/ui/reposition/*.tsx`
    2. Replace forbidden hook with alternative (e.g., useId → useRef + module counter as per HelpTip.tsx pattern)
    3. Re-run `npm run lint:shim`

    Why: Importing forbidden hooks causes `SyntaxError: ... is not a valid Hook` at module load time, breaks entire UI registration, falls back to host placeholder. This was a Phase 8 hotfix (commit 1788c02); Phase 9 must enforce it.

    Reference: 09-CONTEXT.md "React shim guard (NEW — required)" section 2.
  </how-to-verify>
  <resume-signal>Type "shim-verified" or describe any forbidden imports found</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Checkpoint 2: Static build verification + typecheck</name>
  <what-built>
    All Phase 9 components (Plans 1, 2, 3) migrated to host tokens.
    Automated step: Run build, typecheck, and test suite.
  </what-built>
  <how-to-verify>
    Execute static checks (no VPS/host required for this checkpoint):

    ```bash
    npm run typecheck && npm run test:run && npm run build
    ```

    Expected output:
    ```
    typecheck: 0 errors
    test:run: all tests passing
    build: [SUCCESS] — dist/worker.js, dist/manifest.js, dist/ui/index.js compiled
    ```

    If failures occur:
    1. Review error messages
    2. Fix issues (missing tokens, type mismatches, etc.)
    3. Re-run checks

    Grep verification (part of test suite, automated):
    ```bash
    grep -c "gap-xs\|gap-sm\|gap-md\|gap-lg\|px-sm\|px-md\|px-lg\|py-sm\|py-md\|text-heading\|text-body\|text-label\|rounded-lg\|rounded-xl\|bg-accent\|text-accent\|text-destructive" src/ui/revive/*.tsx src/ui/reposition/*.tsx
    ```

    Expected: 0 hits

    Reference: 09-CONTEXT.md "Verification Gate" section 1 (static checks).
  </how-to-verify>
  <resume-signal>Type "build-verified" or describe any build/typecheck/test failures</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Checkpoint 3: Live host mount smoke test (NEW — Phase 9 required, Phase 8 missed this)</name>
  <what-built>
    All Phase 9 components migrated and built. Ready for live mount test in Paperclip host.
    Automated step: Deploy plugin to VPS, mount each panel, verify no runtime errors.
  </what-built>
  <how-to-verify>
    **Setup (VPS):**

    1. SSH to paperclip-vps:
       ```bash
       ssh paperclip-vps
       cd /workspace/paperclip
       docker-compose ps  # verify docker-server-1 running on port 3100
       ```

    2. Build and deploy plugin (from plugin-compass repo):
       ```bash
       npm run build
       VERSION=$(jq -r .version package.json)
       # Copy dist/ to Paperclip plugin directory or use upgrade API
       ```

    3. Upgrade plugin in host (via admin API):
       ```bash
       curl -X POST http://localhost:3100/api/plugins/paperclip-plugin-compass/upgrade \
         -H "Authorization: Bearer $ADMIN_TOKEN" \
         -H "Content-Type: application/json" \
         -d "{\"version\": \"$VERSION\"}"
       # Sleep ~15s for npm propagation
       sleep 15
       ```

    **Mount Test (for each panel: RevivePanel, RepositionPanel):**

    4. Visit Paperclip host in browser (http://localhost:3100)
    5. Select test company: ALE (`086b697e-072a-42ca-80ee-f3712fa63704`) or BUS (`824f2949-2c77-48ff-92fb-fdf84f9c5c4a`)
    6. Open Compass plugin sidebar
    7. Navigate to Revive mode → RevivePanel should render (NOT host placeholder `<displayName>: <displayName>`)
    8. Navigate to Reposition mode → RepositionPanel should render (NOT host placeholder)
    9. Open browser DevTools (F12):
       - Console: Check for `[plugin-loader] Failed to load UI module` errors (MUST BE 0)
       - Check for `SyntaxError: ... is not a valid Hook` (MUST BE 0 — if seen, shim guard failed)
       - Screenshot both light and dark modes (toggle host theme)
    10. Visually compare light/dark modes: panels should look indistinguishable from host shell (no collapsed layouts, proper spacing, correct colors in both themes)

    **Expected Results:**
    - ✓ Panel content renders (not placeholder)
    - ✓ No console errors
    - ✓ Light + dark mode visual parity
    - ✓ PluginPageProps context prop received (companyId path works)
    - ✓ All buttons/inputs functional

    If failures occur:
    1. Check console errors and stack traces
    2. If `Failed to load UI module`: likely syntax error or forbidden import (re-run shim guard)
    3. If visual issues: token mismatch (re-run grep verification)
    4. Fix and redeploy

    Reference: 09-CONTEXT.md "Verification Gate" section 3 (live host mount smoke test) — NEW requirement; Phase 8 skipped this and missed PluginPageProps contract + useId shim crashes (post-ship hotfixes).
  </how-to-verify>
  <resume-signal>Type "host-mount-verified" with panel names (e.g., "RevivePanel, RepositionPanel both mounted + no errors") or describe any issues found</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Checkpoint 4: Plugin SDK payload audit (NEW — Phase 9 requirement)</name>
  <what-built>
    All Phase 9 components integrated. Automated step: Audit any new bridge handlers for sensitive data leaks.
  </what-built>
  <how-to-verify>
    Scan Phase 9 components for any `getX` handlers (getData, getReviveActions, getRepositionStatus, etc.):

    ```bash
    grep -n "ctx.agents\|ctx.issues\|ctx.documents\|ctx.goals" src/ui/revive/*.tsx src/ui/reposition/*.tsx
    ```

    Expected: Likely 0-2 hits (Reposition may call `ctx.documents.list()` for vision access).

    For each handler found:
    1. Verify it returns explicit allowlist of fields (NOT entire object)
    2. Check forbidden fields are NEVER included:
       - `adapterConfig.env` (LLM API keys, DATABASE_URL)
       - `assigneeAdapterOverrides`
       - `executionWorkspaceSettings`
    3. Reference sanitization pattern: `src/primitives/inventory.ts` (Phase 8 hotfix 0.3.25)

    If any sensitive data found:
    1. Create sanitization function (allowlist pattern from inventory.ts)
    2. Apply to handler return value
    3. Re-run grep

    Expected final result: 0 hits for sensitive fields

    Reference: 09-CONTEXT.md "Verification Gate" section 4 (plugin SDK payload audit) and commit cb9a6fa (fix: sanitize getInventory payload).
  </how-to-verify>
  <resume-signal>Type "sdk-audit-verified" or describe any sensitive fields found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin UI ↔ Host React tree | Plugin components render in host tree; forbidden hooks cause syntax errors at load |
| Plugin bridge handlers ↔ Host context | Bridge handlers (getX) receive ctx.agents/issues/documents; must sanitize before crossing |
| Modal state ↔ User action | Modal opens/closes based on parent state; selection passed via callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Spoofing | React hook imports | mitigate | React shim guard (npm run lint:shim) enforces zero forbidden imports; syntax error fails fast at load time |
| T-09-08 | Information Disclosure | Bridge handler payloads | mitigate | SDK payload audit verifies no adapterConfig.env, assigneeAdapterOverrides, executionWorkspaceSettings leak (per inventory.ts pattern) |
| T-09-09 | Denial of Service | Modal backdrop overlay | accept | Fixed inset-0 overlay is bounded; no unbounded rendering |
| T-09-10 | Tampering | Class name string values | mitigate | Static class strings in D-09 chrome pattern prevent runtime injection; full strings prevent Tailwind JIT collision |

</threat_model>

<verification>
Plan 3 completion verified when:
1. `npm run lint:shim` passes: 0 forbidden React imports in src/ui/revive/ and src/ui/reposition/
2. `npm run typecheck` passes (all five files)
3. `npm run test:run` passes (all tests including Phase 9 new tests)
4. `npm run build` succeeds: dist/{worker.js,manifest.js,ui/index.js} compiled
5. Grep audit: 0 hits for broken tokens in src/ui/reposition/
6. **Live host mount smoke test (new):** RevivePanel and RepositionPanel render in VPS Paperclip host without console errors, both light + dark modes visually correct
7. **SDK payload audit (new):** 0 sensitive fields (adapterConfig.env, etc.) found in any bridge handlers
8. All five components use host tokens exclusively (bg-background, bg-card, border-border, text-*, gap-*, px-*, py-*, rounded-none, bg-background/80 backdrop-blur-sm)
</verification>

<success_criteria>
- [x] RepositionPanel: bg-background, flex flex-col, numeric gap/padding only
- [x] RepositionInterviewFlow: wrapper uses host tokens; reuses Phase 8 Found interview components
- [x] IntentEntry: bg-card border-border rounded-none, textarea styling with host tokens
- [x] ScopeConfirmation: D-09 chrome (backdrop: bg-background/80 backdrop-blur-sm; panel: bg-card border border-border rounded-none shadow-lg)
- [x] CascadeReviewPanel: D-09 chrome (identical to ScopeConfirmation)
- [x] Zero broken custom tokens in all five files
- [x] React shim guard passes: 0 forbidden imports
- [x] Build passes, typecheck passes, tests pass
- [x] Live host mount smoke test passes: both panels render without errors, light/dark parity
- [x] SDK payload audit passes: 0 sensitive fields leak
- [x] Modal chrome consistency confirmed across all modes (Assess, Found, Revive, Reposition all use D-09 pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/09-revive-reposition-panels/09-03-SUMMARY.md` with:
- 5 components migrated
- 3 verification checkpoints completed (React shim guard, static build, live host mount smoke test, SDK payload audit)
- D-09 modal chrome pattern verified consistent across all 4 mode panels
- All tokens verified per host baseline + new verification gates
- Ready for Phase 10 (Memory panel + comprehensive verification + documentation)
</output>
