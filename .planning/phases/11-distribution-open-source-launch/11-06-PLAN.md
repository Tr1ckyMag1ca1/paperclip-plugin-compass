---
phase: 11-distribution-open-source-launch
plan: 06
type: execute
wave: 7
depends_on: [11-05]
files_modified: []
autonomous: false
requirements:
  - DIST-02
  - DIST-03
user_setup:
  - service: Paperclip VPS
    why: "Install verification on live plugin-manager instance"
    env_vars: []
    dashboard_config:
      - task: "Access Paperclip VPS via SSH (Tailscale 100.79.31.30:3100)"
        location: "Paperclip VPS, container docker-server-1, Compass plugin in sidebar"

must_haves:
  truths:
    - "npm publish succeeds for v1.1.0 under @paperclipai/paperclip-plugin-compass scope"
    - "GitHub release v1.1.0 created with .tgz artifact and CHANGELOG-derived release notes"
    - "Plugin installs via plugin-manager UI on VPS Paperclip instance (Tailscale 100.79.31.30:3100)"
    - "All 5 mode panels render without console errors (Found, Assess, Revive, Reposition, History)"
    - "Screenshots captured for all 5 modes in light and dark themes"
    - "Evidence committed to .planning/phases/11-distribution-open-source-launch/evidence/ with 11-VERIFICATION.md summary"
  artifacts:
    - path: .planning/phases/11-distribution-open-source-launch/11-VERIFICATION.md
      provides: "VPS install verification report with evidence"
      must_contain:
        - "v1.1.0"
        - "5 mode panels"
        - "zero console errors"
        - "screenshots paths"
    - path: .planning/phases/11-distribution-open-source-launch/evidence/
      provides: "Screenshot evidence of all 5 modes"
      must_contain:
        - "mode-found.png or similar (Found mode)"
        - "mode-assess.png or similar (Assess mode)"
        - "mode-revive.png or similar (Revive mode)"
        - "mode-reposition.png or similar (Reposition mode)"
        - "mode-history.png or similar (History mode)"
  key_links:
    - from: "git tag v1.1.0"
      to: ".github/workflows/release.yml"
      via: "Tag push triggers GitHub Actions release workflow"
      pattern: "on: push: tags: v*.*.*"
    - from: "npm publish"
      to: "plugin-manager registry"
      via: "npm registry serves plugin to Paperclip instances"
      pattern: "@paperclipai/paperclip-plugin-compass"
    - from: "VPS install"
      to: "evidence/ screenshots"
      via: "Visual verification of mode panels in light + dark themes"
      must_verify: "Zero console errors per mode"
---

<objective>
Execute npm publish via git tag push (triggers Wave 5 CI workflow), verify GitHub release creation, install plugin on live Paperclip VPS, and capture visual evidence (screenshots) of all 5 mode panels rendering correctly. Per D-06, verification scope = render all 5 modes without console errors; not full per-mode UAT walkthrough.

Purpose: Ship v1.1.0 to npm registry and confirm real-world installation path works end-to-end. This is the public launch; afterwards, plugin is live on npm and accessible to all users via plugin-manager.

Output: npm package published, GitHub release created with .tgz artifact, VPS install verified with screenshot evidence committed to phase folder, 11-VERIFICATION.md summary written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-distribution-open-source-launch/11-CONTEXT.md (D-06, D-07: VPS verify, evidence capture)
@.planning/phases/11-distribution-open-source-launch/11-RESEARCH.md (VPS access, plugin-manager install flow)
@.github/workflows/release.yml (tag-triggered publish workflow from Wave 5)
@package.json (version 1.1.0, scoped name)
@CHANGELOG.md (source for release notes)
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Push git tag v1.1.0 to trigger automated npm publish</name>
  <what-built>
Wave 5 CI workflows (.github/workflows/release.yml and ci.yml) are ready. Wave 6 verification passed. Ready to publish v1.1.0.
  </what-built>
  <how-to-verify>
1. In the repo root, ensure you are on main branch and all commits are pushed: `git status` (clean) and `git log -1` (latest commit)
2. Verify NPM_TOKEN secret is provisioned in GitHub repo Secrets (Settings > Secrets and variables > Actions > NPM_TOKEN visible)
3. Push the tag: `git tag v1.1.0 && git push origin v1.1.0`
4. Monitor GitHub Actions: go to repo Actions tab, watch the "Release & Publish" workflow for v1.1.0 tag
5. Workflow should complete in ~3-5 minutes with:
   - verify stage passes (npm run verify succeeds)
   - build stage passes (npm run build succeeds)
   - publish stage: "npm publish --access public" outputs "npm notice + deps published"
   - release stage: "gh release create" outputs GitHub release URL
6. On npm registry, verify package exists: `npm view @paperclipai/paperclip-plugin-compass@1.1.0` (or visit https://www.npmjs.com/package/@paperclipai/paperclip-plugin-compass)
7. On GitHub, verify release exists: repo Releases page shows "v1.1.0" with .tgz artifact and CHANGELOG-derived notes

If any stage fails, the tag remains in git but npm publish is blocked (workflow fails). Do NOT retry with the same tag; instead, fix the issue locally, delete the tag (`git tag -d v1.1.0 && git push origin :v1.1.0`), and retry after fix.
  </how-to-verify>
  <resume-signal>Type "published" once npm publish completes successfully and GitHub release is visible, or describe any issues encountered.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify npm package is available and plugin-manager can fetch it</name>
  <files></files>
  <action>
After npm publish succeeds (Task 1), validate the npm package is correctly published:

1. Verify npm registry has the package:
   ```bash
   npm view @paperclipai/paperclip-plugin-compass@1.1.0 version
   ```
   Expected output: `1.1.0`

2. Check package.json metadata in registry:
   ```bash
   npm view @paperclipai/paperclip-plugin-compass@1.1.0 name,version,license
   ```
   Expected: `@paperclipai/paperclip-plugin-compass`, `1.1.0`, `MIT`

3. Verify scoped package is public (not private):
   ```bash
   npm view @paperclipai/paperclip-plugin-compass@1.1.0 private
   ```
   Expected output: `undefined` or `false` (public)

4. Verify .tgz artifact is downloadable:
   ```bash
   npm pack @paperclipai/paperclip-plugin-compass@1.1.0 --dry-run
   ```
   Should show tarball size and contents list.

All four checks must pass before proceeding to Task 3 VPS install. If any check fails, investigate (e.g., authentication issue, registry lag) and retry.
  </action>
  <verify>
    <automated>npm view @paperclipai/paperclip-plugin-compass@1.1.0 version</automated>
  </verify>
  <done>npm package published and verified on npm registry. Plugin-manager can fetch @paperclipai/paperclip-plugin-compass@1.1.0.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Install plugin on live Paperclip VPS and capture mode panel screenshots</name>
  <what-built>
v1.1.0 published to npm registry. Plugin-manager can now discover and install the plugin.
  </what-built>
  <how-to-verify>
**Step 1: Access VPS Paperclip instance**
- SSH to Paperclip VPS: Tailscale `100.79.31.30:3100`, container `docker-server-1` (from auto-memory paperclip_vps_access.md)
- Open browser to VPS Paperclip URL (check VPS documentation for exact URL; typically http://localhost:3100 if local or Tailscale address)

**Step 2: Install Compass plugin**
- Log in to Paperclip
- Navigate to Administration > Plugins (or similar plugin management UI)
- Search for "Compass" or "@paperclipai/paperclip-plugin-compass"
- Click "Install" and confirm (plugin-manager will fetch v1.1.0 from npm registry)
- Wait for install to complete (~10-30 seconds)
- Verify plugin appears in sidebar under "Plugins" section

**Step 3: Open a test company and render all 5 mode panels**
- Select an existing test company (or create a new one if needed)
- Click "Compass" plugin in sidebar (should open Found/Assess/Revive/Reposition/History mode selector)
- For each of the 5 modes:
  1. **Found mode:** Click "Found" dropdown; should show vision-quest interview form (if company has no VISION.md) or preview if VISION exists
  2. **Assess mode:** Click "Assess" dropdown; should show drift audit summary or "Run drift audit" button
  3. **Revive mode:** Click "Revive" dropdown; should show stall summary or "Find what's blocking" button
  4. **Reposition mode:** Click "Reposition" dropdown; should show pivot interview form or "Start pivot strategy" button
  5. **History mode:** Click "History" dropdown; should show timeline of findings and scheduled check-ins

**Step 4: Capture screenshots and verify zero console errors**
- For each mode in step 3:
  - Open browser DevTools (F12 or right-click > Inspect > Console)
  - Check Console tab: should show zero errors (warnings OK)
  - Take a screenshot of the mode panel with Console visible (showing "0 errors")
- Repeat for both light and dark themes (if Paperclip host has theme toggle):
  - Toggle theme in Paperclip host settings
  - Re-open each mode panel and capture screenshot in dark theme

**Step 5: Document evidence**
- Save screenshots as PNG files with clear names:
  - `evidence/mode-found-light.png`, `evidence/mode-found-dark.png`
  - `evidence/mode-assess-light.png`, `evidence/mode-assess-dark.png`
  - `evidence/mode-revive-light.png`, `evidence/mode-revive-dark.png`
  - `evidence/mode-reposition-light.png`, `evidence/mode-reposition-dark.png`
  - `evidence/mode-history-light.png`, `evidence/mode-history-dark.png`
- (Minimum: one screenshot per mode; ideal: light + dark for parity verification per Phase 7-10 work)

**Step 6: Verify plugin upgrade path (if v0.3.x already installed)**
- If plugin-manager shows "Upgrade available," click upgrade
- Confirm plugin restarts cleanly and all 5 modes still render (using steps above)
- If fresh install (no prior version), skip this step

If any mode fails to render or console shows errors, do NOT proceed to verification summary. Debug the issue, fix locally, re-tag (if code fix needed), and retry install.
  </how-to-verify>
  <resume-signal>Type "verified" once all 5 modes render without console errors in light + dark themes and screenshots are saved to evidence/ folder, or describe any issues encountered.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Commit evidence to phase folder and write 11-VERIFICATION.md summary</name>
  <files>
    .planning/phases/11-distribution-open-source-launch/11-VERIFICATION.md
    .planning/phases/11-distribution-open-source-launch/evidence/*
  </files>
  <action>
1. Create `.planning/phases/11-distribution-open-source-launch/evidence/` folder (if it doesn't exist):
   ```bash
   mkdir -p .planning/phases/11-distribution-open-source-launch/evidence
   ```

2. Copy all screenshots from Task 3 into the `evidence/` folder:
   ```bash
   cp /path/to/screenshots/* .planning/phases/11-distribution-open-source-launch/evidence/
   ```

3. Create `.planning/phases/11-distribution-open-source-launch/11-VERIFICATION.md` with the following structure:

```markdown
# Phase 11: Distribution + Open-Source Launch - Verification Report

**Date:** 2026-05-11 (update with actual date)
**Phase:** 11-distribution-open-source-launch
**Version:** v1.1.0

## Summary

Compass plugin v1.1.0 published to npm under scoped package name `@paperclipai/paperclip-plugin-compass` and verified on live Paperclip VPS instance (Tailscale 100.79.31.30:3100, container docker-server-1). All 5 mode panels (Found, Assess, Revive, Reposition, History) render without console errors in light and dark themes.

**Status:** PASS — Ready for public availability.

## Evidence

### npm Publish
- Package: `@paperclipai/paperclip-plugin-compass@1.1.0`
- Registry: https://www.npmjs.com/package/@paperclipai/paperclip-plugin-compass
- Publish timestamp: [git tag v1.1.0 date — obtained from `git log -1 --format=%cd v1.1.0`]
- GitHub Release: https://github.com/Tr1ckyMag1ca1/paperclip-plugin-compass/releases/tag/v1.1.0
- Artifact: paperclip-plugin-compass-1.1.0.tgz (attached to release)

### VPS Install Verification
- Host: Paperclip VPS (Tailscale 100.79.31.30:3100, docker-server-1)
- Install method: Plugin-manager UI (Administration > Plugins > Search "Compass" > Install)
- Install status: SUCCESS
- Upgrade path: [If v0.3.x existed prior, note whether upgrade or fresh install]

### Mode Panel Verification

| Mode | Light Theme | Dark Theme | Console Errors | Status |
|------|-------------|------------|----------------|--------|
| Found | ✓ [evidence/mode-found-light.png] | ✓ [evidence/mode-found-dark.png] | 0 | PASS |
| Assess | ✓ [evidence/mode-assess-light.png] | ✓ [evidence/mode-assess-dark.png] | 0 | PASS |
| Revive | ✓ [evidence/mode-revive-light.png] | ✓ [evidence/mode-revive-dark.png] | 0 | PASS |
| Reposition | ✓ [evidence/mode-reposition-light.png] | ✓ [evidence/mode-reposition-dark.png] | 0 | PASS |
| History | ✓ [evidence/mode-history-light.png] | ✓ [evidence/mode-history-dark.png] | 0 | PASS |

### Screenshots
All mode panel screenshots committed to `evidence/` folder:
- Light theme: `evidence/mode-{found|assess|revive|reposition|history}-light.png`
- Dark theme: `evidence/mode-{found|assess|revive|reposition|history}-dark.png`

All images show plugin sidebar integration, mode panel rendering, and browser console (F12) with zero errors.

## Verification Checklist (DIST-02)

- [x] Plugin installs via plugin-manager UI on VPS (Administration > Plugins > Search > Install)
- [x] Plugin registers and appears in sidebar
- [x] All 5 mode panels render without runtime errors
- [x] Console tab shows zero errors for each mode (warnings acceptable)
- [x] Light theme rendering correct
- [x] Dark theme rendering correct
- [x] Evidence (screenshots) captured for all modes
- [x] Evidence committed to phase folder under `evidence/` subdir

## Requirements Traceability

- **DIST-02:** Plugin-manager install verified on live Paperclip VPS host — all 5 mode panels render without errors, end-to-end workflow confirmed ✓
- **DIST-03:** GitHub release v1.1.0 with .tgz artifact and CHANGELOG-derived release notes ✓

## Conclusion

Compass v1.1.0 is production-ready and publicly available. Phase 11 distribution + OSS launch is COMPLETE.

---

**Next steps:** Notify Aron Prins of public launch (Wave 8, separate from this phase).
```

4. Git add and commit evidence + verification report:
   ```bash
   cd .planning/phases/11-distribution-open-source-launch/
   git add evidence/ 11-VERIFICATION.md
   git commit -m "docs(11): add VPS install verification evidence and report (v1.1.0 PASS)"
   ```

5. Verify commit succeeded:
   ```bash
   git log --oneline | head -1
   ```
  </action>
  <verify>
    <automated>test -f .planning/phases/11-distribution-open-source-launch/11-VERIFICATION.md && grep -c "v1.1.0\|PASS" .planning/phases/11-distribution-open-source-launch/11-VERIFICATION.md</automated>
  </verify>
  <done>11-VERIFICATION.md written with evidence summary, screenshots committed to evidence/ folder, verification report completed and committed to git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry | Published package is publicly available; anyone can install via plugin-manager |
| VPS Paperclip instance | Real-world host environment where plugin renders in user's session |
| Evidence (screenshots) | Visual proof that plugin renders correctly; auditable version control record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-19 | Spoofing | Attacker publishes malicious @paperclipai/paperclip-plugin-compass to npm using compromised token | mitigate | NPM_TOKEN provisioned as repo Secret (GitHub-managed, not in code); token scoped to publish only via CI workflow |
| T-11-20 | Tampering | npm package registry is modified post-publish (unlikely but possible with account compromise) | accept | npm registry maintains full audit trail; can deprecate + rollback via `npm deprecate` if needed (D-08) |
| T-11-21 | Repudiation | VPS install "verified" but actually failed; evidence faked | mitigate | Evidence (screenshots) committed to git; console log visible in screenshots (zero errors verifiable); git history is immutable |
| T-11-22 | Disclosure | Screenshots contain sensitive company data in test instance | mitigate | Use clean test company with synthetic data on VPS (no real PII); redact if needed before committing |
| T-11-23 | Denial | Plugin fails post-install due to regression not caught in Wave 6 | mitigate | Wave 6 full verify + npm pack validation; VPS install is real-world test before "launch complete" signal |
</threat_model>

<verification>
After all 4 tasks complete:
1. Confirm git tag v1.1.0 pushed and GitHub Actions release workflow succeeded (all stages green)
2. Confirm npm package is live: `npm view @paperclipai/paperclip-plugin-compass@1.1.0 version` returns 1.1.0
3. Confirm GitHub release exists with .tgz artifact
4. Confirm 11-VERIFICATION.md exists with all 5 mode panel verification checkmarks
5. Confirm evidence/ folder has screenshots for all 5 modes (light + dark)
6. Confirm git commit for verification report succeeded

Phase 11 is complete and ready for Wave 8 Aron onboarding notification (not part of this phase; separate task).
</verification>

<success_criteria>
- npm publish succeeds for @paperclipai/paperclip-plugin-compass@1.1.0 via git tag trigger
- GitHub release v1.1.0 created with .tgz artifact and CHANGELOG-derived release notes
- Plugin installs via plugin-manager UI on VPS without errors
- All 5 mode panels render in light and dark themes with zero console errors
- Screenshots captured for all modes and committed to evidence/ folder
- 11-VERIFICATION.md written with comprehensive evidence summary and checklist
- Phase 11 distribution + OSS launch is COMPLETE
</success_criteria>

<output>
After completion, create `.planning/phases/11-distribution-open-source-launch/11-06-SUMMARY.md`
</output>
