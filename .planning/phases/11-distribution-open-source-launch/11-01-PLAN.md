---
phase: 11-distribution-open-source-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - DIST-01
user_setup: []

must_haves:
  truths:
    - "package.json#name is @paperclipai/paperclip-plugin-compass (scoped)"
    - "package.json#version is 1.1.0"
    - "package.json#license matches LICENSE file (MIT)"
    - "package.json#files includes dist/, package.json, README.md, LICENSE"
    - "Stale self-reference dep removed: paperclip-plugin-compass 0.2.0 not in dependencies"
    - "peerDependencies declares react >=18 and react-dom >=18"
  artifacts:
    - path: package.json
      provides: "npm package metadata for scoped distribution"
      must_contain:
        - "name: @paperclipai/paperclip-plugin-compass"
        - "version: 1.1.0"
        - "license: MIT"
  key_links:
    - from: "package.json#paperclipPlugin"
      to: "dist/manifest.js, dist/worker.js, dist/ui"
      via: "plugin-manager reads manifest path from metadata"
      must_verify: "npm pack" succeeds
---

<objective>
Rename package to scoped `@paperclipai/paperclip-plugin-compass`, bump version to 1.1.0, clean up stale dependencies, and verify npm metadata is correct for public distribution.

Purpose: Establish the canonical package identity and prepare for npm publish in Wave 7. Scoped name prevents conflicts on npm registry; version 1.1.0 aligns with v1.1 milestone.

Output: Updated package.json ready for distribution; all dependency audits pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-distribution-open-source-launch/11-CONTEXT.md
@package.json (current state: version 0.3.26, unscoped name)
@.github/ISSUE_TEMPLATE (existing structure to preserve)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package to scoped identity, bump version, clean dependencies</name>
  <files>package.json</files>
  <action>
1. Update `package.json#name` from `paperclip-plugin-compass` to `@paperclipai/paperclip-plugin-compass` (per D-01).
2. Update `package.json#version` from `0.3.26` to `1.1.0` (per D-02).
3. Verify `package.json#license` is `MIT` (matches LICENSE file at repo root).
4. Remove stale dependency: delete `"paperclip-plugin-compass": "0.2.0"` from `dependencies` (per D-12).
5. Verify `dependencies` only contains `@paperclipai/plugin-sdk` and `lucide-react` (no self-ref).
6. Verify `peerDependencies` declares `react >=18` and `react-dom >=18`.
7. Verify `devDependencies` contains `esbuild`, `typescript`, `vitest`, and `@types/*`.
8. Verify `paperclipPlugin` field points to `./dist/manifest.js`, `./dist/worker.js`, `./dist/ui` (paths unchanged).
9. Verify `files` array includes `dist/`, `package.json`, `README.md`, `LICENSE` (no src/, no node_modules).
10. Run `npm pack --dry-run` to validate structure (confirms manifest.js path exists post-build).
  </action>
  <verify>
    <automated>npm pack --dry-run 2>&1 | grep -E "(package.json|dist/manifest.js|dist/worker.js)" | wc -l</automated>
  </verify>
  <done>package.json updated: scoped name, version 1.1.0, stale dep removed, all metadata fields valid. npm pack --dry-run succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry | Published package metadata read by plugin-manager install flow |
| package.json metadata | Name, version, license, paperclipPlugin paths must be accurate or plugin fails to register |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | package.json metadata corruption | accept | npm publish validates structure; deploy artifact audited by Wave 6 dry-run before Wave 7 publish |
| T-11-02 | Disclosure | Stale self-reference dep leaks old version | mitigate | Remove paperclip-plugin-compass 0.2.0 from dependencies per D-12; verify via grep in Task 1 |
| T-11-03 | Denial | Incorrect paperclipPlugin paths block plugin-manager install | mitigate | npm pack dry-run validates path existence in Wave 6 before actual publish |
</threat_model>

<verification>
Before proceeding to Wave 2, verify:
- `npm pack --dry-run` output includes manifest.js, worker.js, UI bundle, package.json, README.md, LICENSE
- `grep -c "paperclip-plugin-compass.*0.2.0" package.json` returns 0 (no stale self-ref)
- `jq .peerDependencies package.json` shows `react >=18` and `react-dom >=18`
</verification>

<success_criteria>
- package.json#name = `@paperclipai/paperclip-plugin-compass`
- package.json#version = `1.1.0`
- package.json#license = `MIT`
- Stale dependency `paperclip-plugin-compass 0.2.0` removed
- paperclipPlugin paths point to correct dist/ subdirectories
- npm pack --dry-run succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/11-distribution-open-source-launch/11-01-SUMMARY.md`
</output>
