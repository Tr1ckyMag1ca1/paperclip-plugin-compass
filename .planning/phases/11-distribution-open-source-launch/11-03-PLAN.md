---
phase: 11-distribution-open-source-launch
plan: 03
type: execute
wave: 3
depends_on: [11-01]
files_modified:
  - README.md
  - CHANGELOG.md
  - .github/ISSUE_TEMPLATE/bug-report.yml
  - .github/ISSUE_TEMPLATE/feature.yml
  - .github/ISSUE_TEMPLATE/question.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - .github/PULL_REQUEST_TEMPLATE.md
  - CODEOWNERS
autonomous: true
requirements:
  - OSS-01
  - OSS-02
  - OSS-03
  - OSS-04
  - OSS-05
  - OSS-06
user_setup: []

must_haves:
  truths:
    - "README.md includes Aron Prins co-maintainer credit and paperclip-vision lineage link"
    - "README.md has scoped npm install snippet (@paperclipai/paperclip-plugin-compass)"
    - "README.md has badges (npm version, license)"
    - "README.md includes 5 mode panel screenshots (one per mode: Found, Assess, Revive, Reposition, History)"
    - "CHANGELOG.md follows keepachangelog format with v1.0 and v1.1 entries"
    - "CHANGELOG.md v1.0 entry aggregates 6 phases (modes, memory, check-ins)"
    - "CHANGELOG.md v1.1 entry aggregates phases 7-11 (UI parity + distribution)"
    - ".github issue templates (bug, feature, question) in YAML form format"
    - ".github/ISSUE_TEMPLATE/config.yml disables blank issues"
    - ".github/PULL_REQUEST_TEMPLATE.md has contributor checklist"
    - "CODEOWNERS lists @nicholasrhodes @aronprins globally"
    - "LICENSE exists at repo root and matches package.json#license"
  artifacts:
    - path: README.md
      provides: "Package documentation, install instructions, mode overview"
      must_contain:
        - "@paperclipai/paperclip-plugin-compass"
        - "Aron Prins"
        - "paperclip-vision"
        - "npm install"
    - path: CHANGELOG.md
      provides: "Release history in keepachangelog format"
      must_contain:
        - "## [1.0.0]"
        - "## [1.1.0]"
        - "Added"
    - path: .github/ISSUE_TEMPLATE/bug-report.yml
      provides: "Bug report form template"
      must_contain:
        - "title:"
        - "body:"
    - path: .github/ISSUE_TEMPLATE/feature.yml
      provides: "Feature request form template"
      must_contain:
        - "title:"
        - "body:"
    - path: .github/ISSUE_TEMPLATE/question.yml
      provides: "Question/support form template"
      must_contain:
        - "title:"
        - "body:"
    - path: .github/ISSUE_TEMPLATE/config.yml
      provides: "GitHub issue form configuration"
      must_contain:
        - "blank_issues_enabled: false"
    - path: .github/PULL_REQUEST_TEMPLATE.md
      provides: "Pull request checklist"
      must_contain:
        - "[]"
        - "Test"
    - path: CODEOWNERS
      provides: "Code ownership declaration"
      must_contain:
        - "@nicholasrhodes"
        - "@aronprins"
  key_links:
    - from: "README.md"
      to: "CHANGELOG.md"
      via: "README references latest version from CHANGELOG"
      pattern: "1.1.0"
    - from: ".github/ISSUE_TEMPLATE/*"
      to: ".github/ISSUE_TEMPLATE/config.yml"
      via: "config disables blank issues and links templates"
      pattern: "blank_issues_enabled"
    - from: "CODEOWNERS"
      to: "GitHub collaborator list"
      via: "maintainer handles listed for approval gating"
      must_verify: "Aron @aronprins added as collaborator before Wave 7"
---

<objective>
Polish README with Aron Prins co-maintainer credit and install instructions, initialize CHANGELOG in keepachangelog format, create GitHub issue/PR templates, verify CODEOWNERS and LICENSE are correct.

Purpose: Establish OSS project surface — clear installation path for users, contributor guidance, release history, and governance clarity. Per D-13 (README light polish), preserve existing structure and copy; add only scoped install snippet, Aron credit, and lineage link. Per D-14, CHANGELOG aggregates phase-level work, not per-requirement details.

Output: Updated README with scoped install + screenshots + credits, CHANGELOG with v1.0 + v1.1 entries, GitHub issue/PR templates in modern form format, CODEOWNERS verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-distribution-open-source-launch/11-CONTEXT.md (D-13, D-14, D-15, D-16)
@.planning/phases/11-distribution-open-source-launch/11-RESEARCH.md (keepachangelog format, GitHub forms YAML syntax)
@.planning/ROADMAP.md (Phase 11 success criteria, phase-level aggregation guidance)
@README.md (current structure to preserve)
@.github/PULL_REQUEST_TEMPLATE.md (current template)
@CODEOWNERS (current list)
@LICENSE (existing MIT text)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish README.md with Aron credit, scoped install snippet, badges, screenshots</name>
  <files>README.md</files>
  <action>
Update README.md per D-13 (light polish, preserve structure):

1. **Installation section:** Replace or clarify npm install snippet to use scoped package name:
   ```bash
   npm install @paperclipai/paperclip-plugin-compass
   ```
   Add note: "Or install via Paperclip plugin-manager UI (Administration > Plugins > Marketplace) and search 'Compass'."

2. **Add section (before or after Table of Contents):** "## Credits" or footnote at end:
   ```
   Co-maintained with [Aron Prins](https://github.com/aronprins). This plugin builds on strategic interview depth from [paperclip-vision](https://github.com/aronprins/paperclip-vision).
   ```

3. **Add badge line** (below title, before Table of Contents):
   ```markdown
   [![npm version](https://img.shields.io/npm/v/@paperclipai/paperclip-plugin-compass)](https://www.npmjs.com/package/@paperclipai/paperclip-plugin-compass)
   [![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
   ```

4. **Add screenshots:** If not already present, add 5 PNG screenshots (one per mode) to `evidence/` subdir and link from README "The Four Modes" table:
   - `evidence/mode-found.png` — Found mode panel (vision quest interview)
   - `evidence/mode-assess.png` — Assess mode panel (drift report)
   - `evidence/mode-revive.png` — Revive mode panel (stall actions)
   - `evidence/mode-reposition.png` — Reposition mode panel (pivot interview)
   - `evidence/mode-history.png` — History mode panel (memory + schedules)
   
   Add under each mode row: `[screenshot](evidence/mode-{mode-name}.png)` or similar markdown link.

5. Preserve existing structure: Table of Contents, The Four Modes table, Quick Start, How to Use Compass, Installation, Development, Contributing sections unchanged except for the above additions.

After editing, verify README renders in GitHub (simulate by reading back and checking markdown structure).
  </action>
  <verify>
    <automated>grep -c "@paperclipai/paperclip-plugin-compass\|Aron Prins\|paperclip-vision" README.md</automated>
  </verify>
  <done>README.md updated with scoped install snippet, Aron credit, lineage link, and badges. Screenshots referenced (actual PNG uploads handled separately in Wave 7).</done>
</task>

<task type="auto">
  <name>Task 2: Initialize CHANGELOG.md in keepachangelog format with v1.0 and v1.1 entries</name>
  <files>CHANGELOG.md</files>
  <action>
Create CHANGELOG.md at repo root following keepachangelog.com format (per D-14). Structure:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.1.0] - 2026-05-11

### Added

- Phase 7: Foundations + Shared Primitives — Card and SectionHeader components, MainPanel and SidebarLink host token migration, StatusBadge and ModeBanner semantic colors.
- Phase 8: Assess + Found Panels — AssessPanel, DriftReportPanel, EvidenceChip, ConfidenceBar, AmendmentDiff, ApprovalRoutingModal, FoundPanel, vision-quest interview flow all migrated to host tokens.
- Phase 9: Revive + Reposition Panels — RevivePanel, ActionItemCard, ActionQueuePanel, SamplePivotModal, RepositionInterviewFlow, IntentEntry, ScopeConfirmation, CascadeReviewPanel all migrated; React shim guard and live host mount smoke test verification gates.
- Phase 10: Memory + Verification + Documentation — HistoryPanel, FindingCard, SchedulesSection, grep verifier, manual verification checklist, production build verification, bundle size verification.
- Phase 11: Distribution + Open-Source Launch — npm scoped package distribution, plugin-manager VPS install verification, size-limit and externals audits, OSS documentation (README, CHANGELOG, CODEOWNERS, issue/PR templates).

### Changed

- Complete UI reskin to host design tokens (Tailwind v4 CSS vars, OKLCH color format, dark-mode support).

## [1.0.0] - 2026-04-30

### Added

- Phase 1: Foundations — Plugin scaffold, mode detection, main layout, sidebar.
- Phase 2: Found Mode — Vision-quest interview engine, VISION.md generation, agent provisioning, kickoff issue filing.
- Phase 3: Assess Mode — Drift audit engine, 30-day activity comparison, amendment proposal + approval flow.
- Phase 4: Revive Mode — Stall classification, blocker action queue, unblocking suggestions.
- Phase 5: Reposition Mode — Scoped vision-quest re-interview, amendment diff, cascade rebriefing plan.
- Phase 6: Memory + Scheduled Check-ins — Engagement memory (findings history, context refresh), scheduled drift audits with configurable interval.

### Fixed

- Initial release; no prior versions.

---

## Semantic Versioning

- **1.x.y**: UI parity and distribution milestones (v1.1 current, v1.2+ planned for additional features).
- **0.x.y**: Internal pre-release versions (0.3.26 was final pre-public version).
```

Key points:
- Phase-level aggregation: each phase is one "Added" bullet with sub-components listed.
- Semantic version aligns with milestone naming (v1.0 = initial public release, v1.1 = UI parity + distribution).
- Format is cleanly parseable by GitHub release automation and release-please.
- Keep section minimal; full details live in ROADMAP.md and phase summaries.

After creating, verify markdown syntax by reading back.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -c "## \[1.1.0\]\|## \[1.0.0\]" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md created with v1.0 and v1.1 entries in keepachangelog format, phase-level aggregation per D-14.</done>
</task>

<task type="auto">
  <name>Task 3: Create GitHub issue form templates (bug, feature, question, config) and PR template</name>
  <files>
    .github/ISSUE_TEMPLATE/bug-report.yml
    .github/ISSUE_TEMPLATE/feature.yml
    .github/ISSUE_TEMPLATE/question.yml
    .github/ISSUE_TEMPLATE/config.yml
    .github/PULL_REQUEST_TEMPLATE.md
  </files>
  <action>
Create or update GitHub issue and PR templates per D-15. All YAML issue templates use modern GitHub form syntax.

**1. .github/ISSUE_TEMPLATE/bug-report.yml:**
```yaml
name: Bug Report
description: Report a problem with Compass
title: "[Bug] "
labels: ["bug"]
assignees:
  - nicholasrhodes
body:
  - type: markdown
    attributes:
      value: "Thanks for reporting! Please provide as much detail as you can."
  - type: textarea
    id: description
    attributes:
      label: "Description"
      description: "What is the problem?"
      placeholder: "When I click 'Run drift audit' in Assess mode, the panel crashes with..."
    validations:
      required: true
  - type: textarea
    id: repro
    attributes:
      label: "Steps to Reproduce"
      placeholder: |
        1. Open a company in Paperclip
        2. Click Compass in sidebar
        3. Click "Run drift audit" button
        4. Observe: [error]
    validations:
      required: true
  - type: input
    id: compass-version
    attributes:
      label: "Compass Version"
      placeholder: "1.1.0"
    validations:
      required: true
  - type: input
    id: paperclip-version
    attributes:
      label: "Paperclip Host Version"
      placeholder: "1.0.0"
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: "Console Errors"
      description: "Paste any browser console errors (F12 > Console tab)"
      render: "javascript"
```

**2. .github/ISSUE_TEMPLATE/feature.yml:**
```yaml
name: Feature Request
description: Suggest a new feature or improvement
title: "[Feature] "
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: "Help us improve Compass with your ideas!"
  - type: textarea
    id: use-case
    attributes:
      label: "Use Case"
      description: "What problem does this solve or what capability does it add?"
      placeholder: "I want to export the drift audit results as a CSV so I can share with my team..."
    validations:
      required: true
  - type: dropdown
    id: mode
    attributes:
      label: "Which Mode(s)?"
      multiple: true
      options:
        - Found
        - Assess
        - Revive
        - Reposition
        - History
        - All Modes
  - type: textarea
    id: details
    attributes:
      label: "Additional Details"
      placeholder: "Any other context or mockups?"
```

**3. .github/ISSUE_TEMPLATE/question.yml:**
```yaml
name: Question
description: Ask a question about using Compass
title: "[Question] "
labels: ["question"]
body:
  - type: markdown
    attributes:
      value: "Have a question? We're here to help."
  - type: textarea
    id: question
    attributes:
      label: "Your Question"
      description: "What do you want to know?"
      placeholder: "How do I...?"
    validations:
      required: true
  - type: textarea
    id: context
    attributes:
      label: "Context"
      description: "What were you trying to do when you had this question?"
      placeholder: "I was in Assess mode and noticed..."
```

**4. .github/ISSUE_TEMPLATE/config.yml:**
```yaml
blank_issues_enabled: false
contact_links:
  - name: GitHub Discussions
    url: https://github.com/Tr1ckyMag1ca1/paperclip-plugin-compass/discussions
    about: Ask questions or start discussions with the community
```

**5. .github/PULL_REQUEST_TEMPLATE.md** (update if exists, or create):
```markdown
## Description
What does this PR do?

## Related Issue
Closes #(issue number) or fixes (Compass mode/feature).

## Type of Change
- [ ] Bug fix
- [ ] New feature
- [ ] Documentation update
- [ ] Refactor

## Testing
- [ ] Added/updated tests
- [ ] Manual testing on VPS instance (if UI changes)
- [ ] `npm run verify` passes locally
- [ ] Zero console errors in plugin panels

## Checklist
- [ ] Code follows project conventions (TypeScript strict mode, React hooks)
- [ ] README or docs updated (if needed)
- [ ] Changes are small and focused (one feature per PR)
- [ ] Commit messages are clear and atomic
```

After creating, verify YAML syntax by reading back and checking indentation.
  </action>
  <verify>
    <automated>test -f .github/ISSUE_TEMPLATE/bug-report.yml && test -f .github/ISSUE_TEMPLATE/feature.yml && test -f .github/ISSUE_TEMPLATE/question.yml && test -f .github/ISSUE_TEMPLATE/config.yml && test -f .github/PULL_REQUEST_TEMPLATE.md && echo "All templates created"</automated>
  </verify>
  <done>GitHub issue templates (YAML forms) + PR template (markdown) created per D-15. Blank issues disabled.</done>
</task>

<task type="auto">
  <name>Task 4: Verify CODEOWNERS and LICENSE are correct</name>
  <files>CODEOWNERS, LICENSE</files>
  <action>
1. Read CODEOWNERS file; confirm it lists `* @nicholasrhodes @aronprins` (global, all files require approval from both).
2. Read LICENSE file; confirm it contains MIT license text and copyright year is 2026.
3. Cross-check package.json#license field is `MIT` (should be from Wave 1 Task 1).
4. Verify Aron Prins GitHub handle is `@aronprins` (correct; no typos).
5. Document in verification notes: "CODEOWNERS verified for Wave 7 collaborator invite; @aronprins handle matches GitHub registry."

No changes needed unless CODEOWNERS or LICENSE are missing or have typos. If CODEOWNERS is not present or needs update, create/edit it now.
  </action>
  <verify>
    <automated>grep -c "aronprins" CODEOWNERS && grep -c "MIT License" LICENSE</automated>
  </verify>
  <done>CODEOWNERS verified with @nicholasrhodes @aronprins, LICENSE confirmed as MIT with 2026 copyright. Ready for Wave 7 collaborator invite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public documentation (README) | Install instructions, mode overview, screenshots guide user behavior |
| GitHub issue/PR templates | Structured input from contributors; forms validate field presence |
| CODEOWNERS file | Enforces approval routing via GitHub branch protection |
| CHANGELOG | Release notes published to npm and GitHub; version source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Disclosure | Old package name in README causes users to install wrong package | mitigate | README.md updated with scoped @paperclipai name; old unscoped version deprecated post-Wave-7 publish |
| T-11-08 | Integrity | CHANGELOG versions don't match package.json version at publish time | mitigate | Wave 7 tags with version matching CHANGELOG; GitHub release notes auto-derived from CHANGELOG |
| T-11-09 | Information | Contributor submits PR without required information (no test coverage, no description) | mitigate | PR template includes checklist; GitHub branch protection can require PR approvals (enforced via CODEOWNERS) |
| T-11-10 | Information | Unauthorized collaborator edits CODEOWNERS to bypass approval requirement | mitigate | CODEOWNERS file in repo; branch protection on main prevents direct edits without PR |
</threat_model>

<verification>
After all four tasks complete, run:
1. `grep "^@paperclipai" README.md` — confirm scoped package name appears in install section
2. `grep -c "Aron Prins\|paperclip-vision" README.md` — confirm credit and lineage link present
3. `test -f CHANGELOG.md && grep "1.1.0\|1.0.0" CHANGELOG.md` — confirm both versions exist
4. `grep "blank_issues_enabled: false" .github/ISSUE_TEMPLATE/config.yml` — confirm blank issues disabled
5. `grep "aronprins" CODEOWNERS` — confirm Aron listed as code owner

These checks verify all OSS documentation is in place before Wave 5 CI integration and Wave 7 publish.
</verification>

<success_criteria>
- README.md includes scoped install snippet, Aron Prins credit, paperclip-vision lineage link, badges (npm version, license), and references to mode screenshots
- CHANGELOG.md initialized with v1.0 and v1.1 entries in keepachangelog format, phase-level aggregation
- GitHub issue templates created (bug, feature, question) in YAML form syntax with required fields
- GitHub PR template created with contributor checklist
- config.yml disables blank issues
- CODEOWNERS verified with @nicholasrhodes @aronprins listed globally
- LICENSE confirmed as MIT with 2026 copyright; matches package.json#license field
</success_criteria>

<output>
After completion, create `.planning/phases/11-distribution-open-source-launch/11-03-SUMMARY.md`
</output>
