---
phase: 10
plan: 01
type: auto
wave: 1
depends_on: [09-03]
files_modified:
  - src/ui/memory/HistoryPanel.tsx
  - src/ui/memory/FindingCard.tsx
  - src/ui/memory/ScheduleRoutineRow.tsx
autonomous: true
requirements: [UIM-01, UIM-02, UIM-04]
user_setup: []

must_haves:
  truths:
    - "HistoryPanel uses host tokens (px-4 py-2, gap-2, text-xs font-medium) instead of broken custom tokens (px-lg, py-sm, gap-md, text-label)"
    - "FindingCard and FindingStatusBadge use host tokens (px-1 py-1, text-xs font-medium) instead of broken custom tokens (px-xs, py-xs, text-label)"
    - "ScheduleRoutineRow uses host tokens (px-3 py-2, gap-2, text-xs font-medium) instead of broken custom tokens"
    - "Token migration matches Phase 7 mechanical approach: direct class swap, no logic changes"
    - "All three files render without layout collapse or missing spacing in both light and dark modes"
  artifacts:
    - path: "src/ui/memory/HistoryPanel.tsx"
      provides: "Root history panel container migrated to host tokens (28 broken tokens fixed)"
      exports: ["HistoryPanel"]
    - path: "src/ui/memory/FindingCard.tsx"
      provides: "Finding card display + FindingStatusBadge migrated (18 broken tokens fixed)"
      exports: ["FindingCard", "FindingStatusBadge"]
    - path: "src/ui/memory/ScheduleRoutineRow.tsx"
      provides: "Schedule routine row display migrated (15 broken tokens fixed)"
      exports: ["ScheduleRoutineRow"]
  key_links:
    - from: "HistoryPanel"
      to: "FindingCard, ScheduleRoutineRow"
      via: "Component composition (renders arrays of findings/routines)"
      pattern: "className.*gap-2|px-4|py-2|text-xs font-medium"
    - from: "FindingCard"
      to: "FindingStatusBadge, ModeBadge"
      via: "Nested badge components"
      pattern: "className.*px-1 py-1|text-xs font-medium"
    - from: "ScheduleRoutineRow"
      to: "HistoryPanel"
      via: "Rendered as list item in SchedulesTab"
      pattern: "className.*gap-2|px-3"

---

<objective>
Migrate three highest-impact memory components (HistoryPanel, FindingCard, ScheduleRoutineRow) from broken custom tokens to host tokens. These three files account for 61 of 80+ total broken-token references in Phase 10 scope.

Purpose: Establish token migration baseline for remaining memory components; unblock UIV-01 verifier extension in Wave 2.

Output: HistoryPanel, FindingCard, ScheduleRoutineRow fully migrated. All 61 broken tokens replaced with host equivalents. Ready for Phase 10 verification gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-memory-verification-documentation/10-RESEARCH.md
@./UI_REDO_HANDOFF.md
@./CLAUDE.md
</context>

<interfaces>
<!-- Key types and existing memory component signatures -->

From src/ui/memory/:
```typescript
// HistoryPanel root container
export function HistoryPanel(props: HistoryPanelProps): React.ReactElement

// Finding card display
interface FindingCardProps {
  finding: EngagementFinding;
  onStatusChange: (status: FindingStatus) => void;
  onDelete: () => void;
}
export function FindingCard(props: FindingCardProps): React.ReactElement

// Finding status badge
interface FindingStatusBadgeProps {
  status: "open" | "addressed" | "invalidated";
}
export function FindingStatusBadge(props: FindingStatusBadgeProps): React.ReactElement

// Schedule routine row
interface ScheduleRoutineRowProps {
  routine: ScheduledRoutine;
  onEdit: () => void;
  onDelete: () => void;
}
export function ScheduleRoutineRow(props: ScheduleRoutineRowProps): React.ReactElement
```

Token migration reference (from RESEARCH.md):
```
text-label → text-xs font-medium
text-body → text-sm
text-heading → text-base font-semibold
gap-xs → gap-1, gap-sm → gap-2, gap-md → gap-3
px-xs → px-1, px-sm → px-2, px-md → px-3, px-lg → px-4
py-xs → py-1, py-sm → py-2, py-md → py-3, py-lg → py-4
mt-xs → mt-1, mt-sm → mt-2, mt-md → mt-4
pb-sm → pb-2
rounded-lg/rounded-xl → rounded-none
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate HistoryPanel to host tokens (28 broken tokens)</name>
  <files>src/ui/memory/HistoryPanel.tsx</files>
  <action>
Migrate HistoryPanel.tsx from broken custom tokens to host tokens. This file has the highest broken-token count (28) in Phase 10 scope and contains the root tab container, history header, and filter UI.

**Broken tokens in HistoryPanel (from RESEARCH.md inventory):**
- 12× `text-label` → `text-xs font-medium`
- 4× `gap-xs` → `gap-1`
- 4× `px-sm` → `px-2`
- 2× `px-lg` → `px-4`
- 2× `py-md` → `py-4`
- 2× `py-sm` → `py-2`
- 2× `mt-sm` → `mt-2`

**Implementation steps:**

1. Search and replace token by token using grep-sed or manual editing:
   ```bash
   # Example sed for batch replace (if not using manual edit):
   sed -i 's/text-label/text-xs font-medium/g' src/ui/memory/HistoryPanel.tsx
   sed -i 's/gap-xs/gap-1/g' src/ui/memory/HistoryPanel.tsx
   sed -i 's/px-sm/px-2/g' src/ui/memory/HistoryPanel.tsx
   sed -i 's/px-lg/px-4/g' src/ui/memory/HistoryPanel.tsx
   sed -i 's/py-md/py-4/g' src/ui/memory/HistoryPanel.tsx
   sed -i 's/py-sm/py-2/g' src/ui/memory/HistoryPanel.tsx
   sed -i 's/mt-sm/mt-2/g' src/ui/memory/HistoryPanel.tsx
   ```

2. Manually verify top-level container className strings are updated (tab bar area, sticky header, filter buttons).

3. Verify nested components (HistoryTabBar function, if internal) also updated.

4. Grep verification: zero hits on all broken tokens.

5. No logic or behavioral changes — token swap only. Button handlers, state management, event listeners remain unchanged.

**Key sections to update:**
- Sticky tab bar (className with gap-xs, px-sm, py-sm)
- Tab button styling (text-label, px-md py-sm)
- Filter header (px-lg, py-md, gap-xs)
- Content container (px-sm, py-sm spacing)

Reference: RESEARCH.md "HistoryPanel" section in "Memory Components with Broken Tokens" table for detailed line-by-line breakdown.
  </action>
  <verify>
    <automated>
      grep -c "text-xs font-medium" src/ui/memory/HistoryPanel.tsx && \
      grep -c "gap-1\|gap-2" src/ui/memory/HistoryPanel.tsx && \
      grep -c "px-2\|px-4" src/ui/memory/HistoryPanel.tsx && \
      grep -c "py-2\|py-4" src/ui/memory/HistoryPanel.tsx && \
      grep -c "mt-2" src/ui/memory/HistoryPanel.tsx && \
      [ $(grep -c "text-label\|gap-xs\|gap-sm\|px-sm\|px-lg\|py-sm\|py-md\|mt-sm" src/ui/memory/HistoryPanel.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    HistoryPanel.tsx migrated. All 28 broken custom tokens replaced with host tokens (text-xs font-medium, gap-1/2, px-2/4, py-2/4, mt-2). No broken tokens remaining. Component logic and behavior unchanged; token swap only. File compiles and renders in both light and dark modes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate FindingCard and FindingStatusBadge to host tokens (18 broken tokens)</name>
  <files>src/ui/memory/FindingCard.tsx</files>
  <action>
Migrate FindingCard.tsx (includes nested FindingStatusBadge) from broken custom tokens to host tokens. This file is the primary display component for individual findings and accounts for 18 broken-token references.

**Broken tokens in FindingCard (from RESEARCH.md inventory):**
- 10× `text-label` → `text-xs font-medium`
- 4× `gap-xs` → `gap-1`
- 2× `px-md` → `px-3`
- 2× `py-xs` → `py-1` (in FindingStatusBadge)
- 1× `px-xs` → `px-1` (in FindingStatusBadge)

**Implementation steps:**

1. Replace broken tokens in FindingCard main component:
   ```bash
   sed -i 's/text-label/text-xs font-medium/g' src/ui/memory/FindingCard.tsx
   sed -i 's/gap-xs/gap-1/g' src/ui/memory/FindingCard.tsx
   sed -i 's/px-md/px-3/g' src/ui/memory/FindingCard.tsx
   ```

2. Replace broken tokens in nested FindingStatusBadge component:
   ```bash
   sed -i 's/py-xs/py-1/g' src/ui/memory/FindingCard.tsx
   sed -i 's/px-xs/px-1/g' src/ui/memory/FindingCard.tsx
   ```

3. Verify FindingStatusBadge className strings:
   - Badge label font size: `text-xs font-medium` (or `text-xs font-bold` for prominence)
   - Badge padding: `px-1 py-1` (tight badge styling)

4. Grep verification: zero hits on all broken tokens.

5. No logic or behavioral changes. Status change handlers, modals, and deletion logic remain unchanged.

**Key sections to update:**
- Finding card container (gap-xs, px-md spacing)
- Finding metadata line (text-label, mt-xs timestamp)
- Status badge styling (px-xs py-xs font sizing)
- Action buttons area (gap-xs button spacing)

Reference: RESEARCH.md "FindingCard" section for detailed breakdown.
  </action>
  <verify>
    <automated>
      grep -c "text-xs font-medium" src/ui/memory/FindingCard.tsx && \
      grep -c "gap-1" src/ui/memory/FindingCard.tsx && \
      grep -c "px-3" src/ui/memory/FindingCard.tsx && \
      grep -c "px-1 py-1" src/ui/memory/FindingCard.tsx && \
      [ $(grep -c "text-label\|gap-xs\|px-md\|py-xs\|px-xs" src/ui/memory/FindingCard.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    FindingCard.tsx and nested FindingStatusBadge migrated. All 18 broken custom tokens replaced (text-xs font-medium, gap-1, px-3, px-1 py-1). No broken tokens remaining. Modal and action handlers unchanged. Component renders correctly in both light and dark modes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate ScheduleRoutineRow to host tokens (15 broken tokens)</name>
  <files>src/ui/memory/ScheduleRoutineRow.tsx</files>
  <action>
Migrate ScheduleRoutineRow.tsx from broken custom tokens to host tokens. This file displays individual scheduled routines and accounts for 15 broken-token references.

**Broken tokens in ScheduleRoutineRow (from RESEARCH.md inventory):**
- 4× `text-label` → `text-xs font-medium`
- 2× `gap-xs` → `gap-1`
- 2× `px-md` → `px-3`
- 2× `py-sm` → `py-2`
- 2× `px-lg` → `px-4`
- 1× `mt-sm` → `mt-2`
- 1× `py-md` → `py-4` (or `py-3`)
- 1× `gap-sm` → `gap-2`

**Implementation steps:**

1. Replace broken tokens:
   ```bash
   sed -i 's/text-label/text-xs font-medium/g' src/ui/memory/ScheduleRoutineRow.tsx
   sed -i 's/gap-xs/gap-1/g' src/ui/memory/ScheduleRoutineRow.tsx
   sed -i 's/gap-sm/gap-2/g' src/ui/memory/ScheduleRoutineRow.tsx
   sed -i 's/px-md/px-3/g' src/ui/memory/ScheduleRoutineRow.tsx
   sed -i 's/px-lg/px-4/g' src/ui/memory/ScheduleRoutineRow.tsx
   sed -i 's/py-sm/py-2/g' src/ui/memory/ScheduleRoutineRow.tsx
   sed -i 's/py-md/py-4/g' src/ui/memory/ScheduleRoutineRow.tsx
   sed -i 's/mt-sm/mt-2/g' src/ui/memory/ScheduleRoutineRow.tsx
   ```

2. Verify routine name label, schedule display, action buttons all use correct tokens.

3. Grep verification: zero hits on all broken tokens.

4. No logic or behavioral changes. Routine state management, edit/delete handlers remain unchanged.

**Key sections to update:**
- Routine row container (px-lg py-md outer padding)
- Routine name field (text-label, py-sm vertical spacing)
- Schedule display/cron rendering (px-md spacing, text-xs font-medium label)
- Action buttons (gap-xs button group spacing)

Reference: RESEARCH.md "ScheduleRoutineRow" section for detailed breakdown. This is the largest single-file broken-token count after HistoryPanel.
  </action>
  <verify>
    <automated>
      grep -c "text-xs font-medium" src/ui/memory/ScheduleRoutineRow.tsx && \
      grep -c "gap-1\|gap-2" src/ui/memory/ScheduleRoutineRow.tsx && \
      grep -c "px-3\|px-4" src/ui/memory/ScheduleRoutineRow.tsx && \
      grep -c "py-2\|py-4" src/ui/memory/ScheduleRoutineRow.tsx && \
      grep -c "mt-2" src/ui/memory/ScheduleRoutineRow.tsx && \
      [ $(grep -c "text-label\|gap-xs\|gap-sm\|px-md\|px-lg\|py-sm\|py-md\|mt-sm" src/ui/memory/ScheduleRoutineRow.tsx) -eq 0 ]
    </automated>
  </verify>
  <done>
    ScheduleRoutineRow.tsx migrated. All 15 broken custom tokens replaced (text-xs font-medium, gap-1/2, px-3/4, py-2/4, mt-2). No broken tokens remaining. Edit/delete handlers and routine state management unchanged. Component renders correctly in both light and dark modes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Token className strings → Tailwind JIT parser | All className strings are static string literals; no dynamic class generation |
| Memory component UI → Host theme context | Components inherit light/dark theme via CSS custom properties from host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering (CSS class consistency) | HistoryPanel, FindingCard, ScheduleRoutineRow | mitigate | All classes are static Tailwind utilities (no template-string injection). Phase 7 verified no dynamic class generation. Grep verifier confirms zero broken patterns. |
| T-10-02 | Information Disclosure (dark-mode color handling) | All memory components | mitigate | All colors use CSS custom properties (host tokens, not hardcoded). Plugin SDK ensures theme inheritance. No hardcoded colors in Phase 10 work. |

**Phase 10 Wave 1 Security Posture:** No change from prior phases. All components remain presentational; no new data handling, API calls, or authentication logic.

</threat_model>

<verification>
## Phase 10 Plan 01 Verification Checklist

**Automated checks:**
- [ ] HistoryPanel: `grep -c "text-xs font-medium" src/ui/memory/HistoryPanel.tsx` returns ≥8
- [ ] HistoryPanel: `[ $(grep -c "text-label\|gap-xs\|px-sm\|px-lg\|py-sm\|py-md\|mt-sm" src/ui/memory/HistoryPanel.tsx) -eq 0 ]`
- [ ] FindingCard: `grep -c "text-xs font-medium" src/ui/memory/FindingCard.tsx` returns ≥8
- [ ] FindingCard: `[ $(grep -c "text-label\|gap-xs\|px-md\|py-xs\|px-xs" src/ui/memory/FindingCard.tsx) -eq 0 ]`
- [ ] ScheduleRoutineRow: `grep -c "text-xs font-medium" src/ui/memory/ScheduleRoutineRow.tsx` returns ≥4
- [ ] ScheduleRoutineRow: `[ $(grep -c "text-label\|gap-xs\|gap-sm\|px-md\|px-lg\|py-sm\|py-md\|mt-sm" src/ui/memory/ScheduleRoutineRow.tsx) -eq 0 ]`
- [ ] All three files compile without TypeScript errors

**Manual checks (post-execution):**
- [ ] HistoryPanel renders in browser without layout collapse
- [ ] FindingCard displays correct spacing and readability
- [ ] ScheduleRoutineRow renders correctly in schedules list
- [ ] No missing spacing (layout gaps) in any component
- [ ] Dark mode rendering has correct colors (no inversions)

**Traceability:**
- [ ] UIM-01 (HistoryPanel migration) addressed by Task 1
- [ ] UIM-02 (FindingCard + FindingStatusBadge migration) addressed by Task 2
- [ ] UIM-04 (ScheduleRoutineRow migration) addressed by Task 3

</verification>

<success_criteria>
1. HistoryPanel migrated from 28 broken custom tokens to host tokens; zero broken tokens remaining.
2. FindingCard (including FindingStatusBadge) migrated from 18 broken custom tokens to host tokens; zero broken tokens remaining.
3. ScheduleRoutineRow migrated from 15 broken custom tokens to host tokens; zero broken tokens remaining.
4. All token replacements follow Phase 7 mechanical approach (direct class swap, no logic changes).
5. All 61 broken tokens across three files replaced with host equivalents.
6. Components render correctly in both light and dark modes without layout collapse or missing spacing.
7. No broken tokens remaining in Wave 1 scope; unblocks Wave 2 verification gates.
</success_criteria>

<output>
After completion, create `.planning/phases/10-memory-verification-documentation/10-01-SUMMARY.md` documenting:
- HistoryPanel migration complete (Task 1 timestamp, 28 broken tokens fixed)
- FindingCard migration complete (Task 2 timestamp, 18 broken tokens fixed)
- ScheduleRoutineRow migration complete (Task 3 timestamp, 15 broken tokens fixed)
- Total 61 broken tokens migrated in Wave 1
- All three files pass token verification (0 broken patterns found)
- Ready for Wave 2 verifier extension and verification gates
</output>
