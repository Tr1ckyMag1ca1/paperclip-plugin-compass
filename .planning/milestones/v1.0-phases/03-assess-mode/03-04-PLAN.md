---
phase: 03
plan: 04
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - src/worker.ts
  - src/ui/MainPanel.tsx
  - tests/assess/assess.integration.spec.ts
  - tests/ui/assess-integration.spec.ts
autonomous: true
requirements:
  - ASSESS-02
  - ASSESS-09
  - XC-09
must_haves:
  truths:
    - "Assess mode handlers registered in worker"
    - "Assess mode routed from MainPanel based on mode detection"
    - "End-to-end Assess flow works: run → review → apply"
    - "End-to-end founder+ceo flow works: run → review → queue approval → CEO decision → apply"
    - "UI and pure logic layers integrate correctly"
  artifacts:
    - path: "src/worker.ts"
      provides: "Assess mode handlers (runDriftAudit, applyAssessment, checkApprovalStatus)"
    - path: "src/ui/MainPanel.tsx"
      provides: "Mode routing to AssessPanel when in Assess mode"
---

<objective>
**Assess Mode: Integration & Worker Handlers**

Wire Assess mode components into the plugin: register worker handlers for drift detection, amendment application, and approval polling; route MainPanel to AssessPanel when in Assess mode; run end-to-end integration tests for both founder-only and founder+ceo approval flows.

Purpose: Complete Assess mode end-to-end integration—all core logic, UI, and worker handlers connected.

Output: 3 worker handlers, MainPanel routing update, 16+ integration test assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-assess-mode/03-CONTEXT.md
@.planning/phases/02-found-mode/02-04-SUMMARY.md (worker handler pattern from Found mode)
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register Assess handlers in worker and wire MainPanel routing</name>
  <files>src/worker.ts, src/ui/MainPanel.tsx