---
phase: 02
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/ui/found/InterviewSection.tsx
  - src/ui/found/QuestionRenderer.tsx
  - src/ui/found/SectionNavRail.tsx
  - src/ui/found/PresetSelector.tsx
  - src/ui/found/VisionPreview.tsx
  - src/ui/found/ProvisioningSummary.tsx
  - src/ui/found/ConfirmationModal.tsx
  - src/ui/found/ApplyProgress.tsx
  - src/ui/found/ApplyErrorDisplay.tsx
  - src/ui/found/InterviewDraftState.ts
  - src/ui/found/index.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-05
  - FOUND-11

must_haves:
  truths:
    - "Interview renders as linear 6-section flow with section nav rail showing progress and back-navigation"
    - "Founder can answer all questions per section (5 question types: free-text-short/long, single/multi-choice, conditional)"
    - "Interview draft persists to Plugin SDK worker-state, survives plugin reload, resumable mid-interview"
    - "Preview screen shows rendered VISION.md (read-only by default) with inline edit toggle (textarea for v1, no CodeMirror)"
    - "Two-stage approval gate enforced: preview screen (edit toggle) + final confirmation modal with explicit 'I confirm' button"
    - "Apply progress shows sequential steps with status (preflight → doc → agents → issues → wakeups)"
    - "Error display shows problem + rollback status + manual cleanup path if needed"
  artifacts:
    - path: "src/ui/found/InterviewSection.tsx"
      provides: "Form rendering for one interview section with all questions, next/back nav, required field validation"
      must_contain: "export function InterviewSection"
    - path: "src/ui/found/QuestionRenderer.tsx"
      provides: "Renders individual question based on type (5 types); handles conditional show-if logic"
      must_contain: "export function QuestionRenderer"
    - path: "src/ui/found/SectionNavRail.tsx"
      provides: "Progress indicator and section jump navigation (backward only); shows current section highlighted"
      must_contain: "export function SectionNavRail"
    - path: "src/ui/found/PresetSelector.tsx"
      provides: "Radio button group for founding preset selection; locked once selected"
      must_contain: "export function PresetSelector"
    - path: "src/ui/found/VisionPreview.tsx"
      provides: "Rendered VISION.md display with inline edit toggle; not CodeMirror (plain textarea)"
      must_contain: "export function VisionPreview"
    - path: "src/ui/found/ProvisioningSummary.tsx"
      provides: "Read-only summary: agents to create, issues to file, write counts"
      must_contain: "export function ProvisioningSummary"
    - path: "src/ui/found/ConfirmationModal.tsx"
      provides: "Final gate: modal with 'I confirm' button, lists all writes happening"
      must_contain: "export function ConfirmationModal"
    - path: "src/ui/found/ApplyProgress.tsx"
      provides: "Sequential step indicator: preflight → doc → agents → issues → wakeups; shows status per step"
      must_contain: "export function ApplyProgress"
    - path: "src/ui/found/ApplyErrorDisplay.tsx"
      provides: "Error message with rollback status and manual recovery steps"
      must_contain: "export function ApplyErrorDisplay"
    - path: "src/ui/found/InterviewDraftState.ts"
      provides: "Hook: useInterviewDraft(companyId) loads/saves interview answers to worker-state"
      must_contain: "export function useInterviewDraft"
  key_links:
    - from: "src/ui/found/InterviewSection.tsx"
      to: "src/ui/found/QuestionRenderer.tsx"
      via: "InterviewSection maps questions to QuestionRenderer"
      pattern: "question.questions.map.*QuestionRenderer"
    - from: "src/ui/found/InterviewSection.tsx"
      to: "src/ui/found/SectionNavRail.tsx"
      via: "FoundPanel passes section list to rail for progress tracking"
      pattern: "SectionNavRail.*sections=.*currentSection"
    - from: "src/ui/found/VisionPreview.tsx"
      to: "src/ui/found/ProvisioningSummary.tsx"
      via: "Preview displays provisioning summary below VISION"
      pattern: "ProvisioningSummary.*preset"
    - from: "src/ui/found/ConfirmationModal.tsx"
      to: "src/ui/found/ProvisioningSummary.tsx"
      via: "Confirm modal also shows provisioning summary"
      pattern: "ProvisioningSummary.*preset"
    - from: "src/ui/found/InterviewDraftState.ts"
      to: "src/ui/MainPanel.tsx"
      via: "Hook integrates with Plugin SDK usePluginData/usePluginAction"
      pattern: "usePluginData.*usePluginAction"

---

<objective>
Implement full interview UX with two-stage approval gate and apply progress UI.

Purpose: Founder flows through 6-section interview (linear with back-nav), answers persist across reload (draft state), previews VISION.md and can inline-edit, confirms via two-stage gate (preview + modal), watches Apply progress with error recovery. All UI uses Paperclip design tokens (no custom CSS). All components are pure presentation — logic in Wave 1/2 modules.

Output: 10 React components (8 presentational + 1 hook + 1 index barrel export) ready for orchestration in Wave 4 FoundPanel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Key References

From Phase 2 planning:
- `.planning/phases/02-found-mode/02-UI-SPEC.md` — complete visual + interaction contract (sections 3-7 spell out component behavior)
- `.planning/phases/02-found-mode/02-PATTERNS.md` (lines 420–1080+ showing component code patterns)

From Phase 1:
- `src/ui/MainPanel.tsx`, `src/ui/components/ChatPanel.tsx` — establish Tailwind token pattern and Plugin SDK hook usage
- `src/ui/components/ModeBanner.tsx`, `src/ui/components/AgentCard.tsx` — examples of design-token usage (px-lg, text-body, etc.)
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-found-mode/02-CONTEXT.md
@.planning/phases/02-found-mode/02-UI-SPEC.md
@.planning/phases/02-found-mode/02-PATTERNS.md

### Design Token Classes (Inherited from Paperclip Host)

From Phase 1 UI and UI-SPEC (inherited from Paperclip host):

**Spacing:** px-xs, py-xs, gap-xs, ... px-3xl, py-3xl, gap-3xl (8-point scale)
**Typography:** text-body, text-label, text-heading, text-display + font-normal, font-bold
**Colors:** bg-background, bg-card, text-foreground, text-foreground/70, text-accent, text-destructive
**Borders:** border-border, ring-accent
**State:** disabled:opacity-50, hover:bg-card, focus:ring-2

Do NOT add custom CSS files or Tailwind config. All classes from host design tokens.

### Interview Section Titles (Fixed, from UI-SPEC)

1. "Big Picture"
2. "Revenue & Customers"
3. "Growth & Marketing"
4. "Product Direction"
5. "CEO Autonomy"
6. "Vision & Identity"

Each title must match verbatim.

### Plugin SDK Hook Pattern (From Phase 1 MainPanel.tsx)

```typescript
import { usePluginData, usePluginAction } from "@paperclipai/plugin-sdk/ui";

// In component:
const { data, loading, error } = usePluginData("handlerName", { param: value });
const action = usePluginAction("actionName");
```

InterviewDraftState.ts hook must follow this pattern.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement form components (InterviewSection, QuestionRenderer, SectionNavRail)</name>
  <files>
    - src/ui/found/InterviewSection.tsx
    - src/ui/found/QuestionRenderer.tsx
    - src/ui/found/SectionNavRail.tsx
  </files>
  <read_first>
    - `.planning/phases/02-found-mode/02-UI-SPEC.md` (sections 3-4: Interview Flow, Question Renderer, Section Navigation Rail)
    - `.planning/phases/02-found-mode/02-PATTERNS.md` (lines 421–610 showing component code patterns for InterviewSection, QuestionRenderer, SectionNavRail)
    - `src/ui/components/ChatPanel.tsx` or `src/ui/MainPanel.tsx` from Phase 1 (design token usage pattern)
  </read_first>
  <action>
Create 3 form components. All use Paperclip design tokens (px-*, text-*, bg-card, etc.) — no custom CSS. All are controlled components (accept value + onChange from parent). No internal state management.

**1. `src/ui/found/QuestionRenderer.tsx`**

Renders a single question based on type. Props:
- `question: Question` (from src/types/found.ts)
- `value: string` (current answer)
- `onChange: (value: string) => void` (callback on change)

Handles 5 question types (per D-03, UI-SPEC lines 154–157):
1. **free-text-short** → `<input type="text" maxLength={200} />`
2. **free-text-long** → `<textarea rows={4} maxLength={2000} />`
3. **single-choice** → Radio buttons (vertically stacked)
4. **multi-choice** → Checkboxes (vertically stacked); value is comma-separated string
5. **conditional-follow-up** → Appears only if parent question answered with triggering value

Logic for conditional:
- Check question.showIf (parent question ID + equals/includes condition)
- Look up parent question answer in answers record (parent passed via props)
- Only render if condition met

Styling:
- Label: `text-label font-normal` with required indicator (red `*` if required)
- Input: `px-md py-sm rounded border border-border bg-background text-body focus:ring-2 focus:ring-accent`
- Hint: `text-label text-foreground/70 mt-xs`

**2. `src/ui/found/InterviewSection.tsx`**

Renders all questions in a section. Props:
- `section: InterviewSection` (title, intro, questions)
- `answers: Record<string, string>` (all answers keyed by question ID)
- `onAnswersChange: (answers: Record<string, string>) => void` (callback when any answer changes)
- `onNavigate: (direction: "next" | "back") => void` (callback for nav buttons)

Layout:
1. Section title: `<h2 className="text-display font-bold">{section.title}</h2>`
2. Section intro: `<p className="text-body text-foreground/70">{section.intro}</p>` (above questions)
3. Question list: `<div className="space-y-lg">` wrapping all QuestionRenderers
4. Navigation buttons at bottom:
   - "← Back" button (disabled if first section)
   - "Next: [Next Section Name]" button (disabled until all required questions answered)

Validation: Check all required questions have non-empty answers before enabling "Next" button.

Auto-save: Call `onAnswersChange` immediately when user changes answer (no debounce needed for now).

**3. `src/ui/found/SectionNavRail.tsx`**

Progress indicator + section nav. Props:
- `sections: InterviewSection[]` (all 6 sections)
- `currentSectionIndex: number` (currently active section index)
- `completedSections: number[]` (indices of sections with all required answers)
- `onJumpTo: (index: number) => void` (callback to jump to a section)

Layout (from UI-SPEC lines 141–146):
- Horizontal row (top or side; let planner decide placement)
- Progress text: "{completed} of {total} sections"
- Buttons for each section:
  - Current section: highlighted (bg-accent, text-accent-foreground)
  - Completed prior sections: clickable, can jump back (bg-card with accent hover)
  - Future sections: disabled (grayed out, opacity-50)
  - Completed sections: show CheckCircle icon (from lucide-react)

Styling:
- Container: `border-b bg-card px-lg py-sm` (if top rail) or `border-r bg-card` (if side rail)
- Buttons: `shrink-0 flex gap-xs px-md py-sm rounded text-sm font-medium` with conditional classes per state
- Icon: CheckCircle from lucide-react (4px size)

All components follow UI-SPEC exactly. No custom CSS. Use design tokens only.
  </action>
  <verify>
    <automated>grep -c "export function InterviewSection\|export function QuestionRenderer\|export function SectionNavRail" src/ui/found/*.tsx</automated>
  </verify>
  <done>
3 form components created:
- QuestionRenderer: 5 question types (free-text-short/long, single/multi-choice, conditional)
- InterviewSection: layout for one section with all questions and nav buttons
- SectionNavRail: progress indicator with back-nav (jump to prior sections only)

All styled with Paperclip design tokens. No custom CSS. All are controlled components (receive props, no internal state).
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement preview and approval components (VisionPreview, ProvisioningSummary, ConfirmationModal)</name>
  <files>
    - src/ui/found/VisionPreview.tsx
    - src/ui/found/ProvisioningSummary.tsx
    - src/ui/found/ConfirmationModal.tsx
  </files>
  <read_first>
    - `.planning/phases/02-found-mode/02-UI-SPEC.md` (sections 5-6: Preview Screen, Confirmation Modal)
    - `.planning/phases/02-found-mode/02-PATTERNS.md` (lines 706–878 showing VisionPreview, ProvisioningSummary, ConfirmationModal code)
  </read_first>
  <action>
Create 3 approval-gate components. These implement FOUND-11 two-stage approval gate per PITFALLS Pitfall 3.

**1. `src/ui/found/VisionPreview.tsx`**

Renders VISION.md with inline edit toggle. Props:
- `vision: FilledVision` (from template-fill.ts)
- `preset: PresetDefinition | null` (selected preset)
- `onBack: () => void` (back to interview)
- `onConfirm: () => void` (proceed to confirmation modal)

Layout (from UI-SPEC lines 172–197):
1. Header: "Here's the company you're founding. Edit anything before you apply."
2. Edit button (toggle): "Edit" → switches to textarea, changes to "Done editing"
3. VISION.md display:
   - Read-only by default: render markdown as HTML (simple: split on ##, render headers + paragraphs)
   - Edit mode: textarea with VISION.md markdown source
4. Provisioning summary (below VISION): shows agents, issues, write counts
5. Action buttons: "Back to interview" (left), "Confirm & apply" (right)

Edit toggle logic (per D-05, UI-SPEC lines 184–187):
- Toggle on: show textarea with vision.body as value
- User edits markdown source
- Toggle off ("Done editing"): validate no empty required slots, then close editing mode
- If validation fails: show error, stay in editing mode

Validation on toggle-off: Use checkVisionQuality from quality-check.ts. If not isValid, show error message inline.

Markdown rendering (simple for v1):
- Split on `##` for headers
- Render as divs with `text-heading font-bold` for headers, `text-body` for paragraphs
- Or use simple `marked` library if available; for v1, acceptable to keep it basic

**2. `src/ui/found/ProvisioningSummary.tsx`**

Displays what will be created. Props:
- `preset: PresetDefinition | null` (selected preset with agents)

Layout (from UI-SPEC lines 189–193):
- Section header: "When you apply"
- **Agents to create:** List each agent name + role
- **Kickoff issues:** Count + brief description
- **Write count box:** "Total: {doc_count} document, {agent_count} agents, {issue_count} issues, {wakeup_count} wakeups"

Styling:
- Box: `bg-card p-md rounded`
- Lists: unordered list with small text, muted color (text-foreground/70)

Read-only display, no interaction.

**3. `src/ui/found/ConfirmationModal.tsx`**

Final approval gate (FOUND-11 Stage 2). Props:
- `vision: FilledVision` (what's being applied)
- `onConfirm: () => void` (proceed to Apply)
- `onCancel: () => void` (back to preview)

Layout (from UI-SPEC lines 199–219):
1. Modal backdrop: fixed overlay, `bg-black/50`, center content
2. Modal box: `bg-background border border-border rounded-lg shadow-lg max-w-md`
3. Header: "Apply changes to Paperclip"
4. Body:
   ```
   Apply will:
   - Write the company vision document
   - Create [N] agents
   - File [N] kickoff issues
   - Queue wakeups to start the company heartbeating
   
   This is reversible only by manual cleanup in Paperclip.
   
   Write count: 1 document, [N] agents, [N] issues, [N] wakeups
   ```
5. Buttons:
   - "Cancel" (secondary, left): returns to preview
   - "I confirm — apply changes" (primary/accent, right): calls onConfirm

Copy is explicit per UI-SPEC copywriting contract. No ambiguity (not "OK" or "Submit", but "I confirm — apply changes").

Modal focus management: Optional for v1, but good practice — trap focus inside modal, return focus to preview button on cancel.

All components follow UI-SPEC exactly. Use design tokens. No custom CSS.
  </action>
  <verify>
    <automated>grep -c "export function VisionPreview\|export function ProvisioningSummary\|export function ConfirmationModal" src/ui/found/*.tsx</automated>
  </verify>
  <done>
3 approval-gate components created:
- VisionPreview: read-only VISION display with inline edit toggle (textarea, not CodeMirror); validation on toggle-off
- ProvisioningSummary: list of agents, issues, write counts
- ConfirmationModal: final "I confirm" gate with explicit language, backdrop overlay, dual-button action

All styled with design tokens. VisionPreview enforces required slot validation. ConfirmationModal implements FOUND-11 Stage 2.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Apply progress and error display components</name>
  <files>
    - src/ui/found/ApplyProgress.tsx
    - src/ui/found/ApplyErrorDisplay.tsx
  </files>
  <read_first>
    - `.planning/phases/02-found-mode/02-UI-SPEC.md` (section 7: Apply Progress UI, On failure)
    - `.planning/phases/02-found-mode/02-PATTERNS.md` (lines 1145–1280 showing ApplyProgress and ApplyErrorDisplay code)
  </read_first>
  <action>
Create 2 status display components for the Apply flow.

**1. `src/ui/found/ApplyProgress.tsx`**

Shows sequential progress through 5 steps. Props:
- `step: "preflight" | "doc" | "agents" | "issues" | "wakeups" | "complete" | "error"` (current step)
- `progress: { [key: string]: boolean }` (completed steps, keyed by step name)

Layout (from UI-SPEC lines 224–249):
Sequential step list with status icons:
1. Preflight checks (✓ when complete, ⏳ spinner when current, ○ not reached)
2. Vision doc (✓/⏳/○)
3. Agents (✓/⏳/○)
4. Issues (✓/⏳/○)
5. Wakeups (✓/⏳/○)

Each row:
- Icon (left): CheckCircle (✓), Loader (⏳ with animation), or empty circle (○)
- Label (right): "Validating setup…" (preflight), "Writing vision document…" (doc), etc.

After all steps complete:
- Show success message: "✓ Company founded!"
- Subtext: "Your new company is now heartbeating. Check the inbox for kickoff issues."
- Button: "Go to company" (closes plugin, navigates to company)

Styling:
- Container: `space-y-md`
- Row: `flex gap-md items-start`
- Icon: `h-5 w-5` (CheckCircle: text-accent, Loader: animate-spin, circle: border-border)
- Text: `text-body`
- Success box: `bg-accent/10 border border-accent rounded p-lg mt-lg`

No interaction — pure status display.

**2. `src/ui/found/ApplyErrorDisplay.tsx`**

Shows error with recovery path. Props:
- `error: ApplyResult` (from apply.ts, with success=false, errors[], rollbackApplied, etc.)
- `onRetry: () => void` (retry Apply)
- `onClose: () => void` (return to preview)

Layout (from UI-SPEC lines 251–264):
1. Error banner: `bg-destructive/10 border border-destructive rounded p-lg`
2. Error icon (left): AlertTriangle (lucide-react)
3. Error heading: "Apply failed: {step name}"
4. Error details:
   - "Error: [error message]" (first error from error.errors[])
   - If rollback happened: "Rollback completed: ✓ [rollback action]" for each action
   - Or: "Rollback failed: [rollback error]"
5. Next steps: "Check your company and retry if needed."
6. Buttons:
   - "Close" (secondary, left)
   - "Retry" (primary, right)

Styling:
- Banner: destructive color scheme (bg-destructive/10, border-destructive, text-destructive)
- Error code: `font-mono text-sm text-foreground/70 bg-background p-md rounded`
- Rollback checklist: `text-label text-foreground/70` with ✓ prefix

Error message should be founder-readable (not stack traces). From apply.ts, errors are already descriptive.

No retry state management here — onRetry callback triggers parent (FoundPanel) to call Apply again.

All components follow UI-SPEC exactly. Use design tokens. No custom CSS.
  </action>
  <verify>
    <automated>grep -c "export function ApplyProgress\|export function ApplyErrorDisplay" src/ui/found/*.tsx</automated>
  </verify>
  <done>
2 status display components created:
- ApplyProgress: sequential step indicator with 5 steps (preflight → doc → agents → issues → wakeups); shows ✓/⏳/○ icons; success message at end
- ApplyErrorDisplay: error banner with problem statement, rollback status, manual recovery path, retry button

Both styled with design tokens. Both are pure status displays (no internal state).
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement PresetSelector and draft state hook</name>
  <files>
    - src/ui/found/PresetSelector.tsx
    - src/ui/found/InterviewDraftState.ts
    - src/ui/found/index.ts
  </files>
  <read_first>
    - `.planning/phases/02-found-mode/02-CONTEXT.md` (D-12, D-14 on preset selection placement and draft persistence)
    - `.planning/phases/02-found-mode/02-PATTERNS.md` (lines 1027–1084 for PresetSelector, lines 883–932 for InterviewDraftState hook)
    - `src/ui/MainPanel.tsx` from Phase 1 (Plugin SDK hook pattern)
  </read_first>
  <action>
Create preset selector component, draft state hook, and barrel export.

**1. `src/ui/found/PresetSelector.tsx`**

Renders founding preset options. Props:
- `presets: PresetDefinition[]` (available presets from company-wizard)
- `selected: string | null` (selected preset ID)
- `onSelect: (presetId: string) => void` (callback when selected)
- `disabled?: boolean` (locked after selection)

Layout (from UI-SPEC lines 167–171):
- Label: "Choose a founding preset" with required indicator (*)
- Radio buttons (vertically stacked):
  - For each preset: radio input + name + description
  - Selected radio: highlighted (checked)
  - Hover: `hover:bg-card` on row
  - Disabled (after selection): opacity-50

Styling:
- Container: `space-y-md`
- Label: `text-label font-medium`
- Radio row: `flex gap-md p-md border border-border rounded hover:bg-card cursor-pointer transition-colors`
- Preset name: `text-body font-medium`
- Preset description: `text-label text-foreground/70`

Behavior (per D-14, UI-SPEC lines 169):
- Once selected, preset is locked (cannot change during interview — prevents branching question re-renders)
- If disabled=true, all radio inputs are disabled and opacity-50

**2. `src/ui/found/InterviewDraftState.ts` (Hook)**

Manages interview draft persistence across reload. Per D-12, stored in Plugin SDK worker-state.

Export:
- `useInterviewDraft(companyId: string): { draft: InterviewAnswers | null, loading: boolean, saveDraft, clearDraft, loadDraft }`

Logic (from PATTERNS.md lines 901–932):
```typescript
import { useCallback, useEffect, useState } from "react";
import { usePluginData, usePluginAction } from "@paperclipai/plugin-sdk/ui";
import type { InterviewAnswers } from "../types.js";

const DRAFT_STATE_KEY = "compass:found:draft";

export function useInterviewDraft(companyId: string) {
  const [draft, setDraft] = useState<InterviewAnswers | null>(null);
  const [loading, setLoading] = useState(true);

  // Load draft on mount
  useEffect(() => {
    const loadDraft = async () => {
      // Call Plugin SDK to load draft from worker-state
      // Pattern: ctx.state.get({ scopeKind: "company", scopeId: companyId, namespace: DRAFT_STATE_KEY, stateKey: "current" })
      // For now, mock: assume SDK provides a getData handler
      // Real implementation: wire a worker handler in Wave 4
      setLoading(false);
    };
    loadDraft();
  }, [companyId]);

  // Save draft on change (debounced)
  const saveDraft = useCallback(
    (answers: InterviewAnswers) => {
      setDraft(answers);
      // Call SDK to persist to worker-state
      // Real implementation: wire worker handler
    },
    []
  );

  const clearDraft = useCallback(() => {
    setDraft(null);
    // Call SDK to delete draft
  }, []);

  return { draft, loading, saveDraft, clearDraft };
}
```

Notes:
- Hook returns draft object + loading state + save/clear callbacks
- saveDraft is called by parent (FoundPanel) after each answer change
- clearDraft is called on successful Apply or explicit "Discard"
- Worker-side implementation (handlers) comes in Wave 4 FoundPanel

**3. `src/ui/found/index.ts` (Barrel Export)**

Export all Found mode UI components:
```typescript
export { InterviewSection } from "./InterviewSection.js";
export { QuestionRenderer } from "./QuestionRenderer.js";
export { SectionNavRail } from "./SectionNavRail.js";
export { PresetSelector } from "./PresetSelector.js";
export { VisionPreview } from "./VisionPreview.js";
export { ProvisioningSummary } from "./ProvisioningSummary.js";
export { ConfirmationModal } from "./ConfirmationModal.js";
export { ApplyProgress } from "./ApplyProgress.js";
export { ApplyErrorDisplay } from "./ApplyErrorDisplay.js";
export { useInterviewDraft } from "./InterviewDraftState.js";
```

Allows Wave 4 FoundPanel to `import * as FoundUI from "./found"` or selectively import.

All 10 exports match pattern map exactly.
  </action>
  <verify>
    <automated>grep -c "export function PresetSelector\|export function useInterviewDraft" src/ui/found/*.tsx src/ui/found/*.ts</automated>
  </verify>
  <done>
3 final components created:
- PresetSelector: radio button group for founding preset; locked after selection
- InterviewDraftState: hook for loading/saving interview draft to worker-state; survives reload
- index.ts: barrel export of all 10 Found mode UI components and hook

All styled with design tokens. Hook ready for wiring in Wave 4 FoundPanel.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Interview input boundary | Founder-typed text in form inputs (untrusted input) |
| Preview edit boundary | Founder edits VISION markdown source in textarea (untrusted input) |
| Two-stage gate boundary | Preview + Confirmation modal enforce approval before writing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | Interview form inputs | accept | Founder data is text/markdown; no XSS vectors in form rendering (no dangerouslySetInnerHTML) |
| T-02-14 | Spoofing | Preset selection | mitigate | Presets from company-wizard (trusted source); locked after selection to prevent re-render attacks |
| T-02-15 | Repudiation | Two-stage approval gate | mitigate | Explicit "I confirm" button prevents accidental apply; modal makes intent clear |
| T-02-16 | Information Disclosure | VISION preview in textarea | accept | Founder is author; editing markdown is normal; no secrets expected in VISION |
| T-02-17 | Denial of Service | Form validation blocking input | mitigate | Max length on text inputs (200/2000 chars per UI-SPEC); prevents unbounded input |
| T-02-18 | Elevation of Privilege | Bypass two-stage gate in UI | mitigate | Gate is enforced in parent FoundPanel (Wave 4); no code path skips from interview → apply (must go through preview → confirm) |

## Two-Stage Approval Gate (FOUND-11, PITFALLS Pitfall 3)

Implemented across 3 components:
1. **VisionPreview:** Stage 1 (preview + optional edit)
2. **ConfirmationModal:** Stage 2 (final "I confirm" gate)
3. **InterviewSection:** Prevents jumping to Apply without finishing interview

Both stages are UX-enforced (modal, explicit button). Parent FoundPanel enforces state flow (cannot skip).

</threat_model>

<verification>
After executing all tasks in this plan:

1. **Form components exist:** grep returns 3 (InterviewSection, QuestionRenderer, SectionNavRail)
2. **Approval-gate components exist:** grep returns 3 (VisionPreview, ProvisioningSummary, ConfirmationModal)
3. **Status display components exist:** grep returns 2 (ApplyProgress, ApplyErrorDisplay)
4. **PresetSelector exists:** grep returns 1
5. **Draft state hook exists:** grep returns useInterviewDraft
6. **Barrel export exists:** src/ui/found/index.ts re-exports all 10 modules
7. **Design tokens only:** `grep -r "custom-\|@import\|style=" src/ui/found` returns empty (no custom CSS)
8. **Lucide icons imported:** grep for "from lucide-react" shows CheckCircle, Loader, AlertTriangle, Edit2 imported
9. **Plugin SDK hooks used:** grep for "usePluginData\|usePluginAction" shows hooks available for later wiring
10. **TypeScript strict mode:** `npx tsc --noEmit src/ui/found/*.tsx src/ui/found/*.ts` passes

All components are styled with design tokens. All are presentation-only (no business logic, logic from Wave 1/2). All are ready for Wave 4 FoundPanel orchestration.
</verification>

<success_criteria>
- [x] 10 exported components + hooks (8 React components + 1 hook + 1 barrel export)
- [x] All components follow UI-SPEC exactly (copy, layout, interaction)
- [x] All styled with Paperclip design tokens only (no custom CSS)
- [x] Interview flow: 6-section linear progression with section nav rail (back-nav only)
- [x] Question types: free-text-short/long, single-choice, multi-choice, conditional show-if all working
- [x] Draft persistence: useInterviewDraft hook loads/saves to worker-state
- [x] Two-stage approval gate: Preview + Confirmation modal with explicit language
- [x] Inline edit: VisionPreview has toggle for textarea editing (not CodeMirror)
- [x] Apply progress: Sequential 5-step indicator with ✓/⏳/○ icons
- [x] Error display: Shows problem + rollback status + retry button
- [x] Preset selection: Radio button group, locked after selection
- [x] All components are controlled (receive props, no internal state except UI-local state)
- [x] TypeScript strict mode — all files pass `tsc --noEmit`
- [x] Lucide icons imported (CheckCircle, Loader, AlertTriangle, Edit2)
- [x] Plugin SDK hooks available for Wave 4 wiring

Wave 3 complete. Ready for Wave 4 (FoundPanel orchestrator + integration tests).
</success_criteria>

<output>
After execution, create `.planning/phases/02-found-mode/02-03-SUMMARY.md` with:
- List of 10 components created (8 React + 1 hook + 1 barrel export)
- Component breakdown by role (form, approval, status)
- Design system summary (tokens only, no custom CSS)
- Two-stage approval gate implementation summary (FOUND-11, PITFALLS Pitfall 3)
- Draft state persistence summary (worker-state via hook)
- Link to next plan (02-04)
- Status: ✓ COMPLETE
- Commits: reference conventional commit hashes
</output>
