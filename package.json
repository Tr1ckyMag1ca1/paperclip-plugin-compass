{
  "name": "paperclip-plugin-compass",
  "version": "1.1.5",
  "type": "module",
  "private": false,
  "description": "Strategic consultant for AI company lifecycle — diagnose, found, revive, reposition",
  "keywords": [
    "paperclip",
    "plugin",
    "compass",
    "strategy",
    "ai-company",
    "consultant"
  ],
  "author": "Paperclip AI",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Tr1ckyMag1ca1/paperclip-plugin-compass.git"
  },
  "paperclipPlugin": {
    "manifest": "./dist/manifest.js",
    "worker": "./dist/worker.js",
    "ui": "./dist/ui"
  },
  "scripts": {
    "build": "node ./esbuild.config.mjs",
    "dev": "node ./esbuild.config.mjs --watch",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint:shim": "node ./scripts/check-react-shim.mjs",
    "size": "size-limit",
    "verify:broken-patterns": "node ./scripts/verify-broken-patterns.mjs",
    "verify": "npm run typecheck && npm run lint:shim && npm run test:run && npm run size && node ./scripts/check-externals.mjs && npm pack --dry-run"
  },
  "files": [
    "dist/",
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@paperclipai/plugin-sdk": "2026.428.0",
    "lucide-react": "^1.14.0"
  },
  "devDependencies": {
    "@size-limit/preset-app": "^12.1.0",
    "@types/node": ">=20",
    "@types/react": "^19.0.8",
    "esbuild": "^0.27.3",
    "size-limit": "^12.1.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  }
}
